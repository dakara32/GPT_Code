    jobs:
      build-and-report:
        runs-on: ubuntu-latest

        steps:
          # ──────────── デバッグステップ ────────────
          - name: Debug start
            run: echo '🚀 DEBUG: workflow started'
          # ↑この “- name:” から始まる行と、  “run:” は
          #   ・「-」はステップリストなので「steps:」から２つスペース  
          #   ・「run:」はその要素のプロパティなので「-」行からさらに２つスペース  
          #   で揃えてください。
