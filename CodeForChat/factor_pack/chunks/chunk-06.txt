```text
rM[np.ix_(idx, idx)]; s = (score-score.mean())/(score.std()+1e-9)
L1234         return float(s[idx].sum() - lam*((P.sum()-np.trace(P))/2.0))
L1235
L1236     @classmethod
L1237     def swap_local_det(cls, corrM: np.ndarray, score: np.ndarray, idx, lam: float=0.6, max_pass: int=15):
L1238         S, best, improved, passes = sorted(idx), cls._obj(corrM, score, idx, lam), True, 0
L1239         while improved and passes<max_pass:
L1240             improved, passes = False, passes+1
L1241             for i,out in enumerate(list(S)):
L1242                 for inn in range(len(score)):
L1243                     if inn in S: continue
L1244                     cand = sorted(S[:i]+[inn]+S[i+1:]); v = cls._obj(corrM, score, cand, lam)
L1245                     if v>best+1e-10: S, best, improved = cand, v, True; break
L1246                 if improved: break
L1247         return S, best
L1248
L1249     @staticmethod
L1250     def _obj_with_cross(C_within: np.ndarray, C_cross: np.ndarray|None, score: np.ndarray, idx, lam: float, mu: float) -> float:
L1251         idx = list(idx); P = C_within[np.ix_(idx, idx)]; s = (score-score.mean())/(score.std()+1e-9)
L1252         within = (P.sum()-np.trace(P))/2.0; cross = 0.0
L1253         if C_cross is not None and C_cross.size>0: cross = C_cross[idx,:].sum()
L1254         return float(s[idx].sum() - lam*within - mu*cross)
L1255
L1256     @classmethod
L1257     def swap_local_det_cross(cls, C_within: np.ndarray, C_cross: np.ndarray|None, score: np.ndarray, idx, lam: float=0.6, mu: float=0.3, max_pass: int=15):
L1258         S, best, improved, passes, N = sorted(idx), cls._obj_with_cross(C_within,C_cross,score,idx,lam,mu), True, 0, len(score)
L1259         while improved and passes<max_pass:
L1260             improved, passes = False, passes+1
L1261             for i,out in enumerate(list(S)):
L1262                 for inn in range(N):
L1263                     if inn in S: continue
L1264                     cand = sorted(S[:i]+[inn]+S[i+1:]); v = cls._obj_with_cross(C_within,C_cross,score,cand,lam,mu)
L1265                     if v>best+1e-10: S, best, improved = cand, v, True; break
L1266                 if improved: break
L1267         return S, best
L1268
L1269     @staticmethod
L1270     def avg_corr(C: np.ndarray, idx) -> float:
L1271         k = len(idx); P = C[np.ix_(idx, idx)]
L1272         return float((P.sum()-np.trace(P))/(k*(k-1)+1e-12))
L1273
L1274     @classmethod
L1275     def select_bucket_drrs(cls, returns_df: pd.DataFrame, score_ser: pd.Series, pool_tickers: list[str], k: int, *, n_pc: int, gamma: float, lam: float, lookback: int, shrink: float=0.10, g_fixed_tickers: list[str]|None=None, mu: float=0.0):
L1276         g_fixed = [t for t in (g_fixed_tickers or []) if t in returns_df.columns]
L1277         union = [t for t in pool_tickers if t in returns_df.columns]
L1278         for t in g_fixed:
L1279             if t not in union: union.append(t)
L1280         Rdf_all = returns_df[union]; Rdf_all = Rdf_all.iloc[-lookback:] if len(Rdf_all)>=lookback else Rdf_all; Rdf_all = Rdf_all.dropna()
L1281         pool_eff, g_eff = [t for t in pool_tickers if t in Rdf_all.columns], [t for t in g_fixed if t in Rdf_all.columns]
L1282         if len(pool_eff)==0: return dict(idx=[], tickers=[], avg_res_corr=np.nan, sum_score=0.0, objective=-np.inf)
L1283         score = score_ser.reindex(pool_eff).to_numpy(dtype=np.float32)
L1284         C_all = cls.residual_corr(Rdf_all.to_numpy(), n_pc=n_pc, shrink=shrink)
L1285         col_pos = {c:i for i,c in enumerate(Rdf_all.columns)}; pool_pos = [col_pos[t] for t in pool_eff]
L1286         C_within, C_cross = C_all[np.ix_(pool_pos,pool_pos)], None
L1287         if len(g_eff)>0 and mu>0.0:
L1288             g_pos = [col_pos[t] for t in g_eff]; C_cross = C_all[np.ix_(pool_pos,g_pos)]
L1289         R_pool = Rdf_all[pool_eff].to_numpy(); S0 = cls.rrqr_like_det(R_pool, score, k, gamma=gamma)
L1290         S, Jn = (cls.swap_local_det_cross(C_within, C_cross, score, S0, lam=lam, mu=mu, max_pass=15) if C_cross is not None else cls.swap_local_det(C_within, score, S0, lam=lam, max_pass=15))
L1291         selected_tickers = [pool_eff[i] for i in S]
L1292         return dict(idx=S, tickers=selected_tickers, avg_res_corr=cls.avg_corr(C_within,S), sum_score=float(score[S].sum()), objective=float(Jn))
L1293
L1294     # ---- 選定（スコア Series / returns だけを受ける）----
L1295 # === Output：出力整形と送信（表示・Slack） ===
L1296 class Output:
L1297
L1298     def __init__(self, debug=None):
L1299         # self.debug は使わない（互換のため引数は受けるが無視）
L1300         self.miss_df = self.g_table = self.d_table = self.io_table = self.df_metrics_fmt = self.debug_table = None
L1301         self.g_title = self.d_title = ""
L1302         self.g_formatters = self.d_formatters = {}
L1303         # 低スコア（GSC+DSC）Top10 表示/送信用
L1304         self.low10_table = None
L1305         self.debug_text = ""   # デバッグ用本文はここに一本化
L1306         self._debug_logged = False
L1307         self._miss_disp_info: Tuple[pd.DataFrame, bool, int] | None = None
L1308
L1309     @staticmethod
L1310     def _prepare_missing_display(df: pd.DataFrame | None) -> Tuple[pd.DataFrame, bool, int]:
L1311         if df is None or df.empty:
L1312             return pd.DataFrame(), False, 0
L1313         work = df.copy()
L1314         if 'ticker' not in work.columns:
L1315             work = work.reset_index()
L1316             if 'ticker' not in work.columns and 'index' in work.columns:
L1317                 work = work.rename(columns={'index': 'ticker'})
L1318         bool_cols = [c for c in ['EPS_missing', 'REV_missing'] if c in work.columns]
L1319         if bool_cols:
L1320             work = work.loc[work[bool_cols].any(axis=1)]
L1321         if work.empty:
L1322             return pd.DataFrame(columns=work.columns), False, 0
L1323         cols_order = [
L1324             col for col in [
L1325                 'ticker',
L1326                 'EPS_missing',
L1327                 'REV_missing',
L1328                 'eps_imputed',
L1329                 'EPS_TTM',
L1330                 'EPS_Q_LastQ',
L1331                 'EPS_A_LATEST',
L1332                 'REV_TTM',
L1333                 'REV_Q_LastQ',
L1334                 'REV_A_LATEST',
L1335             ]
L1336             if col in work.columns
L1337         ]
L1338         if cols_order:
L1339             work = work.loc[:, cols_order]
L1340         total = len(work)
L1341         truncated = False
L1342         if total > 50:
L1343             work = work.head(20)
L1344             truncated = True
L1345         return work, truncated, total
L1346
L1347     # --- 表示（元 display_results のロジックそのまま） ---
L1348     def display_results(self, *, exist, bench, df_z, g_score, d_score_all,
L1349                         init_G, init_D, top_G, top_D, **kwargs):
L1350         logger.info("📌 reached display_results")
L1351         pd.set_option('display.float_format','{:.3f}'.format)
L1352         print("📈 ファクター分散最適化の結果")
L1353         miss_df, truncated, total = self._prepare_missing_display(self.miss_df)
L1354         self._miss_disp_info = (miss_df, truncated, total)
L1355         if not miss_df.empty:
L1356             print("Missing Data:")
L1357             print(miss_df.to_string(index=False))
L1358             if truncated:
L1359                 print(f"...省略 ({total}件中 上位20件のみ表示)")
L1360
L1361         # ---- 表示用：Changes/Near-Miss のスコア源を“最終集計”に統一するプロキシ ----
L1362         try:
L1363             sc = getattr(self, "_sc", None)
L1364             agg_G = getattr(sc, "_agg_G", None)
L1365             agg_D = getattr(sc, "_agg_D", None)
L1366         except Exception:
L1367             sc = agg_G = agg_D = None
L1368         class _SeriesProxy:
L1369             __slots__ = ("primary", "fallback")
L1370             def __init__(self, primary, fallback): self.primary, self.fallback = primary, fallback
L1371             def get(self, key, default=None):
L1372                 try:
L1373                     v = self.primary.get(key) if hasattr(self.primary, "get") else None
L1374                 except Exception:
L1375                     v = None
L1376                 if v is not None and not (isinstance(v, float) and v != v):
L1377                     return v
L1378                 try:
L1379                     return self.fallback.get(key) if hasattr(self.fallback, "get") else default
L1380                 except Exception:
L1381                     return default
L1382         g_score = _SeriesProxy(agg_G, g_score)
L1383         d_score_all = _SeriesProxy(agg_D, d_score_all)
L1384         near_G = getattr(sc, "_near_G", []) if sc else []
L1385         near_D = getattr(sc, "_near_D", []) if sc else []
L1386
L1387         extra_G = [t for t in init_G if t not in top_G][:5]; G_UNI = top_G + extra_G
L1388         gsc_series = pd.Series({t: g_score.get(t) for t in G_UNI}, name='GSC')
L1389         self.g_table = pd.concat([df_z.loc[G_UNI,['GROWTH_F','MOM','TRD','VOL']], gsc_series], axis=1)
L1390         self.g_table.index = [t + ("⭐️" if t in top_G else "") for t in G_UNI]
L1391         self.g_formatters = {col:"{:.2f}".format for col in ['GROWTH_F','MOM','TRD','VOL']}; self.g_formatters['GSC'] = "{:.3f}".format
L1392         self.g_title = (f"[G枠 / {N_G} / {_fmt_w(g_weights)} / corrM={corrM} / "
L1393                         f"LB={DRRS_G['lookback']} nPC={DRRS_G['n_pc']} γ={DRRS_G['gamma']} λ={DRRS_G['lam']} η={DRRS_G['eta']} shrink={DRRS_SHRINK}]")
L1394         if near_G:
L1395             add = [t for t in near_G if t not in set(G_UNI)][:10]
L1396             if len(add) < 10:
L1397                 try:
L1398                     aggG = getattr(sc, "_agg_G", pd.Series(dtype=float)).dropna().sort_values(ascending=False)
L1399                     out_now = sorted(set(exist) - set(top_G + top_D))  # 今回 OUT
L1400                     used = set(G_UNI + add)
L1401                     def _push(lst):
L1402                         nonlocal add, used
L1403                         for t in lst:
L1404                             if len(add) == 10: break
L1405                             if t in aggG.index and t not in used:
L1406                                 add.append(t); used.add(t)
L1407                     _push(out_now)           # ① 今回 OUT を優先
L1408                     _push(list(aggG.index))  # ② まだ足りなければ上位で充填
L1409                 except Exception:
L1410                     pass
L1411             if add:
L1412                 near_tbl = pd.concat([df_z.loc[add,['GROWTH_F','MOM','TRD','VOL']], pd.Series({t: g_score.get(t) for t in add}, name='GSC')], axis=1)
L1413                 self.g_table = pd.concat([self.g_table, near_tbl], axis=0)
L1414         print(self.g_title); print(self.g_table.to_string(formatters=self.g_formatters))
L1415
L1416         extra_D = [t for t in init_D if t not in top_D][:5]; D_UNI = top_D + extra_D
L1417         cols_D = ['QAL','YLD','VOL','TRD']; d_disp = pd.DataFrame(index=D_UNI)
L1418         d_disp['QAL'], d_disp['YLD'], d_disp['VOL'], d_disp['TRD'] = df_z.loc[D_UNI,'D_QAL'], df_z.loc[D_UNI,'D_YLD'], df_z.loc[D_UNI,'D_VOL_RAW'], df_z.loc[D_UNI,'D_TRD']
L1419         dsc_series = pd.Series({t: d_score_all.get(t) for t in D_UNI}, name='DSC')
L1420         self.d_table = pd.concat([d_disp, dsc_series], axis=1); self.d_table.index = [t + ("⭐️" if t in top_D else "") for t in D_UNI]
L1421         self.d_formatters = {col:"{:.2f}".format for col in cols_D}; self.d_formatters['DSC']="{:.3f}".format
L1422         import scorer
L1423         dw_eff = scorer.D_WEIGHTS_EFF
L1424         self.d_title = (f"[D枠 / {N_D} / {_fmt_w(dw_eff)} / corrM={corrM} / "
L1425                         f"LB={DRRS_D['lookback']} nPC={DRRS_D['n_pc']} γ={DRRS_D['gamma']} λ={DRRS_D['lam']} μ={CROSS_MU_GD} η={DRRS_D['eta']} shrink={DRRS_SHRINK}]")
L1426         if near_D:
L1427             add = [t for t in near_D if t not in set(D_UNI)][:10]
L1428             if add:
L1429                 d_disp2 = pd.DataFrame(index=add)
L1430                 d_disp2['QAL
```