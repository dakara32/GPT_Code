```text
staticmethod
L1238     def _obj(corrM: np.ndarray, score: np.ndarray, idx, lam: float) -> float:
L1239         idx = list(idx); P = corrM[np.ix_(idx, idx)]; s = (score-score.mean())/(score.std()+1e-9)
L1240         return float(s[idx].sum() - lam*((P.sum()-np.trace(P))/2.0))
L1241
L1242     @classmethod
L1243     def swap_local_det(cls, corrM: np.ndarray, score: np.ndarray, idx, lam: float=0.6, max_pass: int=15):
L1244         S, best, improved, passes = sorted(idx), cls._obj(corrM, score, idx, lam), True, 0
L1245         while improved and passes<max_pass:
L1246             improved, passes = False, passes+1
L1247             for i,out in enumerate(list(S)):
L1248                 for inn in range(len(score)):
L1249                     if inn in S: continue
L1250                     cand = sorted(S[:i]+[inn]+S[i+1:]); v = cls._obj(corrM, score, cand, lam)
L1251                     if v>best+1e-10: S, best, improved = cand, v, True; break
L1252                 if improved: break
L1253         return S, best
L1254
L1255     @staticmethod
L1256     def _obj_with_cross(C_within: np.ndarray, C_cross: np.ndarray|None, score: np.ndarray, idx, lam: float, mu: float) -> float:
L1257         idx = list(idx); P = C_within[np.ix_(idx, idx)]; s = (score-score.mean())/(score.std()+1e-9)
L1258         within = (P.sum()-np.trace(P))/2.0; cross = 0.0
L1259         if C_cross is not None and C_cross.size>0: cross = C_cross[idx,:].sum()
L1260         return float(s[idx].sum() - lam*within - mu*cross)
L1261
L1262     @classmethod
L1263     def swap_local_det_cross(cls, C_within: np.ndarray, C_cross: np.ndarray|None, score: np.ndarray, idx, lam: float=0.6, mu: float=0.3, max_pass: int=15):
L1264         S, best, improved, passes, N = sorted(idx), cls._obj_with_cross(C_within,C_cross,score,idx,lam,mu), True, 0, len(score)
L1265         while improved and passes<max_pass:
L1266             improved, passes = False, passes+1
L1267             for i,out in enumerate(list(S)):
L1268                 for inn in range(N):
L1269                     if inn in S: continue
L1270                     cand = sorted(S[:i]+[inn]+S[i+1:]); v = cls._obj_with_cross(C_within,C_cross,score,cand,lam,mu)
L1271                     if v>best+1e-10: S, best, improved = cand, v, True; break
L1272                 if improved: break
L1273         return S, best
L1274
L1275     @staticmethod
L1276     def avg_corr(C: np.ndarray, idx) -> float:
L1277         k = len(idx); P = C[np.ix_(idx, idx)]
L1278         return float((P.sum()-np.trace(P))/(k*(k-1)+1e-12))
L1279
L1280     @classmethod
L1281     def select_bucket_drrs(cls, returns_df: pd.DataFrame, score_ser: pd.Series, pool_tickers: list[str], k: int, *, n_pc: int, gamma: float, lam: float, lookback: int, shrink: float=0.10, g_fixed_tickers: list[str]|None=None, mu: float=0.0):
L1282         g_fixed = [t for t in (g_fixed_tickers or []) if t in returns_df.columns]
L1283         union = [t for t in pool_tickers if t in returns_df.columns]
L1284         for t in g_fixed:
L1285             if t not in union: union.append(t)
L1286         Rdf_all = returns_df[union]; Rdf_all = Rdf_all.iloc[-lookback:] if len(Rdf_all)>=lookback else Rdf_all; Rdf_all = Rdf_all.dropna()
L1287         pool_eff, g_eff = [t for t in pool_tickers if t in Rdf_all.columns], [t for t in g_fixed if t in Rdf_all.columns]
L1288         if len(pool_eff)==0: return dict(idx=[], tickers=[], avg_res_corr=np.nan, sum_score=0.0, objective=-np.inf)
L1289         score = score_ser.reindex(pool_eff).to_numpy(dtype=np.float32)
L1290         C_all = cls.residual_corr(Rdf_all.to_numpy(), n_pc=n_pc, shrink=shrink)
L1291         col_pos = {c:i for i,c in enumerate(Rdf_all.columns)}; pool_pos = [col_pos[t] for t in pool_eff]
L1292         C_within, C_cross = C_all[np.ix_(pool_pos,pool_pos)], None
L1293         if len(g_eff)>0 and mu>0.0:
L1294             g_pos = [col_pos[t] for t in g_eff]; C_cross = C_all[np.ix_(pool_pos,g_pos)]
L1295         R_pool = Rdf_all[pool_eff].to_numpy(); S0 = cls.rrqr_like_det(R_pool, score, k, gamma=gamma)
L1296         S, Jn = (cls.swap_local_det_cross(C_within, C_cross, score, S0, lam=lam, mu=mu, max_pass=15) if C_cross is not None else cls.swap_local_det(C_within, score, S0, lam=lam, max_pass=15))
L1297         selected_tickers = [pool_eff[i] for i in S]
L1298         return dict(idx=S, tickers=selected_tickers, avg_res_corr=cls.avg_corr(C_within,S), sum_score=float(score[S].sum()), objective=float(Jn))
L1299
L1300     # ---- 選定（スコア Series / returns だけを受ける）----
L1301 # === Output：出力整形と送信（表示・Slack） ===
L1302 class Output:
L1303
L1304     def __init__(self, debug=None):
L1305         # self.debug は使わない（互換のため引数は受けるが無視）
L1306         self.miss_df = self.g_table = self.d_table = self.io_table = self.df_metrics_fmt = self.debug_table = None
L1307         self.g_title = self.d_title = ""
L1308         self.g_formatters = self.d_formatters = {}
L1309         # 低スコア（GSC+DSC）Top10 表示/送信用
L1310         self.low10_table = None
L1311         self.debug_text = ""   # デバッグ用本文はここに一本化
L1312         self._debug_logged = False
L1313         self._miss_disp_info: Tuple[pd.DataFrame, bool, int] | None = None
L1314
L1315     @staticmethod
L1316     def _prepare_missing_display(df: pd.DataFrame | None) -> Tuple[pd.DataFrame, bool, int]:
L1317         if df is None or df.empty:
L1318             return pd.DataFrame(), False, 0
L1319         work = df.copy()
L1320         if 'ticker' not in work.columns:
L1321             work = work.reset_index()
L1322             if 'ticker' not in work.columns and 'index' in work.columns:
L1323                 work = work.rename(columns={'index': 'ticker'})
L1324         bool_cols = [c for c in ['EPS_missing', 'REV_missing'] if c in work.columns]
L1325         if bool_cols:
L1326             work = work.loc[work[bool_cols].any(axis=1)]
L1327         if work.empty:
L1328             return pd.DataFrame(columns=work.columns), False, 0
L1329         cols_order = [
L1330             col for col in [
L1331                 'ticker',
L1332                 'EPS_missing',
L1333                 'REV_missing',
L1334                 'eps_imputed',
L1335                 'EPS_TTM',
L1336                 'EPS_Q_LastQ',
L1337                 'EPS_A_LATEST',
L1338                 'REV_TTM',
L1339                 'REV_Q_LastQ',
L1340                 'REV_A_LATEST',
L1341             ]
L1342             if col in work.columns
L1343         ]
L1344         if cols_order:
L1345             work = work.loc[:, cols_order]
L1346         total = len(work)
L1347         truncated = False
L1348         if total > 50:
L1349             work = work.head(20)
L1350             truncated = True
L1351         return work, truncated, total
L1352
L1353     # --- 表示（元 display_results のロジックそのまま） ---
L1354     def display_results(self, *, exist, bench, df_z, g_score, d_score_all,
L1355                         init_G, init_D, top_G, top_D, **kwargs):
L1356         logger.info("📌 reached display_results")
L1357         pd.set_option('display.float_format','{:.3f}'.format)
L1358         print("📈 ファクター分散最適化の結果")
L1359         miss_df, truncated, total = self._prepare_missing_display(self.miss_df)
L1360         self._miss_disp_info = (miss_df, truncated, total)
L1361         lines = compact_missing_lines(miss_df)
L1362         if lines:
L1363             txt = "Missing Data:\n```" + "\n".join(lines) + "```"
L1364             if truncated:
L1365                 txt += f"\n...省略 ({total}件中 上位20件のみ表示)"
L1366             print(txt)
L1367
L1368         # ---- 表示用：Changes/Near-Miss のスコア源を“最終集計”に統一するプロキシ ----
L1369         try:
L1370             sc = getattr(self, "_sc", None)
L1371             agg_G = getattr(sc, "_agg_G", None)
L1372             agg_D = getattr(sc, "_agg_D", None)
L1373         except Exception:
L1374             sc = agg_G = agg_D = None
L1375         class _SeriesProxy:
L1376             __slots__ = ("primary", "fallback")
L1377             def __init__(self, primary, fallback): self.primary, self.fallback = primary, fallback
L1378             def get(self, key, default=None):
L1379                 try:
L1380                     v = self.primary.get(key) if hasattr(self.primary, "get") else None
L1381                 except Exception:
L1382                     v = None
L1383                 if v is not None and not (isinstance(v, float) and v != v):
L1384                     return v
L1385                 try:
L1386                     return self.fallback.get(key) if hasattr(self.fallback, "get") else default
L1387                 except Exception:
L1388                     return default
L1389         g_score = _SeriesProxy(agg_G, g_score)
L1390         d_score_all = _SeriesProxy(agg_D, d_score_all)
L1391         near_G = getattr(sc, "_near_G", []) if sc else []
L1392         near_D = getattr(sc, "_near_D", []) if sc else []
L1393
L1394         extra_G = [t for t in init_G if t not in top_G][:5]; G_UNI = top_G + extra_G
L1395         gsc_series = pd.Series({t: g_score.get(t) for t in G_UNI}, name='GSC')
L1396         self.g_table = pd.concat([df_z.loc[G_UNI,['GROWTH_F','MOM','TRD','VOL']], gsc_series], axis=1)
L1397         self.g_table.index = [t + ("⭐️" if t in top_G else "") for t in G_UNI]
L1398         self.g_formatters = {col:"{:.2f}".format for col in ['GROWTH_F','MOM','TRD','VOL']}; self.g_formatters['GSC'] = "{:.3f}".format
L1399         self.g_title = (f"[G枠 / {N_G} / {_fmt_w(g_weights)} / corrM={corrM} / "
L1400                         f"LB={DRRS_G['lookback']} nPC={DRRS_G['n_pc']} γ={DRRS_G['gamma']} λ={DRRS_G['lam']} η={DRRS_G['eta']} shrink={DRRS_SHRINK}]")
L1401         if near_G:
L1402             add = [t for t in near_G if t not in set(G_UNI)][:10]
L1403             if len(add) < 10:
L1404                 try:
L1405                     aggG = getattr(sc, "_agg_G", pd.Series(dtype=float)).dropna().sort_values(ascending=False)
L1406                     out_now = sorted(set(exist) - set(top_G + top_D))  # 今回 OUT
L1407                     used = set(G_UNI + add)
L1408                     def _push(lst):
L1409                         nonlocal add, used
L1410                         for t in lst:
L1411                             if len(add) == 10: break
L1412                             if t in aggG.index and t not in used:
L1413                                 add.append(t); used.add(t)
L1414                     _push(out_now)           # ① 今回 OUT を優先
L1415                     _push(list(aggG.index))  # ② まだ足りなければ上位で充填
L1416                 except Exception:
L1417                     pass
L1418             if add:
L1419                 near_tbl = pd.concat([df_z.loc[add,['GROWTH_F','MOM','TRD','VOL']], pd.Series({t: g_score.get(t) for t in add}, name='GSC')], axis=1)
L1420                 self.g_table = pd.concat([self.g_table, near_tbl], axis=0)
L1421         print(self.g_title); print(self.g_table.to_string(formatters=self.g_formatters))
L1422
L1423         extra_D = [t for t in init_D if t not in top_D][:5]; D_UNI = top_D + extra_D
L1424         cols_D = ['QAL','YLD','VOL','TRD']; d_disp = pd.DataFrame(index=D_UNI)
L1425         d_disp['QAL'], d_disp['YLD'], d_disp['VOL'], d_disp['TRD'] = df_z.loc[D_UNI,'D_QAL'], df_z.loc[D_UNI,'D_YLD'], df_z.loc[D_UNI,'D_VOL_RAW'], df_z.loc[D_UNI,'D_TRD']
L1426         dsc_series = pd.Series({t: d_score_all.get(t) for t in D_UNI}, name='DSC')
L1427         self.d_table = pd.concat([d_disp, dsc_series], axis=1); self.d_table.index = [t + ("⭐️" if t in top_D else "") for t in D_UNI]
L1428         self.d_formatters = {col:"{:.2f}".format for col in cols_D}; self.d_formatters['DSC']="{:.3f}".format
L1429         import scorer
L1430         dw_eff = scorer.D_WEIGHTS_EFF
L1431         self.d_title = (f"[D枠 / {N_D} / {_fmt_w(dw_eff)} / corrM={corrM} / "
L1432                         f"LB={DRRS_D['lookback']} nPC={DRRS_D['n_pc']} γ={DRRS_D['gamma']} λ={DRRS_D['lam']} μ={CROSS_MU_GD} η={DRRS_D['eta']} shrink={DRRS_SHRINK}]")
L1433         if near_D:
L1434 
```