```text
 DIV_FCF_COVER = float((fcf_val)/(DIV_TTM_PS*float(so)))
L420             except Exception: pass
L421             df.loc[t,'DIV_TTM_PS'], df.loc[t,'DIV_VAR5'], df.loc[t,'DIV_YOY'], df.loc[t,'DIV_FCF_COVER'] = DIV_TTM_PS, DIV_VAR5, DIV_YOY, DIV_FCF_COVER
L422
L423             # --- 財務安定性 ---
L424             df.loc[t,'DEBT2EQ'], df.loc[t,'CURR_RATIO'] = d.get('debtToEquity',np.nan), d.get('currentRatio',np.nan)
L425
L426             # --- EPS 変動 ---
L427             EPS_VAR_8Q = np.nan
L428             try:
L429                 qe, so = tickers_bulk.tickers[t].quarterly_earnings, d.get('sharesOutstanding',None)
L430                 if qe is not None and not qe.empty and so:
L431                     eps_q = (qe['Earnings'].dropna().astype(float)/float(so)).replace([np.inf,-np.inf],np.nan)
L432                     if len(eps_q)>=4: EPS_VAR_8Q = float(eps_q.iloc[-min(8,len(eps_q)):].std(ddof=1))
L433             except Exception: pass
L434             df.loc[t,'EPS_VAR_8Q'] = EPS_VAR_8Q
L435
L436             # --- サイズ/流動性 ---
L437             df.loc[t,'MARKET_CAP'] = d.get('marketCap',np.nan); adv60 = np.nan
L438             try:
L439                 vol_series = ib.data['Volume'][t].dropna()
L440                 if len(vol_series)>=5 and len(s)==len(vol_series):
L441                     dv = (vol_series*s).rolling(60).mean(); adv60 = float(dv.iloc[-1])
L442             except Exception: pass
L443             df.loc[t,'ADV60_USD'] = adv60
L444
L445             # --- 売上/利益の加速度等 ---
L446             REV_Q_YOY=EPS_Q_YOY=REV_YOY_ACC=REV_YOY_VAR=np.nan
L447             REV_ANNUAL_STREAK = REV_YOY = np.nan
L448             EPS_YOY = np.nan
L449             try:
L450                 qe, so = tickers_bulk.tickers[t].quarterly_earnings, d.get('sharesOutstanding',None)
L451                 if qe is not None and not qe.empty:
L452                     if 'Revenue' in qe.columns:
L453                         rev = qe['Revenue'].dropna().astype(float)
L454                         if len(rev)>=5: REV_Q_YOY = _safe_div(rev.iloc[-1]-rev.iloc[-5], rev.iloc[-5])
L455                         if len(rev)>=6:
L456                             yoy_now = _safe_div(rev.iloc[-1]-rev.iloc[-5], rev.iloc[-5]); yoy_prev = _safe_div(rev.iloc[-2]-rev.iloc[-6], rev.iloc[-6])
L457                             if pd.notna(yoy_now) and pd.notna(yoy_prev): REV_YOY_ACC = yoy_now - yoy_prev
L458                         yoy_list=[]
L459                         for k in range(1,5):
L460                             if len(rev)>=4+k:
L461                                 y = _safe_div(rev.iloc[-k]-rev.iloc[-(k+4)], rev.iloc[-(k+4)])
L462                                 if pd.notna(y): yoy_list.append(y)
L463                         if len(yoy_list)>=2: REV_YOY_VAR = float(np.std(yoy_list, ddof=1))
L464                         # NEW: 年次の持続性（直近から遡って前年比プラスが何年連続か、四半期4本揃う完全年のみ）
L465                         try:
L466                             g = rev.groupby(rev.index.year)
L467                             ann_sum, cnt = g.sum(), g.count()
L468                             ann_sum = ann_sum[cnt >= 4]
L469                             if len(ann_sum) >= 2:
L470                                 yoy = ann_sum.pct_change().dropna()
L471                                 if not yoy.empty:
L472                                     REV_YOY = float(yoy.iloc[-1])
L473                                 streak = 0
L474                                 for v in yoy.iloc[::-1]:
L475                                     if pd.isna(v) or v <= 0:
L476                                         break
L477                                     streak += 1
L478                                 REV_ANNUAL_STREAK = float(streak)
L479                         except Exception:
L480                             pass
L481                     if 'Earnings' in qe.columns and so:
L482                         eps_series = (qe['Earnings'].dropna().astype(float)/float(so)).replace([np.inf,-np.inf],np.nan)
L483                         if len(eps_series)>=5 and pd.notna(eps_series.iloc[-5]) and eps_series.iloc[-5]!=0:
L484                             EPS_Q_YOY = _safe_div(eps_series.iloc[-1]-eps_series.iloc[-5], eps_series.iloc[-5])
L485                         try:
L486                             g_eps = eps_series.groupby(eps_series.index.year)
L487                             ann_eps, cnt_eps = g_eps.sum(), g_eps.count()
L488                             ann_eps = ann_eps[cnt_eps >= 4]
L489                             if len(ann_eps) >= 2:
L490                                 eps_yoy = ann_eps.pct_change().dropna()
L491                                 if not eps_yoy.empty:
L492                                     EPS_YOY = float(eps_yoy.iloc[-1])
L493                         except Exception:
L494                             pass
L495             except Exception: pass
L496             df.loc[t,'REV_Q_YOY'], df.loc[t,'EPS_Q_YOY'] = REV_Q_YOY, EPS_Q_YOY
L497             df.loc[t,'REV_YOY_ACC'], df.loc[t,'REV_YOY_VAR'] = REV_YOY_ACC, REV_YOY_VAR
L498             df.loc[t,'REV_YOY'] = REV_YOY
L499             df.loc[t,'REV_ANN_STREAK'] = REV_ANNUAL_STREAK
L500             df.loc[t,'EPS_YOY'] = EPS_YOY
L501
L502             # --- Rule of 40 や周辺 ---
L503             total_rev_ttm = d.get('totalRevenue',np.nan)
L504             FCF_MGN = _safe_div(fcf_val, total_rev_ttm)
L505             df.loc[t,'FCF_MGN'] = FCF_MGN
L506             rule40 = np.nan
L507             try:
L508                 r = df.loc[t,'REV']; rule40 = (r if pd.notna(r) else np.nan) + (FCF_MGN if pd.notna(FCF_MGN) else np.nan)
L509             except Exception: pass
L510             df.loc[t,'RULE40'] = rule40
L511
L512             # --- トレンド補助 ---
L513             sma50  = s.rolling(50).mean()
L514             sma150 = s.rolling(150).mean()
L515             sma200 = s.rolling(200).mean()
L516             p = _safe_last(s)
L517
L518             df.loc[t,'MA50_OVER_150'] = (_safe_last(sma50)/_safe_last(sma150) - 1
L519                 if pd.notna(_safe_last(sma50)) and pd.notna(_safe_last(sma150)) and _safe_last(sma150)!=0 else np.nan)
L520             df.loc[t,'MA150_OVER_200'] = (_safe_last(sma150)/_safe_last(sma200) - 1
L521                 if pd.notna(_safe_last(sma150)) and pd.notna(_safe_last(sma200)) and _safe_last(sma200)!=0 else np.nan)
L522
L523             lo52 = s[-252:].min() if len(s)>=252 else s.min()
L524             df.loc[t,'P_OVER_LOW52'] = (p/lo52 - 1) if (lo52 and lo52>0 and pd.notna(p)) else np.nan
L525
L526             df.loc[t,'MA200_SLOPE_1M'] = np.nan
L527             if len(sma200.dropna()) >= 21:
L528                 cur200 = _safe_last(sma200)
L529                 old2001 = float(sma200.iloc[-21])
L530                 if old2001:
L531                     df.loc[t,'MA200_SLOPE_1M'] = cur200/old2001 - 1
L532
L533             df.loc[t,'P_OVER_150'] = p/_safe_last(sma150)-1 if pd.notna(_safe_last(sma150)) and _safe_last(sma150)!=0 else np.nan
L534             df.loc[t,'P_OVER_200'] = p/_safe_last(sma200)-1 if pd.notna(_safe_last(sma200)) and _safe_last(sma200)!=0 else np.nan
L535             df.loc[t,'MA50_OVER_200'] = _safe_last(sma50)/_safe_last(sma200)-1 if pd.notna(_safe_last(sma50)) and pd.notna(_safe_last(sma200)) and _safe_last(sma200)!=0 else np.nan
L536             df.loc[t,'MA200_SLOPE_5M'] = np.nan
L537             if len(sma200.dropna())>=105:
L538                 cur200, old200 = _safe_last(sma200), float(sma200.iloc[-105])
L539                 if old200 and old200!=0: df.loc[t,'MA200_SLOPE_5M'] = cur200/old200 - 1
L540             # NEW: 200日線が連続で上向きの「日数」
L541             df.loc[t,'MA200_UP_STREAK_D'] = np.nan
L542             try:
L543                 s200 = sma200.dropna()
L544                 if len(s200) >= 2:
L545                     diff200 = s200.diff()
L546                     up = 0
L547                     for v in diff200.iloc[::-1]:
L548                         if pd.isna(v) or v <= 0:
L549                             break
L550                         up += 1
L551                     df.loc[t,'MA200_UP_STREAK_D'] = float(up)
L552             except Exception:
L553                 pass
L554             df.loc[t,'LOW52PCT25_EXCESS'] = np.nan if (lo52 is None or lo52<=0 or pd.isna(p)) else (p/(lo52*1.25)-1)
L555             hi52 = s[-252:].max() if len(s)>=252 else s.max(); df.loc[t,'NEAR_52W_HIGH'] = np.nan
L556             if hi52 and hi52>0 and pd.notna(p):
L557                 d_hi = (p/hi52)-1.0; df.loc[t,'NEAR_52W_HIGH'] = -abs(min(0.0, d_hi))
L558             df.loc[t,'RS_SLOPE_6W'] = self.rs_line_slope(s, ib.spx, 30)
L559             df.loc[t,'RS_SLOPE_13W'] = self.rs_line_slope(s, ib.spx, 65)
L560
L561             df.loc[t,'DIV_STREAK'] = self.div_streak(t)
L562
L563             # --- 欠損メモ ---
L564             fin_cols = ['REV','ROE','BETA','DIV','FCF']
L565             need_finnhub = [col for col in fin_cols if pd.isna(df.loc[t,col])]
L566             if need_finnhub:
L567                 fin_data = self.fetch_finnhub_metrics(t)
L568                 for col in need_finnhub:
L569                     val = fin_data.get(col)
L570                     if val is not None and not pd.isna(val): df.loc[t,col] = val
L571             for col in fin_cols + ['EPS','RS','TR_str','DIV_STREAK']:
L572                 if pd.isna(df.loc[t,col]):
L573                     if col=='DIV':
L574                         status = self.dividend_status(t)
L575                         if status!='none_confident': missing_logs.append({'Ticker':t,'Column':col,'Status':status})
L576                     else:
L577                         missing_logs.append({'Ticker':t,'Column':col})
L578
L579         def _trend_template_pass(row, rs_alpha_thresh=0.10):
L580             c1 = (row.get('P_OVER_150', np.nan) > 0) and (row.get('P_OVER_200', np.nan) > 0)
L581             c2 = (row.get('MA150_OVER_200', np.nan) > 0)
L582             c3 = (row.get('MA200_SLOPE_1M', np.nan) > 0)
L583             c4 = (row.get('MA50_OVER_150', np.nan) > 0) and (row.get('MA50_OVER_200', np.nan) > 0)
L584             c5 = (row.get('TR_str', np.nan) > 0)
L585             c6 = (row.get('P_OVER_LOW52', np.nan) >= 0.30)
L586             c7 = (row.get('NEAR_52W_HIGH', np.nan) >= -0.25)
L587             c8 = (row.get('RS', np.nan) >= 0.10)
L588             return bool(c1 and c2 and c3 and c4 and c5 and c6 and c7 and c8)
L589
L590         if 'trend_template' not in df.columns: df['trend_template'] = df.apply(_trend_template_pass, axis=1).fillna(False)
L591         assert 'trend_template' in df.columns
L592
L593         # === Z化と合成 ===
L594         for col in ['ROE','FCF','REV','EPS']: df[f'{col}_W'] = winsorize_s(df[col], 0.02)
L595
L596         df_z = pd.DataFrame(index=df.index)
L597         for col in ['EPS','REV','ROE','FCF','RS','TR_str','BETA','DIV','DIV_STREAK']: df_z[col] = robust_z(df[col])
L598         df_z['REV'], df_z['EPS'], df_z['TR'] = robust_z(df['REV_W']), robust_z(df['EPS_W']), robust_z(df['TR'])
L599         for col in ['P_OVER_150','P_OVER_200','MA50_OVER_200','MA200_SLOPE_5M','LOW52PCT25_EXCESS','NEAR_52W_HIGH','RS_SLOPE_6W','RS_SLOPE_13W','MA200_UP_STREAK_D']: df_z[col] = robust_z(df[col])
L600
L601         # === Growth深掘り系（欠損保持z + RAW併載） ===
L602         grw_cols = ['REV_Q_YOY','EPS_Q_YOY','REV_YOY','EPS_YOY','REV_YOY_ACC','REV_YOY_VAR','FCF_MGN','RULE40','REV_ANN_STREAK']
L603         for col in grw_cols:
L604             if col in df.columns:
L605                 raw = pd.to_numeric(df[col], errors="coerce")
L606                 df_z[col] = robust_z_keepnan(raw)
L607                 df_z[f'{col}_RAW'] = raw
L608         for k in ("TREND_SLOPE_EPS", "TREND_SLOPE_REV"):
L609             if k in df.columns and k not in df_z.columns:
L610                 raw = pd.to_numeric(df[k], errors="coerce")
L611                 df_z[k] = robust_z_keepnan(raw)
L612                 df_z[f'{k}_RAW'] = raw
L613         for col in ['DOWNSIDE_DEV','MDD_1Y','RESID_VOL','DOWN_OUTPERF','EXT_200','DIV_TTM_PS','DIV_V
```