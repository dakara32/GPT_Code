```text
        print(self.g_title); print(self.g_table.to_string(formatters=self.g_formatters))
L1447
L1448         extra_D = [t for t in init_D if t not in top_D][:5]; D_UNI = top_D + extra_D
L1449         cols_D = ['QAL','YLD','VOL','TRD','BETA_RAW','BETA_Z']; d_disp = pd.DataFrame(index=D_UNI)
L1450         d_disp['QAL'], d_disp['YLD'], d_disp['VOL'], d_disp['TRD'] = df_z.loc[D_UNI,'D_QAL'], df_z.loc[D_UNI,'D_YLD'], df_z.loc[D_UNI,'D_VOL_RAW'], df_z.loc[D_UNI,'D_TRD']
L1451         d_disp['BETA_RAW'] = beta_raw_series.reindex(D_UNI)
L1452         d_disp['BETA_Z'] = beta_z_series.reindex(D_UNI)
L1453         dsc_series = pd.Series({t: d_score_all.get(t) for t in D_UNI}, name='DSC')
L1454         self.d_table = pd.concat([d_disp, dsc_series], axis=1); self.d_table.index = [t + ("⭐️" if t in top_D else "") for t in D_UNI]
L1455         self.d_formatters = {col:"{:.2f}".format for col in cols_D}; self.d_formatters['DSC']="{:.3f}".format
L1456         import scorer
L1457         dw_eff = scorer.D_WEIGHTS_EFF
L1458         self.d_title = (f"[D枠 / {N_D} / {_fmt_w(dw_eff)} / corrM={corrM} / "
L1459                         f"LB={DRRS_D['lookback']} nPC={DRRS_D['n_pc']} γ={DRRS_D['gamma']} λ={DRRS_D['lam']} μ={CROSS_MU_GD} η={DRRS_D['eta']} shrink={DRRS_SHRINK}]")
L1460         if near_D:
L1461             add = [t for t in near_D if t not in set(D_UNI)][:10]
L1462             if add:
L1463                 d_disp2 = pd.DataFrame(index=add)
L1464                 d_disp2['QAL'], d_disp2['YLD'], d_disp2['VOL'], d_disp2['TRD'] = df_z.loc[add,'D_QAL'], df_z.loc[add,'D_YLD'], df_z.loc[add,'D_VOL_RAW'], df_z.loc[add,'D_TRD']
L1465                 d_disp2['BETA_RAW'] = beta_raw_series.reindex(add)
L1466                 d_disp2['BETA_Z'] = beta_z_series.reindex(add)
L1467                 near_tbl = pd.concat([d_disp2, pd.Series({t: d_score_all.get(t) for t in add}, name='DSC')], axis=1)
L1468                 self.d_table = pd.concat([self.d_table, near_tbl], axis=0)
L1469         print(self.d_title); print(self.d_table.to_string(formatters=self.d_formatters))
L1470
L1471         # === Changes（IN の GSC/DSC を表示。OUT は銘柄名のみ） ===
L1472         in_list = sorted(set(list(top_G)+list(top_D)) - set(exist))
L1473         out_list = sorted(set(exist) - set(list(top_G)+list(top_D)))
L1474
L1475         self.io_table = pd.DataFrame({
L1476             'IN': pd.Series(in_list),
L1477             '/ OUT': pd.Series(out_list)
L1478         })
L1479         g_list = [f"{g_score.get(t):.3f}" if pd.notna(g_score.get(t)) else '—' for t in out_list]
L1480         d_list = [f"{d_score_all.get(t):.3f}" if pd.notna(d_score_all.get(t)) else '—' for t in out_list]
L1481         self.io_table['GSC'] = pd.Series(g_list)
L1482         self.io_table['DSC'] = pd.Series(d_list)
L1483
L1484         print("Changes:")
L1485         print(self.io_table.to_string(index=False))
L1486
L1487         all_tickers = list(set(exist + list(top_G) + list(top_D) + [bench])); prices = yf.download(all_tickers, period='1y', auto_adjust=True, progress=False, threads=False)['Close'].ffill(limit=2)
L1488         ret = prices.pct_change(); portfolios = {'CUR':exist,'NEW':list(top_G)+list(top_D)}; metrics={}
L1489         for name,ticks in portfolios.items():
L1490             pr = ret[ticks].mean(axis=1, skipna=True).dropna(); cum = (1+pr).cumprod()-1; n = len(pr)
L1491             if n>=252: ann_ret, ann_vol = (1+cum.iloc[-1])**(252/n)-1, pr.std()*np.sqrt(252)
L1492             else: ann_ret, ann_vol = cum.iloc[-1], pr.std()*np.sqrt(n)
L1493             sharpe, drawdown = ann_ret/ann_vol, (cum - cum.cummax()).min()
L1494             if len(ticks)>=2:
L1495                 C_raw = ret[ticks].corr(); RAW_rho = C_raw.mask(np.eye(len(ticks), dtype=bool)).stack().mean()
L1496                 R = ret[ticks].dropna().to_numpy(); C_resid = Selector.residual_corr(R, n_pc=3, shrink=DRRS_SHRINK)
L1497                 RESID_rho = float((C_resid.sum()-np.trace(C_resid))/(C_resid.shape[0]*(C_resid.shape[0]-1)))
L1498             else: RAW_rho = RESID_rho = np.nan
L1499             divy = ttm_div_yield_portfolio(ticks); metrics[name] = {'RET':ann_ret,'VOL':ann_vol,'SHP':sharpe,'MDD':drawdown,'RAWρ':RAW_rho,'RESIDρ':RESID_rho,'DIVY':divy}
L1500         df_metrics = pd.DataFrame(metrics).T; df_metrics_pct = df_metrics.copy(); self.df_metrics = df_metrics
L1501         for col in ['RET','VOL','MDD','DIVY']: df_metrics_pct[col] = df_metrics_pct[col]*100
L1502         cols_order = ['RET','VOL','SHP','MDD','RAWρ','RESIDρ','DIVY']; df_metrics_pct = df_metrics_pct.reindex(columns=cols_order)
L1503         def _fmt_row(s):
L1504             return pd.Series({'RET':f"{s['RET']:.1f}%",'VOL':f"{s['VOL']:.1f}%",'SHP':f"{s['SHP']:.1f}",'MDD':f"{s['MDD']:.1f}%",'RAWρ':(f"{s['RAWρ']:.2f}" if pd.notna(s['RAWρ']) else "NaN"),'RESIDρ':(f"{s['RESIDρ']:.2f}" if pd.notna(s['RESIDρ']) else "NaN"),'DIVY':f"{s['DIVY']:.1f}%"})
L1505         self.df_metrics_fmt = df_metrics_pct.apply(_fmt_row, axis=1); print("Performance Comparison:"); print(self.df_metrics_fmt.to_string())
L1506         # === 追加: GSC+DSC が低い順 TOP10 ===
L1507         try:
L1508             all_scores = pd.DataFrame({'GSC': df_z['GSC'], 'DSC': df_z['DSC']}).copy()
L1509             all_scores['G_plus_D'] = all_scores['GSC'] + all_scores['DSC']
L1510             all_scores = all_scores.dropna(subset=['G_plus_D'])
L1511             self.low10_table = all_scores.sort_values('G_plus_D', ascending=True).head(10).round(3)
L1512             print("Low Score Candidates (GSC+DSC bottom 10):")
L1513             print(self.low10_table.to_string())
L1514         except Exception as e:
L1515             print(f"[warn] low-score ranking failed: {e}")
L1516             self.low10_table = None
L1517         self.debug_text = ""
L1518         if debug_mode:
L1519             logger.info("debug_mode=True: df_z dump handled in scorer; skipping factor-side debug output")
L1520         else:
L1521             logger.debug(
L1522                 "skip debug log: debug_mode=%s debug_text_empty=%s",
L1523                 debug_mode, True
L1524             )
L1525         self._debug_logged = True
L1526
L1527     # --- Slack送信（元 notify_slack のロジックそのまま） ---
L1528     def notify_slack(self):
L1529         SLACK_WEBHOOK_URL = os.environ.get("SLACK_WEBHOOK_URL")
L1530
L1531         if not SLACK_WEBHOOK_URL:
L1532             print("⚠️ SLACK_WEBHOOK_URL not set (main report skipped)")
L1533             return
L1534
L1535         def _filter_suffix_from(spec: dict, group: str) -> str:
L1536             g = spec.get(group, {})
L1537             parts = [str(m) for m in g.get("pre_mask", [])]
L1538             for k, v in (g.get("pre_filter", {}) or {}).items():
L1539                 base, op = (k[:-4], "<") if k.endswith("_max") else ((k[:-4], ">") if k.endswith("_min") else (k, "="))
L1540                 name = {"beta": "β"}.get(base, base)
L1541                 try:
L1542                     val = f"{float(v):g}"
L1543                 except Exception:
L1544                     val = str(v)
L1545                 parts.append(f"{name}{op}{val}")
L1546             return "" if not parts else " / filter:" + " & ".join(parts)
L1547
L1548         def _inject_filter_suffix(title: str, group: str) -> str:
L1549             suf = _filter_suffix_from(FILTER_SPEC, group)
L1550             return f"{title[:-1]}{suf}]" if suf and title.endswith("]") else (title + suf)
L1551
L1552         def _blk(title, tbl, fmt=None, drop=()):
L1553             if tbl is None or getattr(tbl, 'empty', False):
L1554                 return f"{title}\n(選定なし)\n"
L1555             if drop and hasattr(tbl, 'columns'):
L1556                 keep = [c for c in tbl.columns if c not in drop]
L1557                 tbl, fmt = tbl[keep], {k: v for k, v in (fmt or {}).items() if k in keep}
L1558             return f"{title}\n```{tbl.to_string(formatters=fmt)}```\n"
L1559
L1560         message = "📈 ファクター分散最適化の結果\n"
L1561         miss_df, truncated, total = self._miss_disp_info or self._prepare_missing_display(self.miss_df)
L1562         lines = compact_missing_lines(miss_df, limit=300)
L1563         missing_txt = "Missing Data:\n```" + "\n".join(lines) + "```" if lines else ""
L1564         trunc_note = f"...省略 ({total}件中 上位20件のみ表示)" if truncated else ""
L1565         if missing_txt:
L1566             message += missing_txt + ("\n" + trunc_note if trunc_note else "") + "\n"
L1567         message += _blk(_inject_filter_suffix(self.g_title, "G"), self.g_table, self.g_formatters, drop=("TRD",))
L1568         message += _blk(_inject_filter_suffix(self.d_title, "D"), self.d_table, self.d_formatters)
L1569         message += "Changes\n" + ("(変更なし)\n" if self.io_table is None or getattr(self.io_table, 'empty', False) else f"```{self.io_table.to_string(index=False)}```\n")
L1570         message += "Performance Comparison:\n```" + self.df_metrics_fmt.to_string() + "```"
L1571
L1572         try:
L1573             r = requests.post(SLACK_WEBHOOK_URL, json={"text": message})
L1574             print(f"[DBG] main_post status={getattr(r, 'status_code', None)} size={len(message)}")
L1575             if r is not None:
L1576                 r.raise_for_status()
L1577         except Exception as e:
L1578             print(f"[ERR] main_post_failed: {e}")
L1579
L1580 def _infer_g_universe(feature_df, selected12=None, near5=None):
L1581     try:
L1582         out = feature_df.index[feature_df['group'].astype(str).str.upper().eq('G')].tolist()
L1583         if out: return out
L1584     except Exception:
L1585         pass
L1586     base = set()
L1587     for lst in (selected12 or []), (near5 or []):
L1588         for x in (lst or []): base.add(x)
L1589     return list(base) if base else list(feature_df.index)
L1590
L1591 def _fmt_with_fire_mark(tickers, feature_df):
L1592     out = []
L1593     for t in tickers or []:
L1594         try:
L1595             br = bool(feature_df.at[t, "G_BREAKOUT_recent_5d"])
L1596             pb = bool(feature_df.at[t, "G_PULLBACK_recent_5d"])
L1597             out.append(f"{t}{' 🔥' if (br or pb) else ''}")
L1598         except Exception:
L1599             out.append(t)
L1600     return out
L1601
L1602 def _label_recent_event(t, feature_df):
L1603     try:
L1604         br = bool(feature_df.at[t, "G_BREAKOUT_recent_5d"]); dbr = str(feature_df.at[t, "G_BREAKOUT_last_date"]) if br else ""
L1605         pb = bool(feature_df.at[t, "G_PULLBACK_recent_5d"]); dpb = str(feature_df.at[t, "G_PULLBACK_last_date"]) if pb else ""
L1606         if   br and not pb: return f"{t}（ブレイクアウト確定 {dbr}）"
L1607         elif pb and not br: return f"{t}（押し目反発 {dpb}）"
L1608         elif br and pb:     return f"{t}（ブレイクアウト確定 {dbr}／押し目反発 {dpb}）"
L1609     except Exception:
L1610         pass
L1611     return t
L1612
L1613 # === パイプライン可視化：G/D共通フロー（出力は不変） ===
L1614
L1615 def io_build_input_bundle() -> InputBundle:
L1616     """
L1617     既存の『データ取得→前処理』を実行し、InputBundle を返す。
L1618     処理内容・列名・丸め・例外・ログ文言は現行どおり（変更禁止）。
L1619     """
L1620     state = Input(cand=cand, exist=exist, bench=bench, price_max=CAND_PRICE_MAX, finnhub_api_key=FINNHUB_API_KEY).prepare_data()
L1621     return InputBundle(cand=state["cand"], tickers=state["tickers"], bench=bench, data=state["data"], px=state["px"], spx=state["spx"], tickers_bulk=state["tickers_bulk"], info=state["info"], eps_df=state["eps_df"], fcf_df=state["fcf_df"], returns=state["returns"], missing_logs=state["missing_logs"])
L1622
L1623 def run_group(sc: Scorer, group: str, inb: InputBundle, cfg: PipelineConfig,
L1624               n_target: int) -> tuple[list, float, float, float]:
L1625     """
L1626     G/Dを同一手順で処理：採点→フィルター→選定（相関低減込み）。
L1627     戻り値：(pick, avg_res_corr, sum_score, objective)
L1628     JSON保存は既存フォーマット（キー名・丸め桁・順序）を踏襲。
L1629     """
L1630     sc.cfg = cfg
L1631
L1632     if hasattr(sc, "score_build_features"):
L1633         feat = sc.score_build_features(inb)
L1634         if not hasattr(sc, "_feat_logged"):
L1635             T.log("features built (scorer)")
L
```