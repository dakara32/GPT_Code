```text
            add = [t for t in near_D if t not in set(D_UNI)][:10]
L1435             if add:
L1436                 d_disp2 = pd.DataFrame(index=add)
L1437                 d_disp2['QAL'], d_disp2['YLD'], d_disp2['VOL'], d_disp2['TRD'] = df_z.loc[add,'D_QAL'], df_z.loc[add,'D_YLD'], df_z.loc[add,'D_VOL_RAW'], df_z.loc[add,'D_TRD']
L1438                 near_tbl = pd.concat([d_disp2, pd.Series({t: d_score_all.get(t) for t in add}, name='DSC')], axis=1)
L1439                 self.d_table = pd.concat([self.d_table, near_tbl], axis=0)
L1440         print(self.d_title); print(self.d_table.to_string(formatters=self.d_formatters))
L1441
L1442         # === Changes（IN の GSC/DSC を表示。OUT は銘柄名のみ） ===
L1443         in_list = sorted(set(list(top_G)+list(top_D)) - set(exist))
L1444         out_list = sorted(set(exist) - set(list(top_G)+list(top_D)))
L1445
L1446         self.io_table = pd.DataFrame({
L1447             'IN': pd.Series(in_list),
L1448             '/ OUT': pd.Series(out_list)
L1449         })
L1450         g_list = [f"{g_score.get(t):.3f}" if pd.notna(g_score.get(t)) else '—' for t in out_list]
L1451         d_list = [f"{d_score_all.get(t):.3f}" if pd.notna(d_score_all.get(t)) else '—' for t in out_list]
L1452         self.io_table['GSC'] = pd.Series(g_list)
L1453         self.io_table['DSC'] = pd.Series(d_list)
L1454
L1455         print("Changes:")
L1456         print(self.io_table.to_string(index=False))
L1457
L1458         all_tickers = list(set(exist + list(top_G) + list(top_D) + [bench])); prices = yf.download(all_tickers, period='1y', auto_adjust=True, progress=False, threads=False)['Close'].ffill(limit=2)
L1459         ret = prices.pct_change(); portfolios = {'CUR':exist,'NEW':list(top_G)+list(top_D)}; metrics={}
L1460         for name,ticks in portfolios.items():
L1461             pr = ret[ticks].mean(axis=1, skipna=True).dropna(); cum = (1+pr).cumprod()-1; n = len(pr)
L1462             if n>=252: ann_ret, ann_vol = (1+cum.iloc[-1])**(252/n)-1, pr.std()*np.sqrt(252)
L1463             else: ann_ret, ann_vol = cum.iloc[-1], pr.std()*np.sqrt(n)
L1464             sharpe, drawdown = ann_ret/ann_vol, (cum - cum.cummax()).min()
L1465             if len(ticks)>=2:
L1466                 C_raw = ret[ticks].corr(); RAW_rho = C_raw.mask(np.eye(len(ticks), dtype=bool)).stack().mean()
L1467                 R = ret[ticks].dropna().to_numpy(); C_resid = Selector.residual_corr(R, n_pc=3, shrink=DRRS_SHRINK)
L1468                 RESID_rho = float((C_resid.sum()-np.trace(C_resid))/(C_resid.shape[0]*(C_resid.shape[0]-1)))
L1469             else: RAW_rho = RESID_rho = np.nan
L1470             divy = ttm_div_yield_portfolio(ticks); metrics[name] = {'RET':ann_ret,'VOL':ann_vol,'SHP':sharpe,'MDD':drawdown,'RAWρ':RAW_rho,'RESIDρ':RESID_rho,'DIVY':divy}
L1471         df_metrics = pd.DataFrame(metrics).T; df_metrics_pct = df_metrics.copy(); self.df_metrics = df_metrics
L1472         for col in ['RET','VOL','MDD','DIVY']: df_metrics_pct[col] = df_metrics_pct[col]*100
L1473         cols_order = ['RET','VOL','SHP','MDD','RAWρ','RESIDρ','DIVY']; df_metrics_pct = df_metrics_pct.reindex(columns=cols_order)
L1474         def _fmt_row(s):
L1475             return pd.Series({'RET':f"{s['RET']:.1f}%",'VOL':f"{s['VOL']:.1f}%",'SHP':f"{s['SHP']:.1f}",'MDD':f"{s['MDD']:.1f}%",'RAWρ':(f"{s['RAWρ']:.2f}" if pd.notna(s['RAWρ']) else "NaN"),'RESIDρ':(f"{s['RESIDρ']:.2f}" if pd.notna(s['RESIDρ']) else "NaN"),'DIVY':f"{s['DIVY']:.1f}%"})
L1476         self.df_metrics_fmt = df_metrics_pct.apply(_fmt_row, axis=1); print("Performance Comparison:"); print(self.df_metrics_fmt.to_string())
L1477         # === 追加: GSC+DSC が低い順 TOP10 ===
L1478         try:
L1479             all_scores = pd.DataFrame({'GSC': df_z['GSC'], 'DSC': df_z['DSC']}).copy()
L1480             all_scores['G_plus_D'] = all_scores['GSC'] + all_scores['DSC']
L1481             all_scores = all_scores.dropna(subset=['G_plus_D'])
L1482             self.low10_table = all_scores.sort_values('G_plus_D', ascending=True).head(10).round(3)
L1483             print("Low Score Candidates (GSC+DSC bottom 10):")
L1484             print(self.low10_table.to_string())
L1485         except Exception as e:
L1486             print(f"[warn] low-score ranking failed: {e}")
L1487             self.low10_table = None
L1488         self.debug_text = ""
L1489         if debug_mode:
L1490             logger.info("debug_mode=True: df_z dump handled in scorer; skipping factor-side debug output")
L1491         else:
L1492             logger.debug(
L1493                 "skip debug log: debug_mode=%s debug_text_empty=%s",
L1494                 debug_mode, True
L1495             )
L1496         self._debug_logged = True
L1497
L1498     # --- Slack送信（元 notify_slack のロジックそのまま） ---
L1499     def notify_slack(self):
L1500         SLACK_WEBHOOK_URL = os.environ.get("SLACK_WEBHOOK_URL")
L1501
L1502         if not SLACK_WEBHOOK_URL:
L1503             print("⚠️ SLACK_WEBHOOK_URL not set (main report skipped)")
L1504             return
L1505
L1506         def _filter_suffix_from(spec: dict, group: str) -> str:
L1507             g = spec.get(group, {})
L1508             parts = [str(m) for m in g.get("pre_mask", [])]
L1509             for k, v in (g.get("pre_filter", {}) or {}).items():
L1510                 base, op = (k[:-4], "<") if k.endswith("_max") else ((k[:-4], ">") if k.endswith("_min") else (k, "="))
L1511                 name = {"beta": "β"}.get(base, base)
L1512                 try:
L1513                     val = f"{float(v):g}"
L1514                 except Exception:
L1515                     val = str(v)
L1516                 parts.append(f"{name}{op}{val}")
L1517             return "" if not parts else " / filter:" + " & ".join(parts)
L1518
L1519         def _inject_filter_suffix(title: str, group: str) -> str:
L1520             suf = _filter_suffix_from(FILTER_SPEC, group)
L1521             return f"{title[:-1]}{suf}]" if suf and title.endswith("]") else (title + suf)
L1522
L1523         def _blk(title, tbl, fmt=None, drop=()):
L1524             if tbl is None or getattr(tbl, 'empty', False):
L1525                 return f"{title}\n(選定なし)\n"
L1526             if drop and hasattr(tbl, 'columns'):
L1527                 keep = [c for c in tbl.columns if c not in drop]
L1528                 tbl, fmt = tbl[keep], {k: v for k, v in (fmt or {}).items() if k in keep}
L1529             return f"{title}\n```{tbl.to_string(formatters=fmt)}```\n"
L1530
L1531         message = "📈 ファクター分散最適化の結果\n"
L1532         miss_df, truncated, total = self._miss_disp_info or self._prepare_missing_display(self.miss_df)
L1533         lines = compact_missing_lines(miss_df, limit=300)
L1534         missing_txt = "Missing Data:\n```" + "\n".join(lines) + "```" if lines else ""
L1535         trunc_note = f"...省略 ({total}件中 上位20件のみ表示)" if truncated else ""
L1536         if missing_txt:
L1537             message += missing_txt + ("\n" + trunc_note if trunc_note else "") + "\n"
L1538         message += _blk(_inject_filter_suffix(self.g_title, "G"), self.g_table, self.g_formatters, drop=("TRD",))
L1539         message += _blk(_inject_filter_suffix(self.d_title, "D"), self.d_table, self.d_formatters)
L1540         message += "Changes\n" + ("(変更なし)\n" if self.io_table is None or getattr(self.io_table, 'empty', False) else f"```{self.io_table.to_string(index=False)}```\n")
L1541         message += "Performance Comparison:\n```" + self.df_metrics_fmt.to_string() + "```"
L1542
L1543         try:
L1544             r = requests.post(SLACK_WEBHOOK_URL, json={"text": message})
L1545             print(f"[DBG] main_post status={getattr(r, 'status_code', None)} size={len(message)}")
L1546             if r is not None:
L1547                 r.raise_for_status()
L1548         except Exception as e:
L1549             print(f"[ERR] main_post_failed: {e}")
L1550
L1551 def _infer_g_universe(feature_df, selected12=None, near5=None):
L1552     try:
L1553         out = feature_df.index[feature_df['group'].astype(str).str.upper().eq('G')].tolist()
L1554         if out: return out
L1555     except Exception:
L1556         pass
L1557     base = set()
L1558     for lst in (selected12 or []), (near5 or []):
L1559         for x in (lst or []): base.add(x)
L1560     return list(base) if base else list(feature_df.index)
L1561
L1562 def _fmt_with_fire_mark(tickers, feature_df):
L1563     out = []
L1564     for t in tickers or []:
L1565         try:
L1566             br = bool(feature_df.at[t, "G_BREAKOUT_recent_5d"])
L1567             pb = bool(feature_df.at[t, "G_PULLBACK_recent_5d"])
L1568             out.append(f"{t}{' 🔥' if (br or pb) else ''}")
L1569         except Exception:
L1570             out.append(t)
L1571     return out
L1572
L1573 def _label_recent_event(t, feature_df):
L1574     try:
L1575         br = bool(feature_df.at[t, "G_BREAKOUT_recent_5d"]); dbr = str(feature_df.at[t, "G_BREAKOUT_last_date"]) if br else ""
L1576         pb = bool(feature_df.at[t, "G_PULLBACK_recent_5d"]); dpb = str(feature_df.at[t, "G_PULLBACK_last_date"]) if pb else ""
L1577         if   br and not pb: return f"{t}（ブレイクアウト確定 {dbr}）"
L1578         elif pb and not br: return f"{t}（押し目反発 {dpb}）"
L1579         elif br and pb:     return f"{t}（ブレイクアウト確定 {dbr}／押し目反発 {dpb}）"
L1580     except Exception:
L1581         pass
L1582     return t
L1583
L1584 # === パイプライン可視化：G/D共通フロー（出力は不変） ===
L1585
L1586 def io_build_input_bundle() -> InputBundle:
L1587     """
L1588     既存の『データ取得→前処理』を実行し、InputBundle を返す。
L1589     処理内容・列名・丸め・例外・ログ文言は現行どおり（変更禁止）。
L1590     """
L1591     state = Input(cand=cand, exist=exist, bench=bench, price_max=CAND_PRICE_MAX, finnhub_api_key=FINNHUB_API_KEY).prepare_data()
L1592     return InputBundle(cand=state["cand"], tickers=state["tickers"], bench=bench, data=state["data"], px=state["px"], spx=state["spx"], tickers_bulk=state["tickers_bulk"], info=state["info"], eps_df=state["eps_df"], fcf_df=state["fcf_df"], returns=state["returns"], missing_logs=state["missing_logs"])
L1593
L1594 def run_group(sc: Scorer, group: str, inb: InputBundle, cfg: PipelineConfig,
L1595               n_target: int) -> tuple[list, float, float, float]:
L1596     """
L1597     G/Dを同一手順で処理：採点→フィルター→選定（相関低減込み）。
L1598     戻り値：(pick, avg_res_corr, sum_score, objective)
L1599     JSON保存は既存フォーマット（キー名・丸め桁・順序）を踏襲。
L1600     """
L1601     sc.cfg = cfg
L1602
L1603     if hasattr(sc, "score_build_features"):
L1604         feat = sc.score_build_features(inb)
L1605         if not hasattr(sc, "_feat_logged"):
L1606             T.log("features built (scorer)")
L1607             sc._feat_logged = True
L1608         agg = sc.score_aggregate(feat, group, cfg) if hasattr(sc, "score_aggregate") else feat
L1609     else:
L1610         fb = sc.aggregate_scores(inb, cfg)
L1611         if not hasattr(sc, "_feat_logged"):
L1612             T.log("features built (scorer)")
L1613             sc._feat_logged = True
L1614         sc._feat = fb
L1615         agg = fb.g_score if group == "G" else fb.d_score_all
L1616         if group == "D" and hasattr(fb, "df"):
L1617             agg = agg[fb.df['BETA'] < D_BETA_MAX]
L1618
L1619     if hasattr(sc, "filter_candidates"):
L1620         agg = agg[sc.filter_candidates(inb, agg, group, cfg)]
L1621
L1622     if isinstance(agg, pd.Series):
L1623         agg = _as_numeric_series(agg)
L1624
L1625     selector = Selector()
L1626     if hasattr(sc, "select_diversified"):
L1627         pick, avg_r, sum_sc, obj = sc.select_diversified(agg, group, cfg, n_target,
L1628             selector=selector, prev_tickers=None,
L1629             corrM=cfg.drrs.corrM, shrink=cfg.drrs.shrink,
L1630             cross_mu=cfg.drrs.cross_mu_gd)
L1631     else:
L1632         if group == "G":
L1633             init = agg.nlargest(min(cfg.drrs.corrM, len(agg))).index.tolist()
L1634             res = selector.select_bucket_drrs(returns_df=inb.returns, score_ser=agg, pool_tickers=init, k=n_target,
L1635                 n_pc=cfg.drrs.G.get("n_pc", 3), gamma=cfg.drrs.G.get("gamma"
```