```text
A200_UP_STREAK_D']).clip(-3.0,3.0)
L604         df_z['VOL'] = robust_z(df['BETA'])
L605         df_z.rename(columns={'GROWTH_F':'GRW','MOM_F':'MOM','QUALITY_F':'QAL','YIELD_F':'YLD'}, inplace=True)
L606
L607         # === begin: BIO LOSS PENALTY =====================================
L608         try:
L609             penalty_z = float(os.getenv("BIO_LOSS_PENALTY_Z", "0.8"))
L610         except Exception:
L611             penalty_z = 0.8
L612
L613         def _is_bio_like(t: str) -> bool:
L614             inf = info.get(t, {}) if isinstance(info, dict) else {}
L615             sec = str(inf.get("sector", "")).lower()
L616             ind = str(inf.get("industry", "")).lower()
L617             if "health" not in sec:
L618                 return False
L619             keys = ("biotech", "biopharma", "pharma")
L620             return any(k in ind for k in keys)
L621
L622         tickers_s = pd.Index(df_z.index)
L623         is_bio = pd.Series({t: _is_bio_like(t) for t in tickers_s})
L624         is_loss = pd.Series({t: (pd.notna(df.loc[t,"EPS"]) and df.loc[t,"EPS"] <= 0) for t in tickers_s})
L625         mask_bio_loss = (is_bio & is_loss).reindex(df_z.index).fillna(False)
L626
L627         if bool(mask_bio_loss.any()) and penalty_z > 0:
L628             df_z.loc[mask_bio_loss, "GRW"] = df_z.loc[mask_bio_loss, "GRW"] - penalty_z
L629             df_z["GRW"] = df_z["GRW"].clip(-3.0, 3.0)
L630         # === end: BIO LOSS PENALTY =======================================
L631
L632         df_z['TRD'] = 0.0  # TRDはスコア寄与から外し、テンプレ判定はフィルタで行う（列は表示互換のため残す）
L633         if 'BETA' not in df_z.columns: df_z['BETA'] = robust_z(df['BETA'])
L634
L635         df_z['D_VOL_RAW'] = robust_z(0.40*df_z['DOWNSIDE_DEV'] + 0.22*df_z['RESID_VOL'] + 0.18*df_z['MDD_1Y'] - 0.10*df_z['DOWN_OUTPERF'] - 0.05*df_z['EXT_200'] - 0.08*df_z['SIZE'] - 0.10*df_z['LIQ'] + 0.10*df_z['BETA'])
L636         df_z['D_QAL']     = robust_z(0.35*df_z['QAL'] + 0.20*df_z['FCF'] + 0.15*df_z['CURR_RATIO'] - 0.15*df_z['DEBT2EQ'] - 0.15*df_z['EPS_VAR_8Q'])
L637         df_z['D_YLD']     = df['YLD']
L638         df_z['D_TRD']     = robust_z(0.40*df_z.get('MA200_SLOPE_5M',0) - 0.30*df_z.get('EXT_200',0) + 0.15*df_z.get('NEAR_52W_HIGH',0) + 0.15*df_z['TR'])
L639
L640         # --- 重みは cfg を優先（外部があればそれを使用） ---
L641         # ① 全銘柄で G/D スコアを算出（unmasked）
L642         g_score_all = df_z.mul(pd.Series(cfg.weights.g)).sum(axis=1)
L643
L644         d_comp = pd.concat({
L645             'QAL': df_z['D_QAL'],
L646             'YLD': df_z['D_YLD'],
L647             'VOL': df_z['D_VOL_RAW'],
L648             'TRD': df_z['D_TRD']
L649         }, axis=1)
L650         dw = pd.Series(cfg.weights.d, dtype=float).reindex(['QAL','YLD','VOL','TRD']).fillna(0.0)
L651         globals()['D_WEIGHTS_EFF'] = dw.copy()
L652         d_score_all = d_comp.mul(dw, axis=1).sum(axis=1)
L653
L654         # ② テンプレ判定（既存ロジックそのまま）
L655         mask = df['trend_template']
L656         if not bool(mask.any()):
L657             mask = ((df.get('P_OVER_LOW52', np.nan) >= 0.25) &
L658                 (df.get('NEAR_52W_HIGH', np.nan) >= -0.30) &
L659                 (df.get('RS', np.nan) >= 0.08) &
L660                 (df.get('MA200_SLOPE_1M', np.nan) > 0) &
L661                 (df.get('P_OVER_150', np.nan) > 0) & (df.get('P_OVER_200', np.nan) > 0) &
L662                 (df.get('MA150_OVER_200', np.nan) > 0) &
L663                 (df.get('MA50_OVER_150', np.nan) > 0) & (df.get('MA50_OVER_200', np.nan) > 0) &
L664                 (df.get('TR_str', np.nan) > 0)).fillna(False)
L665             df['trend_template'] = mask
L666
L667         # ③ 採用用は mask、表示/分析用は列で全銘柄保存
L668         g_score = g_score_all.loc[mask]
L669         Scorer.g_score = g_score
L670         df_z['GSC'] = g_score_all
L671         df_z['DSC'] = d_score_all
L672
L673         try:
L674             current = (pd.read_csv("current_tickers.csv")
L675                   .iloc[:, 0]
L676                   .str.upper()
L677                   .tolist())
L678         except FileNotFoundError:
L679             warnings.warn("current_tickers.csv not found — bonus skipped")
L680             current = []
L681
L682         mask_bonus = g_score.index.isin(current)
L683         if mask_bonus.any():
L684             # 1) factor.BONUS_COEFF から k を決め、無ければ 0.4
L685             k = float(getattr(sys.modules.get("factor"), "BONUS_COEFF", 0.4))
L686             # 2) g 側の σ を取り、NaN なら 0 に丸める
L687             sigma_g = g_score.std()
L688             if pd.isna(sigma_g):
L689                 sigma_g = 0.0
L690             bonus_g = round(k * sigma_g, 3)
L691             g_score.loc[mask_bonus] += bonus_g
L692             Scorer.g_score = g_score
L693             # 3) D 側も同様に σ の NaN をケア
L694             sigma_d = d_score_all.std()
L695             if pd.isna(sigma_d):
L696                 sigma_d = 0.0
L697             bonus_d = round(k * sigma_d, 3)
L698             d_score_all.loc[d_score_all.index.isin(current)] += bonus_d
L699
L700         try:
L701             df = _apply_growth_entry_flags(df, ib, self, win_breakout=5, win_pullback=5)
L702         except Exception:
L703             pass
L704
L705         from factor import FeatureBundle  # type: ignore  # 実行時importなし（循環回避）
L706         return FeatureBundle(df=df,
L707             df_z=df_z,
L708             g_score=g_score,
L709             d_score_all=d_score_all,
L710             missing_logs=pd.DataFrame(missing_logs))
L711
L712 def _apply_growth_entry_flags(feature_df, bundle, self_obj, win_breakout=5, win_pullback=5):
L713     """
L714     G枠ユニバースに対し、ブレイクアウト確定/押し目反発の「直近N営業日内の発火」を判定し、
L715     次の列を feature_df に追加する（index=ticker）。
L716       - G_BREAKOUT_recent_5d : bool
L717       - G_BREAKOUT_last_date : str "YYYY-MM-DD"
L718       - G_PULLBACK_recent_5d : bool
L719       - G_PULLBACK_last_date : str "YYYY-MM-DD"
L720       - G_PIVOT_price        : float
L721     失敗しても例外は握り潰し、既存処理を阻害しない。
L722     """
L723     try:
L724         px   = bundle.px                      # 終値 DataFrame
L725         hi   = bundle.data['High']
L726         lo   = bundle.data['Low']
L727         vol  = bundle.data['Volume']
L728         bench= bundle.spx                     # ベンチマーク Series
L729
L730         # Gユニバース推定：self.g_universe 優先 → feature_df['group']=='G' → 全銘柄
L731         g_universe = getattr(self_obj, "g_universe", None)
L732         if g_universe is None:
L733             try:
L734                 g_universe = feature_df.index[feature_df['group'].astype(str).str.upper().eq('G')].tolist()
L735             except Exception:
L736                 g_universe = list(feature_df.index)
L737         if not g_universe:
L738             return feature_df
L739
L740         # 指標
L741         ema21 = px[g_universe].ewm(span=21, adjust=False).mean()
L742         ma50  = px[g_universe].rolling(50).mean()
L743         ma150 = px[g_universe].rolling(150).mean()
L744         ma200 = px[g_universe].rolling(200).mean()
L745         atr20 = (hi[g_universe] - lo[g_universe]).rolling(20).mean()
L746         vol20 = vol[g_universe].rolling(20).mean()
L747         vol50 = vol[g_universe].rolling(50).mean()
L748
L749         # トレンドテンプレート合格
L750         trend_template_ok = (px[g_universe] > ma50) & (px[g_universe] > ma150) & (px[g_universe] > ma200) \
L751                             & (ma150 > ma200) & (ma200.diff() > 0)
L752
L753         # 汎用ピボット：直近65営業日の高値（当日除外）
L754         pivot_price = hi[g_universe].rolling(65).max().shift(1)
L755
L756         # 相対力：年内高値更新
L757         bench_aligned = bench.reindex(px.index).ffill()
L758         rs = px[g_universe].div(bench_aligned, axis=0)
L759         rs_high = rs.rolling(252).max().shift(1)
L760
L761         # ブレイクアウト「発生日」：条件立ち上がり
L762         breakout_today = trend_template_ok & (px[g_universe] > pivot_price) \
L763                          & (vol[g_universe] >= 1.5 * vol50) & (rs > rs_high)
L764         breakout_event = breakout_today & ~breakout_today.shift(1).fillna(False)
L765
L766         # 押し目反発「発生日」：EMA21帯×出来高ドライアップ×前日高値越え×終値EMA21上
L767         near_ema21_band = px[g_universe].between(ema21 - atr20, ema21 + atr20)
L768         volume_dryup = (vol20 / vol50) <= 1.0
L769         pullback_bounce_confirmed = (px[g_universe] > hi[g_universe].shift(1)) & (px[g_universe] > ema21)
L770         pullback_today = trend_template_ok & near_ema21_band & volume_dryup & pullback_bounce_confirmed
L771         pullback_event = pullback_today & ~pullback_today.shift(1).fillna(False)
L772
L773         # 直近N営業日内の発火 / 最終発生日
L774         rows = []
L775         for t in g_universe:
L776             def _recent_and_date(s, win):
L777                 sw = s[t].iloc[-win:]
L778                 if sw.any():
L779                     d = sw[sw].index[-1]
L780                     return True, d.strftime("%Y-%m-%d")
L781                 return False, ""
L782             br_recent, br_date = _recent_and_date(breakout_event, win_breakout)
L783             pb_recent, pb_date = _recent_and_date(pullback_event, win_pullback)
L784             rows.append((t, {
L785                 "G_BREAKOUT_recent_5d": br_recent,
L786                 "G_BREAKOUT_last_date": br_date,
L787                 "G_PULLBACK_recent_5d": pb_recent,
L788                 "G_PULLBACK_last_date": pb_date,
L789                 "G_PIVOT_price": float(pivot_price[t].iloc[-1]) if t in pivot_price.columns else float('nan'),
L790             }))
L791         flags = pd.DataFrame({k: v for k, v in rows}).T
L792
L793         # 列を作成・上書き
L794         cols = ["G_BREAKOUT_recent_5d","G_BREAKOUT_last_date","G_PULLBACK_recent_5d","G_PULLBACK_last_date","G_PIVOT_price"]
L795         for c in cols:
L796             if c not in feature_df.columns:
L797                 feature_df[c] = np.nan
L798         feature_df.loc[flags.index, flags.columns] = flags
L799
L800     except Exception:
L801         pass
L802     return feature_df
L803
```

## <.github/workflows/weekly-report.yml>
```text
L1 name: Weekly Stock Report
L2
L3 on:
L4   push:
L5     branches: [ main ]
L6     paths-ignore:
L7       - 'CodeForChat/**'
L8   schedule:
L9     - cron: '0 0 * * 6'  # UTC 00:00 → JST 09:00（土）
L10   workflow_dispatch:
L11
L12 jobs:
L13   build-and-report:
L14     runs-on: ubuntu-latest
L15     permissions:
L16       contents: write
L17
L18     steps:
L19       - name: Debug start
L20         run: echo '🚀 DEBUGstarted'
L21               
L22       - name: Checkout repository
L23         uses: actions/checkout@v3
L24
L25       - name: Setup Python
L26         uses: actions/setup-python@v5
L27         with:
L28           python-version: '3.x'
L29           cache: 'pip'
L30           cache-dependency-path: requirements.txt
L31
L32       - name: Install dependencies
L33         run: pip install -r requirements.txt
L34
L35       - name: Prepare results directory
L36         run: mkdir -p results
L37
L38       - name: Run factor & scoring
L39         env:
L40           SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
L41           FINNHUB_API_KEY: ${{ secrets.FINNHUB_API_KEY }}
L42           FIN_THREADS: "8"
L43         run: python factor.py
```

## <documents/README.md>
```text
L1 # 運用ルール
L2
L3 ## 基本構成
L4 - 20銘柄を均等配分（現金を除き1銘柄あたり5%）
L5 - moomoo証券で運用
L6 - **Growth枠 12銘柄 / Defense枠 8銘柄**（NORMAL 基準）
L7
L8 ## Barbell Growth-Defense方針
L9 - Growth枠 **12銘柄**：高成長で乖離源となる攻めの銘柄
L10 - Defense枠 **8銘柄**：低ボラで安定成長し配当を増やす守りの銘柄
L11 - 「猛烈に伸びる攻め × 着実に稼ぐ盾」の組合せで乖離→半戻しプレミアムを狙う
L12
L13 ## レジーム判定（trend_template 合格“本数”で判定）
L14 - 合格本数 = current+candidate 全体のうち、trend_template 条件を満たした銘柄の**本数(C)**（基準 N_G=12）
L15 - しきい値は過去~600営業日の分布から**毎回自動採用**（分位点と運用“床”のmax）
L16   - 緊急入り: `max(q05, 12本)`（= N_G）
L17   - 緊急解除: `max(q20, 18本)`（= ceil(1.5×12)）
L18   - 通常復帰: `max(q60, 36本)`（= 3×N_G）
L19 - ヒステリシス: 前回モードに依存（EMERG→解除は23本以上、CAUTION→通常は45本以上）
L20
L21 ## レジーム別の現金・ドリフト
L22  - **通常(NORMAL)** : 現金 **10%** / ドリフト閾値 **12%**
L23  - **警戒(CAUTION)** : 現金 **12.5%** / ドリフト閾値 **14%**
L24  - **緊急(EMERG)** : 現金 **20%** / **ドリフト売買停止**（20×5%に全戻しのみ）
L25
L26 ## モード別の推奨“保有銘柄数”（MMF≒現金）
L27 *各枠=5%（20銘柄均等）。モード移行時は**Gの枠数のみ**調整
```