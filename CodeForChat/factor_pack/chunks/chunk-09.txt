```text
52).max()
L640         tt &= (px.divide(lo252).sub(1.0) >= 0.30)   # P_OVER_LOW52 >= 0.30
L641         tt &= (px >= (0.75 * hi252))                # NEAR_52W_HIGH >= -0.25
L642
L643         r12  = px.divide(px.shift(252)).sub(1.0)
L644         br12 = spx.divide(spx.shift(252)).sub(1.0)
L645         r1   = px.divide(px.shift(22)).sub(1.0)
L646         br1  = spx.divide(spx.shift(22)).sub(1.0)
L647         rs   = 0.7*(r12.sub(br12, axis=0)) + 0.3*(r1.sub(br1, axis=0))
L648         tt &= (rs >= 0.10)
L649
L650         return tt.fillna(False).sum(axis=1).astype(int)
L651
L652     # ---- スコア集計（DTO/Configを受け取り、FeatureBundleを返す） ----
L653     def aggregate_scores(self, ib: Any, cfg):
L654         if cfg is None:
L655             raise ValueError("cfg is required; pass factor.PipelineConfig")
L656         self._validate_ib_for_scorer(ib)
L657
L658         px, spx, tickers = ib.px, ib.spx, ib.tickers
L659         tickers_bulk, info, eps_df, fcf_df = ib.tickers_bulk, ib.info, ib.eps_df, ib.fcf_df
L660
L661         df, missing_logs = pd.DataFrame(index=tickers), []
L662         for t in tickers:
L663             d, s = info[t], px[t]; ev = self.ev_fallback(d, tickers_bulk.tickers[t])
L664             try:
L665                 volume_series_full = ib.data['Volume'][t]
L666             except Exception:
L667                 volume_series_full = None
L668
L669             grw_result = _calc_grw_flexible(t, d, s, volume_series_full)
L670             df.loc[t,'GRW_FLEX_SCORE'] = grw_result.get('score')
L671             df.loc[t,'GRW_FLEX_WEIGHT'] = grw_result.get('weight')
L672             df.loc[t,'GRW_FLEX_CORE'] = grw_result.get('core')
L673             df.loc[t,'GRW_FLEX_PRICE'] = grw_result.get('price_proxy')
L674             df.loc[t,'DEBUG_GRW_PATH'] = grw_result.get('path')
L675             df.loc[t,'DEBUG_GRW_PARTS'] = grw_result.get('parts')
L676
L677             # --- 基本特徴 ---
L678             df.loc[t,'TR']   = self.trend(s)
L679             df.loc[t,'EPS']  = _scalar(eps_df.loc[t,'EPS_TTM']) if t in eps_df.index else np.nan
L680             df.loc[t,'EPS_Q'] = _scalar(eps_df.loc[t,'EPS_Q_LastQ']) if t in eps_df.index else np.nan
L681             df.loc[t,'REV_TTM'] = _scalar(eps_df.loc[t,'REV_TTM']) if t in eps_df.index else np.nan
L682             df.loc[t,'REV_Q']   = _scalar(eps_df.loc[t,'REV_Q_LastQ']) if t in eps_df.index else np.nan
L683             df.loc[t,'REV']  = d.get('revenueGrowth',np.nan)
L684             df.loc[t,'ROE']  = d.get('returnOnEquity',np.nan)
L685             df.loc[t,'BETA'] = self.calc_beta(s, spx, lookback=252)
L686
L687             # --- 配当（欠損補完含む） ---
L688             div = d.get('dividendYield') if d.get('dividendYield') is not None else d.get('trailingAnnualDividendYield')
L689             if div is None or pd.isna(div):
L690                 try:
L691                     divs = yf.Ticker(t).dividends
L692                     if divs is not None and not divs.empty:
L693                         last_close = s.iloc[-1]; div_1y = divs[divs.index >= (divs.index.max() - pd.Timedelta(days=365))].sum()
L694                         if last_close and last_close>0: div = float(div_1y/last_close)
L695                 except Exception: pass
L696             df.loc[t,'DIV'] = 0.0 if (div is None or pd.isna(div)) else float(div)
L697
L698             # --- FCF/EV ---
L699             fcf_val = fcf_df.loc[t,'FCF_TTM'] if t in fcf_df.index else np.nan
L700             df.loc[t,'FCF'] = (fcf_val/ev) if (pd.notna(fcf_val) and pd.notna(ev) and ev>0) else np.nan
L701
L702             # --- モメンタム・ボラ関連 ---
L703             df.loc[t,'RS'], df.loc[t,'TR_str'] = self.rs(s, spx), self.tr_str(s)
L704             r, rm = s.pct_change().dropna(), spx.pct_change().dropna()
L705             n = int(min(len(r), len(rm)))
L706
L707             DOWNSIDE_DEV = np.nan
L708             if n>=60:
L709                 r6 = r.iloc[-min(len(r),126):]; neg = r6[r6<0]
L710                 if len(neg)>=10: DOWNSIDE_DEV = float(neg.std(ddof=0)*np.sqrt(252))
L711             df.loc[t,'DOWNSIDE_DEV'] = DOWNSIDE_DEV
L712
L713             MDD_1Y = np.nan
L714             try:
L715                 w = s.iloc[-min(len(s),252):].dropna()
L716                 if len(w)>=30:
L717                     roll_max = w.cummax(); MDD_1Y = float((w/roll_max - 1.0).min())
L718             except Exception: pass
L719             df.loc[t,'MDD_1Y'] = MDD_1Y
L720
L721             RESID_VOL = np.nan
L722             if n>=120:
L723                 rr, rrm = r.iloc[-n:].align(rm.iloc[-n:], join='inner')
L724                 if len(rr)==len(rrm) and len(rr)>=120 and rrm.var()>0:
L725                     beta = float(np.cov(rr, rrm)[0,1]/np.var(rrm)); resid = rr - beta*rrm
L726                     RESID_VOL = float(resid.std(ddof=0)*np.sqrt(252))
L727             df.loc[t,'RESID_VOL'] = RESID_VOL
L728
L729             DOWN_OUTPERF = np.nan
L730             if n>=60:
L731                 m, x = rm.iloc[-n:], r.iloc[-n:]; mask = m<0
L732                 if mask.sum()>=10:
L733                     mr, sr = float(m[mask].mean()), float(x[mask].mean())
L734                     DOWN_OUTPERF = (sr - mr)/abs(mr) if mr!=0 else np.nan
L735             df.loc[t,'DOWN_OUTPERF'] = DOWN_OUTPERF
L736
L737             # --- 長期移動平均/位置 ---
L738             sma200 = s.rolling(200).mean(); df.loc[t,'EXT_200'] = np.nan
L739             if pd.notna(sma200.iloc[-1]) and sma200.iloc[-1]!=0: df.loc[t,'EXT_200'] = abs(float(s.iloc[-1]/sma200.iloc[-1]-1.0))
L740
L741             # --- 配当の詳細系 ---
L742             DIV_TTM_PS=DIV_VAR5=DIV_YOY=DIV_FCF_COVER=np.nan
L743             try:
L744                 divs = yf.Ticker(t).dividends.dropna()
L745                 if not divs.empty:
L746                     last_close = s.iloc[-1]; div_1y = float(divs[divs.index >= (divs.index.max()-pd.Timedelta(days=365))].sum())
L747                     DIV_TTM_PS = div_1y if div_1y>0 else np.nan
L748                     ann = divs.groupby(divs.index.year).sum()
L749                     if len(ann)>=2 and ann.iloc[-2]!=0: DIV_YOY = float(ann.iloc[-1]/ann.iloc[-2]-1.0)
L750                     tail = ann.iloc[-5:] if len(ann)>=5 else ann
L751                     if len(tail)>=3 and tail.mean()!=0: DIV_VAR5 = float(tail.std(ddof=1)/abs(tail.mean()))
L752                 so = d.get('sharesOutstanding',None)
L753                 if so and pd.notna(DIV_TTM_PS) and pd.notna(fcf_val) and fcf_val!=0:
L754                     DIV_FCF_COVER = float((fcf_val)/(DIV_TTM_PS*float(so)))
L755             except Exception: pass
L756             df.loc[t,'DIV_TTM_PS'], df.loc[t,'DIV_VAR5'], df.loc[t,'DIV_YOY'], df.loc[t,'DIV_FCF_COVER'] = DIV_TTM_PS, DIV_VAR5, DIV_YOY, DIV_FCF_COVER
L757
L758             # --- 財務安定性 ---
L759             df.loc[t,'DEBT2EQ'], df.loc[t,'CURR_RATIO'] = d.get('debtToEquity',np.nan), d.get('currentRatio',np.nan)
L760
L761             # --- EPS 変動 ---
L762             EPS_VAR_8Q = np.nan
L763             try:
L764                 qe, so = tickers_bulk.tickers[t].quarterly_earnings, d.get('sharesOutstanding',None)
L765                 if qe is not None and not qe.empty and so:
L766                     eps_q = (qe['Earnings'].dropna().astype(float)/float(so)).replace([np.inf,-np.inf],np.nan)
L767                     if len(eps_q)>=4: EPS_VAR_8Q = float(eps_q.iloc[-min(8,len(eps_q)):].std(ddof=1))
L768             except Exception: pass
L769             df.loc[t,'EPS_VAR_8Q'] = EPS_VAR_8Q
L770
L771             # --- サイズ/流動性 ---
L772             df.loc[t,'MARKET_CAP'] = d.get('marketCap',np.nan); adv60 = np.nan
L773             try:
L774                 if isinstance(volume_series_full, pd.Series):
L775                     vol_series = volume_series_full.reindex(s.index).dropna()
L776                     if len(vol_series) >= 5:
L777                         aligned_px = s.reindex(vol_series.index).dropna()
L778                         if len(aligned_px) == len(vol_series):
L779                             dv = (vol_series*aligned_px).rolling(60).mean()
L780                             if not dv.dropna().empty:
L781                                 adv60 = float(dv.dropna().iloc[-1])
L782             except Exception:
L783                 pass
L784             df.loc[t,'ADV60_USD'] = adv60
L785
L786             # --- Rule of 40 や周辺 ---
L787             total_rev_ttm = d.get('totalRevenue',np.nan)
L788             FCF_MGN = _safe_div(fcf_val, total_rev_ttm)
L789             df.loc[t,'FCF_MGN'] = FCF_MGN
L790             rule40 = np.nan
L791             try:
L792                 r = df.loc[t,'REV']; rule40 = (r if pd.notna(r) else np.nan) + (FCF_MGN if pd.notna(FCF_MGN) else np.nan)
L793             except Exception: pass
L794             df.loc[t,'RULE40'] = rule40
L795
L796             # --- トレンド補助 ---
L797             sma50  = s.rolling(50).mean()
L798             sma150 = s.rolling(150).mean()
L799             sma200 = s.rolling(200).mean()
L800             p = _safe_last(s)
L801
L802             df.loc[t,'MA50_OVER_150'] = (_safe_last(sma50)/_safe_last(sma150) - 1
L803                 if pd.notna(_safe_last(sma50)) and pd.notna(_safe_last(sma150)) and _safe_last(sma150)!=0 else np.nan)
L804             df.loc[t,'MA150_OVER_200'] = (_safe_last(sma150)/_safe_last(sma200) - 1
L805                 if pd.notna(_safe_last(sma150)) and pd.notna(_safe_last(sma200)) and _safe_last(sma200)!=0 else np.nan)
L806
L807             lo52 = s[-252:].min() if len(s)>=252 else s.min()
L808             df.loc[t,'P_OVER_LOW52'] = (p/lo52 - 1) if (lo52 and lo52>0 and pd.notna(p)) else np.nan
L809
L810             df.loc[t,'MA200_SLOPE_1M'] = np.nan
L811             if len(sma200.dropna()) >= 21:
L812                 cur200 = _safe_last(sma200)
L813                 old2001 = float(sma200.iloc[-21])
L814                 if old2001:
L815                     df.loc[t,'MA200_SLOPE_1M'] = cur200/old2001 - 1
L816
L817             df.loc[t,'P_OVER_150'] = p/_safe_last(sma150)-1 if pd.notna(_safe_last(sma150)) and _safe_last(sma150)!=0 else np.nan
L818             df.loc[t,'P_OVER_200'] = p/_safe_last(sma200)-1 if pd.notna(_safe_last(sma200)) and _safe_last(sma200)!=0 else np.nan
L819             df.loc[t,'MA50_OVER_200'] = _safe_last(sma50)/_safe_last(sma200)-1 if pd.notna(_safe_last(sma50)) and pd.notna(_safe_last(sma200)) and _safe_last(sma200)!=0 else np.nan
L820             df.loc[t,'MA200_SLOPE_5M'] = np.nan
L821             if len(sma200.dropna())>=105:
L822                 cur200, old200 = _safe_last(sma200), float(sma200.iloc[-105])
L823                 if old200 and old200!=0: df.loc[t,'MA200_SLOPE_5M'] = cur200/old200 - 1
L824             # NEW: 200日線が連続で上向きの「日数」
L825             df.loc[t,'MA200_UP_STREAK_D'] = np.nan
L826             try:
L827                 s200 = sma200.dropna()
L828                 if len(s200) >= 2:
L829                     diff200 = s200.diff()
L830                     up = 0
L831                     for v in diff200.iloc[::-1]:
L832                         if pd.isna(v) or v <= 0:
L833                             break
L834                         up += 1
L835                     df.loc[t,'MA200_UP_STREAK_D'] = float(up)
L836             except Exception:
L837                 pass
L838             df.loc[t,'LOW52PCT25_EXCESS'] = np.nan if (lo52 is None or lo52<=0 or pd.isna(p)) else (p/(lo52*1.25)-1)
L839             hi52 = s[-252:].max() if len(s)>=252 else s.max(); df.loc[t,'NEAR_52W_HIGH'] = np.nan
L840             if hi52 and hi52>0 and pd.notna(p):
L841                 d_hi = (p/hi52)-1.0; df.loc[t,'NEAR_52W_HIGH'] = -abs(min(0.0, d_hi))
L842             df.loc[t,'RS_SLOPE_6W'] = self.rs_line_slope(s, ib.spx, 30)
L843             df.loc[t,'RS_SLOPE_13W'] = self.rs_line_slope(s, ib.spx, 65)
L844
L845             df.loc[t,'DIV_STREAK'] = self.div_streak(t)
L846
L847             # --- 欠損メモ ---
L848             fin_cols = ['REV','ROE','BETA','DIV','FCF']
L849             need_finnhub = [col for col in fin_cols if pd.isna(df.loc[t,col])]
L850 
```