```text
:
L239             k=n.lower()
L240             if k in idx_lower: return df.loc[idx_lower[k]]
L241         return None
L242
L243     @staticmethod
L244     def _sum_last_n(s: pd.Series|None, n: int) -> float|None:
L245         if s is None or s.empty: return None
L246         v=s.dropna().astype(float); return None if v.empty else v.iloc[:n].sum()
L247
L248     @staticmethod
L249     def _latest(s: pd.Series|None) -> float|None:
L250         if s is None or s.empty: return None
L251         v=s.dropna().astype(float); return v.iloc[0] if not v.empty else None
L252
L253     def fetch_cfo_capex_ttm_yf(self, tickers: list[str]) -> pd.DataFrame:
L254         from concurrent.futures import ThreadPoolExecutor, as_completed
L255         pick, sumn, latest, aliases = self._pick_row, self._sum_last_n, self._latest, self._CF_ALIASES
L256
L257         def one(t: str):
L258             try:
L259                 tk = yf.Ticker(t)  # ★ セッションは渡さない（YFがcurl_cffiで管理）
L260                 qcf = tk.quarterly_cashflow
L261                 cfo_q, capex_q = pick(qcf, aliases["cfo"]), pick(qcf, aliases["capex"])
L262                 fcf_q = pick(qcf, ["Free Cash Flow","FreeCashFlow","Free cash flow"])
L263                 cfo, capex, fcf = sumn(cfo_q,4), sumn(capex_q,4), sumn(fcf_q,4)
L264                 if any(v is None for v in (cfo, capex, fcf)):
L265                     acf = tk.cashflow
L266                     if cfo   is None: cfo   = latest(pick(acf, aliases["cfo"]))
L267                     if capex is None: capex = latest(pick(acf, aliases["capex"]))
L268                     if fcf   is None: fcf   = latest(pick(acf, ["Free Cash Flow","FreeCashFlow","Free cash flow"]))
L269             except Exception as e:
L270                 print(f"[warn] yf financials error: {t}: {e}"); cfo=capex=fcf=None
L271             n=np.nan
L272             return {"ticker":t,
L273                     "cfo_ttm_yf":   n if cfo   is None else cfo,
L274                     "capex_ttm_yf": n if capex is None else capex,
L275                     "fcf_ttm_yf_direct": n if fcf is None else fcf}
L276
L277         rows, mw = [], int(os.getenv("FIN_THREADS","8"))
L278         with ThreadPoolExecutor(max_workers=mw) as ex:
L279             rows=[f.result() for f in as_completed(ex.submit(one,t) for t in tickers)]
L280         return pd.DataFrame(rows).set_index("ticker")
L281
L282     _FINN_CFO_KEYS = ["netCashProvidedByOperatingActivities","netCashFromOperatingActivities","cashFlowFromOperatingActivities","operatingCashFlow"]
L283     _FINN_CAPEX_KEYS = ["capitalExpenditure","capitalExpenditures","purchaseOfPPE","investmentsInPropertyPlantAndEquipment"]
L284
L285     @staticmethod
L286     def _first_key(d: dict, keys: list[str]):
L287         for k in keys:
L288             if k in d and d[k] is not None: return d[k]
L289         return None
L290
L291     @staticmethod
L292     def _finn_get(session: requests.Session, url: str, params: dict, retries: int=3, sleep_s: float=0.5):
L293         for i in range(retries):
L294             r = session.get(url, params=params, timeout=15)
L295             if r.status_code==429: time.sleep(min(2**i*sleep_s,4.0)); continue
L296             r.raise_for_status(); return r.json()
L297         r.raise_for_status()
L298
L299     def fetch_cfo_capex_ttm_finnhub(self, tickers: list[str], api_key: str|None=None) -> pd.DataFrame:
L300         api_key = api_key or os.getenv("FINNHUB_API_KEY")
L301         if not api_key: raise ValueError("Finnhub API key not provided. Set FINNHUB_API_KEY or pass api_key=")
L302         base, s, rows = "https://finnhub.io/api/v1", requests.Session(), []
L303         for sym in tickers:
L304             cfo_ttm = capex_ttm = None
L305             try:
L306                 j = self._finn_get(s, f"{base}/stock/cash-flow", {"symbol":sym,"frequency":"quarterly","limit":8,"token":api_key})
L307                 arr = j.get("cashFlow") or []; cfo_vals, capex_vals = [], []
L308                 for item in arr[:4]:
L309                     cfo_vals.append(self._first_key(item,self._FINN_CFO_KEYS)); capex_vals.append(self._first_key(item,self._FINN_CAPEX_KEYS))
L310                 if any(v is not None for v in cfo_vals): cfo_ttm = float(np.nansum([np.nan if v is None else float(v) for v in cfo_vals]))
L311                 if any(v is not None for v in capex_vals): capex_ttm = float(np.nansum([np.nan if v is None else float(v) for v in capex_vals]))
L312             except Exception: pass
L313             if cfo_ttm is None or capex_ttm is None:
L314                 try:
L315                     j = self._finn_get(s, f"{base}/stock/cash-flow", {"symbol":sym,"frequency":"annual","limit":1,"token":api_key})
L316                     arr = j.get("cashFlow") or []
L317                     if arr:
L318                         item0 = arr[0]
L319                         if cfo_ttm is None:
L320                             v = self._first_key(item0,self._FINN_CFO_KEYS)
L321                             if v is not None: cfo_ttm = float(v)
L322                         if capex_ttm is None:
L323                             v = self._first_key(item0,self._FINN_CAPEX_KEYS)
L324                             if v is not None: capex_ttm = float(v)
L325                 except Exception: pass
L326             rows.append({"ticker":sym,"cfo_ttm_fh":np.nan if cfo_ttm is None else cfo_ttm,"capex_ttm_fh":np.nan if capex_ttm is None else capex_ttm})
L327         return pd.DataFrame(rows).set_index("ticker")
L328
L329     def compute_fcf_with_fallback(self, tickers: list[str], finnhub_api_key: str|None=None) -> pd.DataFrame:
L330         yf_df = self.fetch_cfo_capex_ttm_yf(tickers)
L331         T.log("financials (yf) done")
L332         miss_mask = yf_df[["cfo_ttm_yf","capex_ttm_yf","fcf_ttm_yf_direct"]].isna().any(axis=1)
L333         need = yf_df.index[miss_mask].tolist(); print(f"[T] yf financials missing: {len(need)} {need[:10]}{'...' if len(need)>10 else ''}")
L334         if need:
L335             fh_df = self.fetch_cfo_capex_ttm_finnhub(need, api_key=finnhub_api_key)
L336             df = yf_df.join(fh_df, how="left")
L337             for col_yf, col_fh in [("cfo_ttm_yf","cfo_ttm_fh"),("capex_ttm_yf","capex_ttm_fh")]:
L338                 df[col_yf] = df[col_yf].fillna(df[col_fh])
L339             print("[T] financials (finnhub) done (fallback only)")
L340         else:
L341             df = yf_df.assign(cfo_ttm_fh=np.nan, capex_ttm_fh=np.nan)
L342             print("[T] financials (finnhub) skipped (no missing)")
L343         df["cfo_ttm"]  = df["cfo_ttm_yf"].where(df["cfo_ttm_yf"].notna(), df["cfo_ttm_fh"])
L344         df["capex_ttm"] = df["capex_ttm_yf"].where(df["capex_ttm_yf"].notna(), df["capex_ttm_fh"])
L345         cfo, capex = pd.to_numeric(df["cfo_ttm"], errors="coerce"), pd.to_numeric(df["capex_ttm"], errors="coerce").abs()
L346         fcf_calc = cfo - capex
L347         fcf_direct = pd.to_numeric(df.get("fcf_ttm_yf_direct"), errors="coerce")
L348         df["fcf_ttm"] = fcf_calc.where(fcf_calc.notna(), fcf_direct)
L349         df["cfo_source"]  = np.where(df["cfo_ttm_yf"].notna(),"yfinance",np.where(df["cfo_ttm_fh"].notna(),"finnhub",""))
L350         df["capex_source"] = np.where(df["capex_ttm_yf"].notna(),"yfinance",np.where(df["capex_ttm_fh"].notna(),"finnhub",""))
L351         df["fcf_imputed"] = df[["cfo_ttm","capex_ttm"]].isna().any(axis=1) & df["fcf_ttm"].notna()
L352         cols = ["cfo_ttm_yf","capex_ttm_yf","cfo_ttm_fh","capex_ttm_fh","cfo_ttm","capex_ttm","fcf_ttm","fcf_ttm_yf_direct","cfo_source","capex_source","fcf_imputed"]
L353         return df[cols].sort_index()
L354
L355     def _build_eps_df(self, tickers, tickers_bulk, info):
L356         eps_rows=[]
L357         for t in tickers:
L358             info_t, eps_ttm, eps_q = info[t], info[t].get("trailingEps", np.nan), np.nan
L359             try:
L360                 qearn, so = tickers_bulk.tickers[t].quarterly_earnings, info_t.get("sharesOutstanding")
L361                 if so and qearn is not None and not qearn.empty and "Earnings" in qearn.columns:
L362                     eps_ttm_q = qearn["Earnings"].head(4).sum()/so
L363                     if pd.notna(eps_ttm_q) and (pd.isna(eps_ttm) or (abs(eps_ttm)>0 and abs(eps_ttm/eps_ttm_q)>3)): eps_ttm = eps_ttm_q
L364                     eps_q = qearn["Earnings"].iloc[-1]/so
L365             except Exception: pass
L366             eps_rows.append({"ticker":t,"eps_ttm":eps_ttm,"eps_q_recent":eps_q})
L367         return self.impute_eps_ttm(pd.DataFrame(eps_rows).set_index("ticker"))
L368
L369     def prepare_data(self):
L370         """Fetch price and fundamental data for all tickers."""
L371         cand_info = yf.Tickers(" ".join(self.cand)); cand_prices = {}
L372         for t in self.cand:
L373             try: cand_prices[t] = cand_info.tickers[t].fast_info.get("lastPrice", np.inf)
L374             except Exception as e: print(f"{t}: price fetch failed ({e})"); cand_prices[t] = np.inf
L375         cand_f = [t for t,p in cand_prices.items() if p<=self.price_max]
L376         T.log("price cap filter done (CAND_PRICE_MAX)")
L377         tickers = sorted(set(self.exist + cand_f))
L378         T.log(f"universe prepared: unique={len(tickers)} bench={self.bench}")
L379         data = yf.download(tickers + [self.bench], period="600d", auto_adjust=True, progress=False)
L380         T.log("yf.download done")
L381         px, spx = data["Close"], data["Close"][self.bench]
L382         clip_days = int(os.getenv("PRICE_CLIP_DAYS", "0"))   # 0なら無効（既定）
L383         if clip_days > 0:
L384             px  = px.tail(clip_days + 1)
L385             spx = spx.tail(clip_days + 1)
L386             print(f"[T] price window clipped by env: {len(px)} rows (PRICE_CLIP_DAYS={clip_days})")
L387         else:
L388             print(f"[T] price window clip skipped; rows={len(px)}")
L389         tickers_bulk, info = yf.Tickers(" ".join(tickers)), {}
L390         for t in tickers:
L391             try: info[t] = tickers_bulk.tickers[t].info
L392             except Exception as e: print(f"{t}: info fetch failed ({e})"); info[t] = {}
L393         eps_df = self._build_eps_df(tickers, tickers_bulk, info)
L394         fcf_df = self.compute_fcf_with_fallback(tickers, finnhub_api_key=self.api_key)
L395         T.log("eps/fcf prep done")
L396         returns = px[tickers].pct_change()
L397         T.log("price prep/returns done")
L398         return dict(cand=cand_f, tickers=tickers, data=data, px=px, spx=spx, tickers_bulk=tickers_bulk, info=info, eps_df=eps_df, fcf_df=fcf_df, returns=returns)
L399
L400 # === Selector：相関低減・選定（スコア＆リターンだけ読む） ===
L401 class Selector:
L402     # ---- DRRS helpers（Selector専用） ----
L403     @staticmethod
L404     def _z_np(X: np.ndarray) -> np.ndarray:
L405         X = np.asarray(X, dtype=np.float32); m = np.nanmean(X, axis=0, keepdims=True); s = np.nanstd(X, axis=0, keepdims=True)+1e-9
L406         return (np.nan_to_num(X)-m)/s
L407
L408     @classmethod
L409     def residual_corr(cls, R: np.ndarray, n_pc: int=3, shrink: float=0.1) -> np.ndarray:
L410         Z = cls._z_np(R); U,S,_ = np.linalg.svd(Z, full_matrices=False); F = U[:,:n_pc]*S[:n_pc]; B = np.linalg.lstsq(F, Z, rcond=None)[0]
L411         E = Z - F@B; C = np.corrcoef(E, rowvar=False)
L412         off = C - np.diag(np.diag(C)); iu = np.triu_indices_from(off,1); avg_abs = np.nanmean(np.abs(off[iu])) if iu[0].size else 0.0
L413         shrink_eff = float(np.clip(shrink + 0.5*avg_abs, 0.1, 0.6)); N = C.shape[0]
L414         return (1.0 - shrink_eff)*C + shrink_eff*np.eye(N, dtype=C.dtype)
L415
L416     @classmethod
L417     def rrqr_like_det(cls, R: np.ndarray, score: np.ndarray, k: int, gamma: float=1.0):
L418         Z, w = cls._z_np(R), (score-score.min())/(np.ptp(score)+1e-12); X = Z*(1.0 + gamma*w)
L419         N, k = X.shape[1], max(0, min(k, X.shape[1]))
L420         if k==0: return []
L421         S, selected, Rres = [], np.zeros(N, dtype=bool), X.copy()
L422         for _ in range(k):
L42
```