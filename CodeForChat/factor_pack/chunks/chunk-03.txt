```text
@staticmethod
L436     def _obj_with_cross(C_within: np.ndarray, C_cross: np.ndarray|None, score: np.ndarray, idx, lam: float, mu: float) -> float:
L437         idx = list(idx); P = C_within[np.ix_(idx, idx)]; s = (score-score.mean())/(score.std()+1e-9)
L438         within = (P.sum()-np.trace(P))/2.0; cross = 0.0
L439         if C_cross is not None and C_cross.size>0: cross = C_cross[idx,:].sum()
L440         return float(s[idx].sum() - lam*within - mu*cross)
L441
L442     @classmethod
L443     def swap_local_det_cross(cls, C_within: np.ndarray, C_cross: np.ndarray|None, score: np.ndarray, idx, lam: float=0.6, mu: float=0.3, max_pass: int=15):
L444         S, best, improved, passes, N = sorted(idx), cls._obj_with_cross(C_within,C_cross,score,idx,lam,mu), True, 0, len(score)
L445         while improved and passes<max_pass:
L446             improved, passes = False, passes+1
L447             for i,out in enumerate(list(S)):
L448                 for inn in range(N):
L449                     if inn in S: continue
L450                     cand = sorted(S[:i]+[inn]+S[i+1:]); v = cls._obj_with_cross(C_within,C_cross,score,cand,lam,mu)
L451                     if v>best+1e-10: S, best, improved = cand, v, True; break
L452                 if improved: break
L453         return S, best
L454
L455     @staticmethod
L456     def avg_corr(C: np.ndarray, idx) -> float:
L457         k = len(idx); P = C[np.ix_(idx, idx)]
L458         return float((P.sum()-np.trace(P))/(k*(k-1)+1e-12))
L459
L460     @classmethod
L461     def select_bucket_drrs(cls, returns_df: pd.DataFrame, score_ser: pd.Series, pool_tickers: list[str], k: int, *, n_pc: int, gamma: float, lam: float, lookback: int, shrink: float=0.10, g_fixed_tickers: list[str]|None=None, mu: float=0.0):
L462         g_fixed = [t for t in (g_fixed_tickers or []) if t in returns_df.columns]
L463         union = [t for t in pool_tickers if t in returns_df.columns]
L464         for t in g_fixed:
L465             if t not in union: union.append(t)
L466         Rdf_all = returns_df[union]; Rdf_all = Rdf_all.iloc[-lookback:] if len(Rdf_all)>=lookback else Rdf_all; Rdf_all = Rdf_all.dropna()
L467         pool_eff, g_eff = [t for t in pool_tickers if t in Rdf_all.columns], [t for t in g_fixed if t in Rdf_all.columns]
L468         if len(pool_eff)==0: return dict(idx=[], tickers=[], avg_res_corr=np.nan, sum_score=0.0, objective=-np.inf)
L469         score = score_ser.reindex(pool_eff).to_numpy(dtype=np.float32)
L470         C_all = cls.residual_corr(Rdf_all.to_numpy(), n_pc=n_pc, shrink=shrink)
L471         col_pos = {c:i for i,c in enumerate(Rdf_all.columns)}; pool_pos = [col_pos[t] for t in pool_eff]
L472         C_within, C_cross = C_all[np.ix_(pool_pos,pool_pos)], None
L473         if len(g_eff)>0 and mu>0.0:
L474             g_pos = [col_pos[t] for t in g_eff]; C_cross = C_all[np.ix_(pool_pos,g_pos)]
L475         R_pool = Rdf_all[pool_eff].to_numpy(); S0 = cls.rrqr_like_det(R_pool, score, k, gamma=gamma)
L476         S, Jn = (cls.swap_local_det_cross(C_within, C_cross, score, S0, lam=lam, mu=mu, max_pass=15) if C_cross is not None else cls.swap_local_det(C_within, score, S0, lam=lam, max_pass=15))
L477         selected_tickers = [pool_eff[i] for i in S]
L478         return dict(idx=S, tickers=selected_tickers, avg_res_corr=cls.avg_corr(C_within,S), sum_score=float(score[S].sum()), objective=float(Jn))
L479
L480     # ---- 選定（スコア Series / returns だけを受ける）----
L481 # === Output：出力整形と送信（表示・Slack） ===
L482 class Output:
L483
L484     def __init__(self, debug=False):
L485         self.debug = debug
L486         self.miss_df = self.g_table = self.d_table = self.io_table = self.df_metrics_fmt = self.debug_table = None
L487         self.g_title = self.d_title = ""
L488         self.g_formatters = self.d_formatters = {}
L489         # 低スコア（GSC+DSC）Top10 表示/送信用
L490         self.low10_table = None
L491
L492     # --- 表示（元 display_results のロジックそのまま） ---
L493     def display_results(self, *, exist, bench, df_z, g_score, d_score_all,
L494                         init_G, init_D, top_G, top_D, **kwargs):
L495         pd.set_option('display.float_format','{:.3f}'.format)
L496         print("📈 ファクター分散最適化の結果")
L497         if self.miss_df is not None and not self.miss_df.empty:
L498             print("Missing Data:")
L499             print(self.miss_df.to_string(index=False))
L500
L501         # ---- 表示用：Changes/Near-Miss のスコア源を“最終集計”に統一するプロキシ ----
L502         try:
L503             sc = getattr(self, "_sc", None)
L504             agg_G = getattr(sc, "_agg_G", None)
L505             agg_D = getattr(sc, "_agg_D", None)
L506         except Exception:
L507             sc = agg_G = agg_D = None
L508         class _SeriesProxy:
L509             __slots__ = ("primary", "fallback")
L510             def __init__(self, primary, fallback): self.primary, self.fallback = primary, fallback
L511             def get(self, key, default=None):
L512                 try:
L513                     v = self.primary.get(key) if hasattr(self.primary, "get") else None
L514                     if v is not None and not (isinstance(v, float) and v != v):
L515                         return v
L516                 except Exception:
L517                     pass
L518                 try:
L519                     return self.fallback.get(key) if hasattr(self.fallback, "get") else default
L520                 except Exception:
L521                     return default
L522         g_score = _SeriesProxy(agg_G, g_score)
L523         d_score_all = _SeriesProxy(agg_D, d_score_all)
L524         near_G = getattr(sc, "_near_G", []) if sc else []
L525         near_D = getattr(sc, "_near_D", []) if sc else []
L526
L527         extra_G = [t for t in init_G if t not in top_G][:5]; G_UNI = top_G + extra_G
L528         gsc_series = pd.Series({t: g_score.get(t) for t in G_UNI}, name='GSC')
L529         self.g_table = pd.concat([df_z.loc[G_UNI,['GRW','MOM','TRD','VOL']], gsc_series], axis=1)
L530         self.g_table.index = [t + ("⭐️" if t in top_G else "") for t in G_UNI]
L531         self.g_formatters = {col:"{:.2f}".format for col in ['GRW','MOM','TRD','VOL']}; self.g_formatters['GSC'] = "{:.3f}".format
L532         self.g_title = (f"[G枠 / {N_G} / {_fmt_w(g_weights)} / corrM={corrM} / "
L533                         f"LB={DRRS_G['lookback']} nPC={DRRS_G['n_pc']} γ={DRRS_G['gamma']} λ={DRRS_G['lam']} η={DRRS_G['eta']} shrink={DRRS_SHRINK}]")
L534         if near_G:
L535             add = [t for t in near_G if t not in set(G_UNI)][:10]
L536             if len(add) < 10:
L537                 try:
L538                     aggG = getattr(sc, "_agg_G", pd.Series(dtype=float)).sort_values(ascending=False)
L539                     out_now = sorted(set(exist) - set(top_G + top_D))  # 今回 OUT
L540                     used = set(G_UNI + add)
L541                     def _push(lst):
L542                         nonlocal add, used
L543                         for t in lst:
L544                             if len(add) == 10: break
L545                             if t in aggG.index and t not in used:
L546                                 add.append(t); used.add(t)
L547                     _push(out_now)           # ① 今回 OUT を優先
L548                     _push(list(aggG.index))  # ② まだ足りなければ上位で充填
L549                 except Exception:
L550                     pass
L551             if add:
L552                 near_tbl = pd.concat([df_z.loc[add,['GRW','MOM','TRD','VOL']], pd.Series({t: g_score.get(t) for t in add}, name='GSC')], axis=1)
L553                 self.g_table = pd.concat([self.g_table, near_tbl], axis=0)
L554         print(self.g_title); print(self.g_table.to_string(formatters=self.g_formatters))
L555
L556         extra_D = [t for t in init_D if t not in top_D][:5]; D_UNI = top_D + extra_D
L557         cols_D = ['QAL','YLD','VOL','TRD']; d_disp = pd.DataFrame(index=D_UNI)
L558         d_disp['QAL'], d_disp['YLD'], d_disp['VOL'], d_disp['TRD'] = df_z.loc[D_UNI,'D_QAL'], df_z.loc[D_UNI,'D_YLD'], df_z.loc[D_UNI,'D_VOL_RAW'], df_z.loc[D_UNI,'D_TRD']
L559         dsc_series = pd.Series({t: d_score_all.get(t) for t in D_UNI}, name='DSC')
L560         self.d_table = pd.concat([d_disp, dsc_series], axis=1); self.d_table.index = [t + ("⭐️" if t in top_D else "") for t in D_UNI]
L561         self.d_formatters = {col:"{:.2f}".format for col in cols_D}; self.d_formatters['DSC']="{:.3f}".format
L562         import scorer
L563         dw_eff = scorer.D_WEIGHTS_EFF
L564         self.d_title = (f"[D枠 / {N_D} / {_fmt_w(dw_eff)} / corrM={corrM} / "
L565                         f"LB={DRRS_D['lookback']} nPC={DRRS_D['n_pc']} γ={DRRS_D['gamma']} λ={DRRS_D['lam']} μ={CROSS_MU_GD} η={DRRS_D['eta']} shrink={DRRS_SHRINK}]")
L566         if near_D:
L567             add = [t for t in near_D if t not in set(D_UNI)][:10]
L568             if add:
L569                 d_disp2 = pd.DataFrame(index=add)
L570                 d_disp2['QAL'], d_disp2['YLD'], d_disp2['VOL'], d_disp2['TRD'] = df_z.loc[add,'D_QAL'], df_z.loc[add,'D_YLD'], df_z.loc[add,'D_VOL_RAW'], df_z.loc[add,'D_TRD']
L571                 near_tbl = pd.concat([d_disp2, pd.Series({t: d_score_all.get(t) for t in add}, name='DSC')], axis=1)
L572                 self.d_table = pd.concat([self.d_table, near_tbl], axis=0)
L573         print(self.d_title); print(self.d_table.to_string(formatters=self.d_formatters))
L574
L575         # === Changes（IN の GSC/DSC を表示。OUT は銘柄名のみ） ===
L576         in_list = sorted(set(list(top_G)+list(top_D)) - set(exist))
L577         out_list = sorted(set(exist) - set(list(top_G)+list(top_D)))
L578
L579         self.io_table = pd.DataFrame({
L580             'IN': pd.Series(in_list),
L581             '/ OUT': pd.Series(out_list)
L582         })
L583         g_list = [f"{g_score.get(t):.3f}" if pd.notna(g_score.get(t)) else '—' for t in out_list]
L584         d_list = [f"{d_score_all.get(t):.3f}" if pd.notna(d_score_all.get(t)) else '—' for t in out_list]
L585         self.io_table['GSC'] = pd.Series(g_list)
L586         self.io_table['DSC'] = pd.Series(d_list)
L587
L588         print("Changes:")
L589         print(self.io_table.to_string(index=False))
L590
L591         all_tickers = list(set(exist + list(top_G) + list(top_D) + [bench])); prices = yf.download(all_tickers, period='1y', auto_adjust=True, progress=False)['Close']
L592         ret = prices.pct_change(); portfolios = {'CUR':exist,'NEW':list(top_G)+list(top_D)}; metrics={}
L593         for name,ticks in portfolios.items():
L594             pr = ret[ticks].mean(axis=1, skipna=True).dropna(); cum = (1+pr).cumprod()-1; n = len(pr)
L595             if n>=252: ann_ret, ann_vol = (1+cum.iloc[-1])**(252/n)-1, pr.std()*np.sqrt(252)
L596             else: ann_ret, ann_vol = cum.iloc[-1], pr.std()*np.sqrt(n)
L597             sharpe, drawdown = ann_ret/ann_vol, (cum - cum.cummax()).min()
L598             if len(ticks)>=2:
L599                 C_raw = ret[ticks].corr(); RAW_rho = C_raw.mask(np.eye(len(ticks), dtype=bool)).stack().mean()
L600                 R = ret[ticks].dropna().to_numpy(); C_resid = Selector.residual_corr(R, n_pc=3, shrink=DRRS_SHRINK)
L601                 RESID_rho = float((C_resid.sum()-np.trace(C_resid))/(C_resid.shape[0]*(C_resid.shape[0]-1)))
L602             else: RAW_rho = RESID_rho = np.nan
L603             divy = ttm_div_yield_portfolio(ticks); metrics[name] = {'RET':ann_ret,'VOL':ann_vol,'SHP':sharpe,'MDD':drawdown,'RAWρ':RAW_rho,'RESIDρ':RESID_rho,'DIVY':divy}
L604         df_metrics = pd.DataFrame(metrics).T; df_metrics_pct = df_metrics.copy(); self.df_metrics = df_metrics
L605         for col in ['RET','VOL','MDD','DIVY']: df_metrics_pct[col] = df_metrics_pct[col]*100
L606         cols_order = ['RET','VOL','SHP','MDD','RAWρ','RESIDρ','DIVY']; df_metrics_pct = df_metrics_pct.reindex(columns=cols_order)
L607         def _fmt_row(s):
L608             return pd.Series({'RET':f"{s['RET']:.1f}%",'VOL':f"{s['VOL']:.1f}%",'SHP':f"{s['SHP']:.1f}",'MDD':f"{s['MDD']:.1f}%",'RAWρ':(f"{s['RAW
```