```text
rows).set_index("ticker"))
L455
L456     def prepare_data(self):
L457         """Fetch price and fundamental data for all tickers."""
L458         cand_info = yf.Tickers(" ".join(self.cand)); cand_prices = {}
L459         for t in self.cand:
L460             try: cand_prices[t] = cand_info.tickers[t].fast_info.get("lastPrice", np.inf)
L461             except Exception as e: print(f"{t}: price fetch failed ({e})"); cand_prices[t] = np.inf
L462         cand_f = [t for t,p in cand_prices.items() if p<=self.price_max]
L463         T.log("price cap filter done (CAND_PRICE_MAX)")
L464         tickers = sorted(set(self.exist + cand_f))
L465         T.log(f"universe prepared: unique={len(tickers)} bench={self.bench}")
L466         data = yf.download(tickers + [self.bench], period="600d",
L467                            auto_adjust=True, progress=False, threads=False)
L468         T.log("yf.download done")
L469         px = data["Close"].dropna(how="all", axis=1).ffill(limit=2)
L470         spx = data["Close"][self.bench].reindex(px.index).ffill()
L471         clip_days = int(os.getenv("PRICE_CLIP_DAYS", "0"))   # 0なら無効（既定）
L472         if clip_days > 0:
L473             px  = px.tail(clip_days + 1)
L474             spx = spx.tail(clip_days + 1)
L475             print(f"[T] price window clipped by env: {len(px)} rows (PRICE_CLIP_DAYS={clip_days})")
L476         else:
L477             print(f"[T] price window clip skipped; rows={len(px)}")
L478         tickers_bulk, info = yf.Tickers(" ".join(tickers)), {}
L479         for t in tickers:
L480             try: info[t] = tickers_bulk.tickers[t].info
L481             except Exception as e: print(f"{t}: info fetch failed ({e})"); info[t] = {}
L482         eps_df = self._build_eps_df(tickers, tickers_bulk, info)
L483         fcf_df = self.compute_fcf_with_fallback(tickers, finnhub_api_key=self.api_key)
L484         T.log("eps/fcf prep done")
L485         returns = px[tickers].pct_change()
L486         T.log("price prep/returns done")
L487         return dict(cand=cand_f, tickers=tickers, data=data, px=px, spx=spx, tickers_bulk=tickers_bulk, info=info, eps_df=eps_df, fcf_df=fcf_df, returns=returns)
L488
L489 # === Selector：相関低減・選定（スコア＆リターンだけ読む） ===
L490 class Selector:
L491     # ---- DRRS helpers（Selector専用） ----
L492     @staticmethod
L493     def _z_np(X: np.ndarray) -> np.ndarray:
L494         X = np.asarray(X, dtype=np.float32); m = np.nanmean(X, axis=0, keepdims=True); s = np.nanstd(X, axis=0, keepdims=True)+1e-9
L495         return (np.nan_to_num(X)-m)/s
L496
L497     @classmethod
L498     def residual_corr(cls, R: np.ndarray, n_pc: int=3, shrink: float=0.1) -> np.ndarray:
L499         Z = cls._z_np(R); U,S,_ = np.linalg.svd(Z, full_matrices=False); F = U[:,:n_pc]*S[:n_pc]; B = np.linalg.lstsq(F, Z, rcond=None)[0]
L500         E = Z - F@B; C = np.corrcoef(E, rowvar=False)
L501         off = C - np.diag(np.diag(C)); iu = np.triu_indices_from(off,1); avg_abs = np.nanmean(np.abs(off[iu])) if iu[0].size else 0.0
L502         shrink_eff = float(np.clip(shrink + 0.5*avg_abs, 0.1, 0.6)); N = C.shape[0]
L503         return (1.0 - shrink_eff)*C + shrink_eff*np.eye(N, dtype=C.dtype)
L504
L505     @classmethod
L506     def rrqr_like_det(cls, R: np.ndarray, score: np.ndarray, k: int, gamma: float=1.0):
L507         Z, w = cls._z_np(R), (score-score.min())/(np.ptp(score)+1e-12); X = Z*(1.0 + gamma*w)
L508         N, k = X.shape[1], max(0, min(k, X.shape[1]))
L509         if k==0: return []
L510         S, selected, Rres = [], np.zeros(N, dtype=bool), X.copy()
L511         for _ in range(k):
L512             norms = (Rres*Rres).sum(axis=0); cand = np.where(~selected)[0]
L513             j = sorted(cand, key=lambda c:(-norms[c], -w[c], c))[0]
L514             S.append(j); selected[j]=True; u = X[:,j:j+1]; u/=(np.linalg.norm(u)+1e-12); Rres = Rres - u @ (u.T @ Rres)
L515         return sorted(S)
L516
L517     @staticmethod
L518     def _obj(corrM: np.ndarray, score: np.ndarray, idx, lam: float) -> float:
L519         idx = list(idx); P = corrM[np.ix_(idx, idx)]; s = (score-score.mean())/(score.std()+1e-9)
L520         return float(s[idx].sum() - lam*((P.sum()-np.trace(P))/2.0))
L521
L522     @classmethod
L523     def swap_local_det(cls, corrM: np.ndarray, score: np.ndarray, idx, lam: float=0.6, max_pass: int=15):
L524         S, best, improved, passes = sorted(idx), cls._obj(corrM, score, idx, lam), True, 0
L525         while improved and passes<max_pass:
L526             improved, passes = False, passes+1
L527             for i,out in enumerate(list(S)):
L528                 for inn in range(len(score)):
L529                     if inn in S: continue
L530                     cand = sorted(S[:i]+[inn]+S[i+1:]); v = cls._obj(corrM, score, cand, lam)
L531                     if v>best+1e-10: S, best, improved = cand, v, True; break
L532                 if improved: break
L533         return S, best
L534
L535     @staticmethod
L536     def _obj_with_cross(C_within: np.ndarray, C_cross: np.ndarray|None, score: np.ndarray, idx, lam: float, mu: float) -> float:
L537         idx = list(idx); P = C_within[np.ix_(idx, idx)]; s = (score-score.mean())/(score.std()+1e-9)
L538         within = (P.sum()-np.trace(P))/2.0; cross = 0.0
L539         if C_cross is not None and C_cross.size>0: cross = C_cross[idx,:].sum()
L540         return float(s[idx].sum() - lam*within - mu*cross)
L541
L542     @classmethod
L543     def swap_local_det_cross(cls, C_within: np.ndarray, C_cross: np.ndarray|None, score: np.ndarray, idx, lam: float=0.6, mu: float=0.3, max_pass: int=15):
L544         S, best, improved, passes, N = sorted(idx), cls._obj_with_cross(C_within,C_cross,score,idx,lam,mu), True, 0, len(score)
L545         while improved and passes<max_pass:
L546             improved, passes = False, passes+1
L547             for i,out in enumerate(list(S)):
L548                 for inn in range(N):
L549                     if inn in S: continue
L550                     cand = sorted(S[:i]+[inn]+S[i+1:]); v = cls._obj_with_cross(C_within,C_cross,score,cand,lam,mu)
L551                     if v>best+1e-10: S, best, improved = cand, v, True; break
L552                 if improved: break
L553         return S, best
L554
L555     @staticmethod
L556     def avg_corr(C: np.ndarray, idx) -> float:
L557         k = len(idx); P = C[np.ix_(idx, idx)]
L558         return float((P.sum()-np.trace(P))/(k*(k-1)+1e-12))
L559
L560     @classmethod
L561     def select_bucket_drrs(cls, returns_df: pd.DataFrame, score_ser: pd.Series, pool_tickers: list[str], k: int, *, n_pc: int, gamma: float, lam: float, lookback: int, shrink: float=0.10, g_fixed_tickers: list[str]|None=None, mu: float=0.0):
L562         g_fixed = [t for t in (g_fixed_tickers or []) if t in returns_df.columns]
L563         union = [t for t in pool_tickers if t in returns_df.columns]
L564         for t in g_fixed:
L565             if t not in union: union.append(t)
L566         Rdf_all = returns_df[union]; Rdf_all = Rdf_all.iloc[-lookback:] if len(Rdf_all)>=lookback else Rdf_all; Rdf_all = Rdf_all.dropna()
L567         pool_eff, g_eff = [t for t in pool_tickers if t in Rdf_all.columns], [t for t in g_fixed if t in Rdf_all.columns]
L568         if len(pool_eff)==0: return dict(idx=[], tickers=[], avg_res_corr=np.nan, sum_score=0.0, objective=-np.inf)
L569         score = score_ser.reindex(pool_eff).to_numpy(dtype=np.float32)
L570         C_all = cls.residual_corr(Rdf_all.to_numpy(), n_pc=n_pc, shrink=shrink)
L571         col_pos = {c:i for i,c in enumerate(Rdf_all.columns)}; pool_pos = [col_pos[t] for t in pool_eff]
L572         C_within, C_cross = C_all[np.ix_(pool_pos,pool_pos)], None
L573         if len(g_eff)>0 and mu>0.0:
L574             g_pos = [col_pos[t] for t in g_eff]; C_cross = C_all[np.ix_(pool_pos,g_pos)]
L575         R_pool = Rdf_all[pool_eff].to_numpy(); S0 = cls.rrqr_like_det(R_pool, score, k, gamma=gamma)
L576         S, Jn = (cls.swap_local_det_cross(C_within, C_cross, score, S0, lam=lam, mu=mu, max_pass=15) if C_cross is not None else cls.swap_local_det(C_within, score, S0, lam=lam, max_pass=15))
L577         selected_tickers = [pool_eff[i] for i in S]
L578         return dict(idx=S, tickers=selected_tickers, avg_res_corr=cls.avg_corr(C_within,S), sum_score=float(score[S].sum()), objective=float(Jn))
L579
L580     # ---- 選定（スコア Series / returns だけを受ける）----
L581 # === Output：出力整形と送信（表示・Slack） ===
L582 class Output:
L583
L584     def __init__(self, debug=None):
L585         # self.debug は使わない（互換のため引数は受けるが無視）
L586         self.miss_df = self.g_table = self.d_table = self.io_table = self.df_metrics_fmt = self.debug_table = None
L587         self.g_title = self.d_title = ""
L588         self.g_formatters = self.d_formatters = {}
L589         # 低スコア（GSC+DSC）Top10 表示/送信用
L590         self.low10_table = None
L591         self.debug_text = ""   # デバッグ用本文はここに一本化
L592
L593     # --- 表示（元 display_results のロジックそのまま） ---
L594     def display_results(self, *, exist, bench, df_z, g_score, d_score_all,
L595                         init_G, init_D, top_G, top_D, **kwargs):
L596         pd.set_option('display.float_format','{:.3f}'.format)
L597         print("📈 ファクター分散最適化の結果")
L598         if self.miss_df is not None and not self.miss_df.empty:
L599             print("Missing Data:")
L600             print(self.miss_df.to_string(index=False))
L601
L602         # ---- 表示用：Changes/Near-Miss のスコア源を“最終集計”に統一するプロキシ ----
L603         try:
L604             sc = getattr(self, "_sc", None)
L605             agg_G = getattr(sc, "_agg_G", None)
L606             agg_D = getattr(sc, "_agg_D", None)
L607         except Exception:
L608             sc = agg_G = agg_D = None
L609         class _SeriesProxy:
L610             __slots__ = ("primary", "fallback")
L611             def __init__(self, primary, fallback): self.primary, self.fallback = primary, fallback
L612             def get(self, key, default=None):
L613                 try:
L614                     v = self.primary.get(key) if hasattr(self.primary, "get") else None
L615                     if v is not None and not (isinstance(v, float) and v != v):
L616                         return v
L617                 except Exception:
L618                     pass
L619                 try:
L620                     return self.fallback.get(key) if hasattr(self.fallback, "get") else default
L621                 except Exception:
L622                     return default
L623         g_score = _SeriesProxy(agg_G, g_score)
L624         d_score_all = _SeriesProxy(agg_D, d_score_all)
L625         near_G = getattr(sc, "_near_G", []) if sc else []
L626         near_D = getattr(sc, "_near_D", []) if sc else []
L627
L628         extra_G = [t for t in init_G if t not in top_G][:5]; G_UNI = top_G + extra_G
L629         gsc_series = pd.Series({t: g_score.get(t) for t in G_UNI}, name='GSC')
L630         self.g_table = pd.concat([df_z.loc[G_UNI,['GROWTH_F','MOM','TRD','VOL']], gsc_series], axis=1)
L631         self.g_table.index = [t + ("⭐️" if t in top_G else "") for t in G_UNI]
L632         self.g_formatters = {col:"{:.2f}".format for col in ['GROWTH_F','MOM','TRD','VOL']}; self.g_formatters['GSC'] = "{:.3f}".format
L633         self.g_title = (f"[G枠 / {N_G} / {_fmt_w(g_weights)} / corrM={corrM} / "
L634                         f"LB={DRRS_G['lookback']} nPC={DRRS_G['n_pc']} γ={DRRS_G['gamma']} λ={DRRS_G['lam']} η={DRRS_G['eta']} shrink={DRRS_SHRINK}]")
L635         if near_G:
L636             add = [t for t in near_G if t not in set(G_UNI)][:10]
L637             if len(add) < 10:
L638                 try:
L639                     aggG = getattr(sc, "_agg_G", pd.Series(dtype=float)).sort_values(ascending=False)
L640                     out_now = sorted(set(exist) - set(top_G + top_D))  # 今回 OUT
L641                     used = set(G_UNI + add)
L642                     def _push(lst):
L643                         nonlocal add, used
L644                         for t in lst:
L645                             if len
```