```text
   shrink_eff = float(np.clip(shrink + 0.5*avg_abs, 0.1, 0.6)); N = C.shape[0]
L431         return (1.0 - shrink_eff)*C + shrink_eff*np.eye(N, dtype=C.dtype)
L432
L433     @classmethod
L434     def rrqr_like_det(cls, R: np.ndarray, score: np.ndarray, k: int, gamma: float=1.0):
L435         Z, w = cls._z_np(R), (score-score.min())/(np.ptp(score)+1e-12); X = Z*(1.0 + gamma*w)
L436         N, k = X.shape[1], max(0, min(k, X.shape[1]))
L437         if k==0: return []
L438         S, selected, Rres = [], np.zeros(N, dtype=bool), X.copy()
L439         for _ in range(k):
L440             norms = (Rres*Rres).sum(axis=0); cand = np.where(~selected)[0]
L441             j = sorted(cand, key=lambda c:(-norms[c], -w[c], c))[0]
L442             S.append(j); selected[j]=True; u = X[:,j:j+1]; u/=(np.linalg.norm(u)+1e-12); Rres = Rres - u @ (u.T @ Rres)
L443         return sorted(S)
L444
L445     @staticmethod
L446     def _obj(corrM: np.ndarray, score: np.ndarray, idx, lam: float) -> float:
L447         idx = list(idx); P = corrM[np.ix_(idx, idx)]; s = (score-score.mean())/(score.std()+1e-9)
L448         return float(s[idx].sum() - lam*((P.sum()-np.trace(P))/2.0))
L449
L450     @classmethod
L451     def swap_local_det(cls, corrM: np.ndarray, score: np.ndarray, idx, lam: float=0.6, max_pass: int=15):
L452         S, best, improved, passes = sorted(idx), cls._obj(corrM, score, idx, lam), True, 0
L453         while improved and passes<max_pass:
L454             improved, passes = False, passes+1
L455             for i,out in enumerate(list(S)):
L456                 for inn in range(len(score)):
L457                     if inn in S: continue
L458                     cand = sorted(S[:i]+[inn]+S[i+1:]); v = cls._obj(corrM, score, cand, lam)
L459                     if v>best+1e-10: S, best, improved = cand, v, True; break
L460                 if improved: break
L461         return S, best
L462
L463     @staticmethod
L464     def _obj_with_cross(C_within: np.ndarray, C_cross: np.ndarray|None, score: np.ndarray, idx, lam: float, mu: float) -> float:
L465         idx = list(idx); P = C_within[np.ix_(idx, idx)]; s = (score-score.mean())/(score.std()+1e-9)
L466         within = (P.sum()-np.trace(P))/2.0; cross = 0.0
L467         if C_cross is not None and C_cross.size>0: cross = C_cross[idx,:].sum()
L468         return float(s[idx].sum() - lam*within - mu*cross)
L469
L470     @classmethod
L471     def swap_local_det_cross(cls, C_within: np.ndarray, C_cross: np.ndarray|None, score: np.ndarray, idx, lam: float=0.6, mu: float=0.3, max_pass: int=15):
L472         S, best, improved, passes, N = sorted(idx), cls._obj_with_cross(C_within,C_cross,score,idx,lam,mu), True, 0, len(score)
L473         while improved and passes<max_pass:
L474             improved, passes = False, passes+1
L475             for i,out in enumerate(list(S)):
L476                 for inn in range(N):
L477                     if inn in S: continue
L478                     cand = sorted(S[:i]+[inn]+S[i+1:]); v = cls._obj_with_cross(C_within,C_cross,score,cand,lam,mu)
L479                     if v>best+1e-10: S, best, improved = cand, v, True; break
L480                 if improved: break
L481         return S, best
L482
L483     @staticmethod
L484     def avg_corr(C: np.ndarray, idx) -> float:
L485         k = len(idx); P = C[np.ix_(idx, idx)]
L486         return float((P.sum()-np.trace(P))/(k*(k-1)+1e-12))
L487
L488     @classmethod
L489     def select_bucket_drrs(cls, returns_df: pd.DataFrame, score_ser: pd.Series, pool_tickers: list[str], k: int, *, n_pc: int, gamma: float, lam: float, lookback: int, shrink: float=0.10, g_fixed_tickers: list[str]|None=None, mu: float=0.0):
L490         g_fixed = [t for t in (g_fixed_tickers or []) if t in returns_df.columns]
L491         union = [t for t in pool_tickers if t in returns_df.columns]
L492         for t in g_fixed:
L493             if t not in union: union.append(t)
L494         Rdf_all = returns_df[union]; Rdf_all = Rdf_all.iloc[-lookback:] if len(Rdf_all)>=lookback else Rdf_all; Rdf_all = Rdf_all.dropna()
L495         pool_eff, g_eff = [t for t in pool_tickers if t in Rdf_all.columns], [t for t in g_fixed if t in Rdf_all.columns]
L496         if len(pool_eff)==0: return dict(idx=[], tickers=[], avg_res_corr=np.nan, sum_score=0.0, objective=-np.inf)
L497         score = score_ser.reindex(pool_eff).to_numpy(dtype=np.float32)
L498         C_all = cls.residual_corr(Rdf_all.to_numpy(), n_pc=n_pc, shrink=shrink)
L499         col_pos = {c:i for i,c in enumerate(Rdf_all.columns)}; pool_pos = [col_pos[t] for t in pool_eff]
L500         C_within, C_cross = C_all[np.ix_(pool_pos,pool_pos)], None
L501         if len(g_eff)>0 and mu>0.0:
L502             g_pos = [col_pos[t] for t in g_eff]; C_cross = C_all[np.ix_(pool_pos,g_pos)]
L503         R_pool = Rdf_all[pool_eff].to_numpy(); S0 = cls.rrqr_like_det(R_pool, score, k, gamma=gamma)
L504         S, Jn = (cls.swap_local_det_cross(C_within, C_cross, score, S0, lam=lam, mu=mu, max_pass=15) if C_cross is not None else cls.swap_local_det(C_within, score, S0, lam=lam, max_pass=15))
L505         selected_tickers = [pool_eff[i] for i in S]
L506         return dict(idx=S, tickers=selected_tickers, avg_res_corr=cls.avg_corr(C_within,S), sum_score=float(score[S].sum()), objective=float(Jn))
L507
L508     # ---- 選定（スコア Series / returns だけを受ける）----
L509 # === Output：出力整形と送信（表示・Slack） ===
L510 class Output:
L511
L512     def __init__(self, debug=False):
L513         self.debug = debug
L514         self.miss_df = self.g_table = self.d_table = self.io_table = self.df_metrics_fmt = self.debug_table = None
L515         self.g_title = self.d_title = ""
L516         self.g_formatters = self.d_formatters = {}
L517         # 低スコア（GSC+DSC）Top10 表示/送信用
L518         self.low10_table = None
L519
L520     # --- 表示（元 display_results のロジックそのまま） ---
L521     def display_results(self, *, exist, bench, df_z, g_score, d_score_all,
L522                         init_G, init_D, top_G, top_D, **kwargs):
L523         pd.set_option('display.float_format','{:.3f}'.format)
L524         print("📈 ファクター分散最適化の結果")
L525         if self.miss_df is not None and not self.miss_df.empty:
L526             print("Missing Data:")
L527             print(self.miss_df.to_string(index=False))
L528
L529         # ---- 表示用：Changes/Near-Miss のスコア源を“最終集計”に統一するプロキシ ----
L530         try:
L531             sc = getattr(self, "_sc", None)
L532             agg_G = getattr(sc, "_agg_G", None)
L533             agg_D = getattr(sc, "_agg_D", None)
L534         except Exception:
L535             sc = agg_G = agg_D = None
L536         class _SeriesProxy:
L537             __slots__ = ("primary", "fallback")
L538             def __init__(self, primary, fallback): self.primary, self.fallback = primary, fallback
L539             def get(self, key, default=None):
L540                 try:
L541                     v = self.primary.get(key) if hasattr(self.primary, "get") else None
L542                     if v is not None and not (isinstance(v, float) and v != v):
L543                         return v
L544                 except Exception:
L545                     pass
L546                 try:
L547                     return self.fallback.get(key) if hasattr(self.fallback, "get") else default
L548                 except Exception:
L549                     return default
L550         g_score = _SeriesProxy(agg_G, g_score)
L551         d_score_all = _SeriesProxy(agg_D, d_score_all)
L552         near_G = getattr(sc, "_near_G", []) if sc else []
L553         near_D = getattr(sc, "_near_D", []) if sc else []
L554
L555         extra_G = [t for t in init_G if t not in top_G][:5]; G_UNI = top_G + extra_G
L556         gsc_series = pd.Series({t: g_score.get(t) for t in G_UNI}, name='GSC')
L557         self.g_table = pd.concat([df_z.loc[G_UNI,['GROWTH_F','MOM','TRD','VOL']], gsc_series], axis=1)
L558         self.g_table.index = [t + ("⭐️" if t in top_G else "") for t in G_UNI]
L559         self.g_formatters = {col:"{:.2f}".format for col in ['GROWTH_F','MOM','TRD','VOL']}; self.g_formatters['GSC'] = "{:.3f}".format
L560         self.g_title = (f"[G枠 / {N_G} / {_fmt_w(g_weights)} / corrM={corrM} / "
L561                         f"LB={DRRS_G['lookback']} nPC={DRRS_G['n_pc']} γ={DRRS_G['gamma']} λ={DRRS_G['lam']} η={DRRS_G['eta']} shrink={DRRS_SHRINK}]")
L562         if near_G:
L563             add = [t for t in near_G if t not in set(G_UNI)][:10]
L564             if len(add) < 10:
L565                 try:
L566                     aggG = getattr(sc, "_agg_G", pd.Series(dtype=float)).sort_values(ascending=False)
L567                     out_now = sorted(set(exist) - set(top_G + top_D))  # 今回 OUT
L568                     used = set(G_UNI + add)
L569                     def _push(lst):
L570                         nonlocal add, used
L571                         for t in lst:
L572                             if len(add) == 10: break
L573                             if t in aggG.index and t not in used:
L574                                 add.append(t); used.add(t)
L575                     _push(out_now)           # ① 今回 OUT を優先
L576                     _push(list(aggG.index))  # ② まだ足りなければ上位で充填
L577                 except Exception:
L578                     pass
L579             if add:
L580                 near_tbl = pd.concat([df_z.loc[add,['GROWTH_F','MOM','TRD','VOL']], pd.Series({t: g_score.get(t) for t in add}, name='GSC')], axis=1)
L581                 self.g_table = pd.concat([self.g_table, near_tbl], axis=0)
L582         print(self.g_title); print(self.g_table.to_string(formatters=self.g_formatters))
L583
L584         extra_D = [t for t in init_D if t not in top_D][:5]; D_UNI = top_D + extra_D
L585         cols_D = ['QAL','YLD','VOL','TRD']; d_disp = pd.DataFrame(index=D_UNI)
L586         d_disp['QAL'], d_disp['YLD'], d_disp['VOL'], d_disp['TRD'] = df_z.loc[D_UNI,'D_QAL'], df_z.loc[D_UNI,'D_YLD'], df_z.loc[D_UNI,'D_VOL_RAW'], df_z.loc[D_UNI,'D_TRD']
L587         dsc_series = pd.Series({t: d_score_all.get(t) for t in D_UNI}, name='DSC')
L588         self.d_table = pd.concat([d_disp, dsc_series], axis=1); self.d_table.index = [t + ("⭐️" if t in top_D else "") for t in D_UNI]
L589         self.d_formatters = {col:"{:.2f}".format for col in cols_D}; self.d_formatters['DSC']="{:.3f}".format
L590         import scorer
L591         dw_eff = scorer.D_WEIGHTS_EFF
L592         self.d_title = (f"[D枠 / {N_D} / {_fmt_w(dw_eff)} / corrM={corrM} / "
L593                         f"LB={DRRS_D['lookback']} nPC={DRRS_D['n_pc']} γ={DRRS_D['gamma']} λ={DRRS_D['lam']} μ={CROSS_MU_GD} η={DRRS_D['eta']} shrink={DRRS_SHRINK}]")
L594         if near_D:
L595             add = [t for t in near_D if t not in set(D_UNI)][:10]
L596             if add:
L597                 d_disp2 = pd.DataFrame(index=add)
L598                 d_disp2['QAL'], d_disp2['YLD'], d_disp2['VOL'], d_disp2['TRD'] = df_z.loc[add,'D_QAL'], df_z.loc[add,'D_YLD'], df_z.loc[add,'D_VOL_RAW'], df_z.loc[add,'D_TRD']
L599                 near_tbl = pd.concat([d_disp2, pd.Series({t: d_score_all.get(t) for t in add}, name='DSC')], axis=1)
L600                 self.d_table = pd.concat([self.d_table, near_tbl], axis=0)
L601         print(self.d_title); print(self.d_table.to_string(formatters=self.d_formatters))
L602
L603         # === Changes（IN の GSC/DSC を表示。OUT は銘柄名のみ） ===
L604         in_list = sorted(set(list(top_G)+list(top_D)) - set(exist))
L605         out_list = sorted(set(exist) - set(list(top_G)+list(top_D)))
L606
L607         self.io_table = pd.DataFrame({
L608             'IN': pd.Series(in_list),
L609             '/ OUT': pd.Series(out_list)
L610         })
L611         g_list = [f"{g_score.get(t):.3f}" if pd.notna(g_score.get(t)) else '—' for t in out_list]
L612         d_list = [f"{d_score_all.get(t):.3f}" if pd.notna(d_score_all.get(t)) else '—' for t in out_list]
L613         self.io_table['GSC'] = pd.Series(g_list)
L614         self.io_table['DSC'] = pd.Series(d_list)
L615
L61
```