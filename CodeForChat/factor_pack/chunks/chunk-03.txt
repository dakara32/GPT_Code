```text
.bench}")
L454         data = yf.download(tickers + [self.bench], period="600d",
L455                            auto_adjust=True, progress=False, threads=False)
L456         T.log("yf.download done")
L457         px = data["Close"].dropna(how="all", axis=1).ffill(limit=2)
L458         spx = data["Close"][self.bench].reindex(px.index).ffill()
L459         clip_days = int(os.getenv("PRICE_CLIP_DAYS", "0"))   # 0なら無効（既定）
L460         if clip_days > 0:
L461             px  = px.tail(clip_days + 1)
L462             spx = spx.tail(clip_days + 1)
L463             print(f"[T] price window clipped by env: {len(px)} rows (PRICE_CLIP_DAYS={clip_days})")
L464         else:
L465             print(f"[T] price window clip skipped; rows={len(px)}")
L466         tickers_bulk, info = yf.Tickers(" ".join(tickers)), {}
L467         for t in tickers:
L468             try: info[t] = tickers_bulk.tickers[t].info
L469             except Exception as e: print(f"{t}: info fetch failed ({e})"); info[t] = {}
L470         eps_df = self._build_eps_df(tickers, tickers_bulk, info)
L471         fcf_df = self.compute_fcf_with_fallback(tickers, finnhub_api_key=self.api_key)
L472         T.log("eps/fcf prep done")
L473         returns = px[tickers].pct_change()
L474         T.log("price prep/returns done")
L475         return dict(cand=cand_f, tickers=tickers, data=data, px=px, spx=spx, tickers_bulk=tickers_bulk, info=info, eps_df=eps_df, fcf_df=fcf_df, returns=returns)
L476
L477 # === Selector：相関低減・選定（スコア＆リターンだけ読む） ===
L478 class Selector:
L479     # ---- DRRS helpers（Selector専用） ----
L480     @staticmethod
L481     def _z_np(X: np.ndarray) -> np.ndarray:
L482         X = np.asarray(X, dtype=np.float32); m = np.nanmean(X, axis=0, keepdims=True); s = np.nanstd(X, axis=0, keepdims=True)+1e-9
L483         return (np.nan_to_num(X)-m)/s
L484
L485     @classmethod
L486     def residual_corr(cls, R: np.ndarray, n_pc: int=3, shrink: float=0.1) -> np.ndarray:
L487         Z = cls._z_np(R); U,S,_ = np.linalg.svd(Z, full_matrices=False); F = U[:,:n_pc]*S[:n_pc]; B = np.linalg.lstsq(F, Z, rcond=None)[0]
L488         E = Z - F@B; C = np.corrcoef(E, rowvar=False)
L489         off = C - np.diag(np.diag(C)); iu = np.triu_indices_from(off,1); avg_abs = np.nanmean(np.abs(off[iu])) if iu[0].size else 0.0
L490         shrink_eff = float(np.clip(shrink + 0.5*avg_abs, 0.1, 0.6)); N = C.shape[0]
L491         return (1.0 - shrink_eff)*C + shrink_eff*np.eye(N, dtype=C.dtype)
L492
L493     @classmethod
L494     def rrqr_like_det(cls, R: np.ndarray, score: np.ndarray, k: int, gamma: float=1.0):
L495         Z, w = cls._z_np(R), (score-score.min())/(np.ptp(score)+1e-12); X = Z*(1.0 + gamma*w)
L496         N, k = X.shape[1], max(0, min(k, X.shape[1]))
L497         if k==0: return []
L498         S, selected, Rres = [], np.zeros(N, dtype=bool), X.copy()
L499         for _ in range(k):
L500             norms = (Rres*Rres).sum(axis=0); cand = np.where(~selected)[0]
L501             j = sorted(cand, key=lambda c:(-norms[c], -w[c], c))[0]
L502             S.append(j); selected[j]=True; u = X[:,j:j+1]; u/=(np.linalg.norm(u)+1e-12); Rres = Rres - u @ (u.T @ Rres)
L503         return sorted(S)
L504
L505     @staticmethod
L506     def _obj(corrM: np.ndarray, score: np.ndarray, idx, lam: float) -> float:
L507         idx = list(idx); P = corrM[np.ix_(idx, idx)]; s = (score-score.mean())/(score.std()+1e-9)
L508         return float(s[idx].sum() - lam*((P.sum()-np.trace(P))/2.0))
L509
L510     @classmethod
L511     def swap_local_det(cls, corrM: np.ndarray, score: np.ndarray, idx, lam: float=0.6, max_pass: int=15):
L512         S, best, improved, passes = sorted(idx), cls._obj(corrM, score, idx, lam), True, 0
L513         while improved and passes<max_pass:
L514             improved, passes = False, passes+1
L515             for i,out in enumerate(list(S)):
L516                 for inn in range(len(score)):
L517                     if inn in S: continue
L518                     cand = sorted(S[:i]+[inn]+S[i+1:]); v = cls._obj(corrM, score, cand, lam)
L519                     if v>best+1e-10: S, best, improved = cand, v, True; break
L520                 if improved: break
L521         return S, best
L522
L523     @staticmethod
L524     def _obj_with_cross(C_within: np.ndarray, C_cross: np.ndarray|None, score: np.ndarray, idx, lam: float, mu: float) -> float:
L525         idx = list(idx); P = C_within[np.ix_(idx, idx)]; s = (score-score.mean())/(score.std()+1e-9)
L526         within = (P.sum()-np.trace(P))/2.0; cross = 0.0
L527         if C_cross is not None and C_cross.size>0: cross = C_cross[idx,:].sum()
L528         return float(s[idx].sum() - lam*within - mu*cross)
L529
L530     @classmethod
L531     def swap_local_det_cross(cls, C_within: np.ndarray, C_cross: np.ndarray|None, score: np.ndarray, idx, lam: float=0.6, mu: float=0.3, max_pass: int=15):
L532         S, best, improved, passes, N = sorted(idx), cls._obj_with_cross(C_within,C_cross,score,idx,lam,mu), True, 0, len(score)
L533         while improved and passes<max_pass:
L534             improved, passes = False, passes+1
L535             for i,out in enumerate(list(S)):
L536                 for inn in range(N):
L537                     if inn in S: continue
L538                     cand = sorted(S[:i]+[inn]+S[i+1:]); v = cls._obj_with_cross(C_within,C_cross,score,cand,lam,mu)
L539                     if v>best+1e-10: S, best, improved = cand, v, True; break
L540                 if improved: break
L541         return S, best
L542
L543     @staticmethod
L544     def avg_corr(C: np.ndarray, idx) -> float:
L545         k = len(idx); P = C[np.ix_(idx, idx)]
L546         return float((P.sum()-np.trace(P))/(k*(k-1)+1e-12))
L547
L548     @classmethod
L549     def select_bucket_drrs(cls, returns_df: pd.DataFrame, score_ser: pd.Series, pool_tickers: list[str], k: int, *, n_pc: int, gamma: float, lam: float, lookback: int, shrink: float=0.10, g_fixed_tickers: list[str]|None=None, mu: float=0.0):
L550         g_fixed = [t for t in (g_fixed_tickers or []) if t in returns_df.columns]
L551         union = [t for t in pool_tickers if t in returns_df.columns]
L552         for t in g_fixed:
L553             if t not in union: union.append(t)
L554         Rdf_all = returns_df[union]; Rdf_all = Rdf_all.iloc[-lookback:] if len(Rdf_all)>=lookback else Rdf_all; Rdf_all = Rdf_all.dropna()
L555         pool_eff, g_eff = [t for t in pool_tickers if t in Rdf_all.columns], [t for t in g_fixed if t in Rdf_all.columns]
L556         if len(pool_eff)==0: return dict(idx=[], tickers=[], avg_res_corr=np.nan, sum_score=0.0, objective=-np.inf)
L557         score = score_ser.reindex(pool_eff).to_numpy(dtype=np.float32)
L558         C_all = cls.residual_corr(Rdf_all.to_numpy(), n_pc=n_pc, shrink=shrink)
L559         col_pos = {c:i for i,c in enumerate(Rdf_all.columns)}; pool_pos = [col_pos[t] for t in pool_eff]
L560         C_within, C_cross = C_all[np.ix_(pool_pos,pool_pos)], None
L561         if len(g_eff)>0 and mu>0.0:
L562             g_pos = [col_pos[t] for t in g_eff]; C_cross = C_all[np.ix_(pool_pos,g_pos)]
L563         R_pool = Rdf_all[pool_eff].to_numpy(); S0 = cls.rrqr_like_det(R_pool, score, k, gamma=gamma)
L564         S, Jn = (cls.swap_local_det_cross(C_within, C_cross, score, S0, lam=lam, mu=mu, max_pass=15) if C_cross is not None else cls.swap_local_det(C_within, score, S0, lam=lam, max_pass=15))
L565         selected_tickers = [pool_eff[i] for i in S]
L566         return dict(idx=S, tickers=selected_tickers, avg_res_corr=cls.avg_corr(C_within,S), sum_score=float(score[S].sum()), objective=float(Jn))
L567
L568     # ---- 選定（スコア Series / returns だけを受ける）----
L569 # === Output：出力整形と送信（表示のみ） ===
L570 class Output:
L571
L572     def __init__(self, debug=None):
L573         # self.debug は使わない（互換のため引数は受けるが無視）
L574         self.miss_df = self.g_table = self.d_table = self.io_table = self.df_metrics_fmt = self.debug_table = None
L575         self.g_title = self.d_title = ""
L576         self.g_formatters = self.d_formatters = {}
L577         # 低スコア（GSC+DSC）Top10 表示/送信用
L578         self.low10_table = None
L579         self.debug_text = ""   # デバッグ用本文はここに一本化
L580
L581     # --- 表示（元 display_results のロジックそのまま） ---
L582     def display_results(self, *, exist, bench, df_z, g_score, d_score_all,
L583                         init_G, init_D, top_G, top_D, **kwargs):
L584         pd.set_option('display.float_format','{:.3f}'.format)
L585         print("📈 ファクター分散最適化の結果")
L586         if self.miss_df is not None and not self.miss_df.empty:
L587             print("Missing Data:")
L588             print(self.miss_df.to_string(index=False))
L589
L590         # ---- 表示用：Changes/Near-Miss のスコア源を“最終集計”に統一するプロキシ ----
L591         try:
L592             sc = getattr(self, "_sc", None)
L593             agg_G = getattr(sc, "_agg_G", None)
L594             agg_D = getattr(sc, "_agg_D", None)
L595         except Exception:
L596             sc = agg_G = agg_D = None
L597         class _SeriesProxy:
L598             __slots__ = ("primary", "fallback")
L599             def __init__(self, primary, fallback): self.primary, self.fallback = primary, fallback
L600             def get(self, key, default=None):
L601                 try:
L602                     v = self.primary.get(key) if hasattr(self.primary, "get") else None
L603                     if v is not None and not (isinstance(v, float) and v != v):
L604                         return v
L605                 except Exception:
L606                     pass
L607                 try:
L608                     return self.fallback.get(key) if hasattr(self.fallback, "get") else default
L609                 except Exception:
L610                     return default
L611         g_score = _SeriesProxy(agg_G, g_score)
L612         d_score_all = _SeriesProxy(agg_D, d_score_all)
L613         near_G = getattr(sc, "_near_G", []) if sc else []
L614         near_D = getattr(sc, "_near_D", []) if sc else []
L615
L616         extra_G = [t for t in init_G if t not in top_G][:5]; G_UNI = top_G + extra_G
L617         gsc_series = pd.Series({t: g_score.get(t) for t in G_UNI}, name='GSC')
L618         self.g_table = pd.concat([df_z.loc[G_UNI,['GROWTH_F','MOM','TRD','VOL']], gsc_series], axis=1)
L619         self.g_table.index = [t + ("⭐️" if t in top_G else "") for t in G_UNI]
L620         self.g_formatters = {col:"{:.2f}".format for col in ['GROWTH_F','MOM','TRD','VOL']}; self.g_formatters['GSC'] = "{:.3f}".format
L621         self.g_title = (f"[G枠 / {N_G} / {_fmt_w(g_weights)} / corrM={corrM} / "
L622                         f"LB={DRRS_G['lookback']} nPC={DRRS_G['n_pc']} γ={DRRS_G['gamma']} λ={DRRS_G['lam']} η={DRRS_G['eta']} shrink={DRRS_SHRINK}]")
L623         if near_G:
L624             add = [t for t in near_G if t not in set(G_UNI)][:10]
L625             if len(add) < 10:
L626                 try:
L627                     aggG = getattr(sc, "_agg_G", pd.Series(dtype=float)).sort_values(ascending=False)
L628                     out_now = sorted(set(exist) - set(top_G + top_D))  # 今回 OUT
L629                     used = set(G_UNI + add)
L630                     def _push(lst):
L631                         nonlocal add, used
L632                         for t in lst:
L633                             if len(add) == 10: break
L634                             if t in aggG.index and t not in used:
L635                                 add.append(t); used.add(t)
L636                     _push(out_now)           # ① 今回 OUT を優先
L637                     _push(list(aggG.index))  # ② まだ足りなければ上位で充填
L638                 except Exception:
L639                     pass
L640             if add:
L641                 near_tbl = pd.concat([df_z.loc[add,['GROWTH_F','MOM','TRD','VOL']], pd.Series({t: g_score.get(t) for t in add}, name='GSC')], axis=1)
L642                 self.g_table = pd.concat([self.g_table, near_tbl], axis=0)
L643         print(self.g_title); print(self.g_table.to_string(formatters=self.g_formatters))
L644
```