```text
ataFrame, names: list[str]) -> pd.Series|None:
L539         if df is None or df.empty: return None
L540         idx_lower={str(i).lower():i for i in df.index}
L541         for n in names:
L542             k=n.lower()
L543             if k in idx_lower: return df.loc[idx_lower[k]]
L544         return None
L545
L546     @staticmethod
L547     def _sum_last_n(s: pd.Series|None, n: int) -> float|None:
L548         if s is None or s.empty: return None
L549         v=s.dropna().astype(float); return None if v.empty else v.iloc[:n].sum()
L550
L551     @staticmethod
L552     def _latest(s: pd.Series|None) -> float|None:
L553         if s is None or s.empty: return None
L554         v=s.dropna().astype(float); return v.iloc[0] if not v.empty else None
L555
L556     def fetch_cfo_capex_ttm_yf(self, tickers: list[str]) -> pd.DataFrame:
L557         from concurrent.futures import ThreadPoolExecutor, as_completed
L558         pick, sumn, latest, aliases = self._pick_row, self._sum_last_n, self._latest, self._CF_ALIASES
L559
L560         def one(t: str):
L561             try:
L562                 tk = yf.Ticker(t)  # ★ セッションは渡さない（YFがcurl_cffiで管理）
L563                 qcf = tk.quarterly_cashflow
L564                 cfo_q, capex_q = pick(qcf, aliases["cfo"]), pick(qcf, aliases["capex"])
L565                 fcf_q = pick(qcf, ["Free Cash Flow","FreeCashFlow","Free cash flow"])
L566                 cfo, capex, fcf = sumn(cfo_q,4), sumn(capex_q,4), sumn(fcf_q,4)
L567                 if any(v is None for v in (cfo, capex, fcf)):
L568                     acf = tk.cashflow
L569                     if cfo   is None: cfo   = latest(pick(acf, aliases["cfo"]))
L570                     if capex is None: capex = latest(pick(acf, aliases["capex"]))
L571                     if fcf   is None: fcf   = latest(pick(acf, ["Free Cash Flow","FreeCashFlow","Free cash flow"]))
L572             except Exception as e:
L573                 print(f"[warn] yf financials error: {t}: {e}"); cfo=capex=fcf=None
L574             n=np.nan
L575             return {"ticker":t,
L576                     "cfo_ttm_yf":   n if cfo   is None else cfo,
L577                     "capex_ttm_yf": n if capex is None else capex,
L578                     "fcf_ttm_yf_direct": n if fcf is None else fcf}
L579
L580         rows, mw = [], int(os.getenv("FIN_THREADS","8"))
L581         with ThreadPoolExecutor(max_workers=mw) as ex:
L582             rows=[f.result() for f in as_completed(ex.submit(one,t) for t in tickers)]
L583         return pd.DataFrame(rows).set_index("ticker")
L584
L585     _FINN_CFO_KEYS = ["netCashProvidedByOperatingActivities","netCashFromOperatingActivities","cashFlowFromOperatingActivities","operatingCashFlow"]
L586     _FINN_CAPEX_KEYS = ["capitalExpenditure","capitalExpenditures","purchaseOfPPE","investmentsInPropertyPlantAndEquipment"]
L587
L588     @staticmethod
L589     def _first_key(d: dict, keys: list[str]):
L590         for k in keys:
L591             if k in d and d[k] is not None: return d[k]
L592         return None
L593
L594     @staticmethod
L595     def _finn_get(session: requests.Session, url: str, params: dict, retries: int=3, sleep_s: float=0.5):
L596         for i in range(retries):
L597             r = session.get(url, params=params, timeout=15)
L598             if r.status_code==429: time.sleep(min(2**i*sleep_s,4.0)); continue
L599             r.raise_for_status(); return r.json()
L600         r.raise_for_status()
L601
L602     def fetch_cfo_capex_ttm_finnhub(self, tickers: list[str], api_key: str|None=None) -> pd.DataFrame:
L603         api_key = api_key or os.getenv("FINNHUB_API_KEY")
L604         if not api_key: raise ValueError("Finnhub API key not provided. Set FINNHUB_API_KEY or pass api_key=")
L605         base, s, rows = "https://finnhub.io/api/v1", requests.Session(), []
L606         for sym in tickers:
L607             cfo_ttm = capex_ttm = None
L608             try:
L609                 j = self._finn_get(s, f"{base}/stock/cash-flow", {"symbol":sym,"frequency":"quarterly","limit":8,"token":api_key})
L610                 arr = j.get("cashFlow") or []; cfo_vals, capex_vals = [], []
L611                 for item in arr[:4]:
L612                     cfo_vals.append(self._first_key(item,self._FINN_CFO_KEYS)); capex_vals.append(self._first_key(item,self._FINN_CAPEX_KEYS))
L613                 if any(v is not None for v in cfo_vals): cfo_ttm = float(np.nansum([np.nan if v is None else float(v) for v in cfo_vals]))
L614                 if any(v is not None for v in capex_vals): capex_ttm = float(np.nansum([np.nan if v is None else float(v) for v in capex_vals]))
L615             except Exception: pass
L616             if cfo_ttm is None or capex_ttm is None:
L617                 try:
L618                     j = self._finn_get(s, f"{base}/stock/cash-flow", {"symbol":sym,"frequency":"annual","limit":1,"token":api_key})
L619                     arr = j.get("cashFlow") or []
L620                     if arr:
L621                         item0 = arr[0]
L622                         if cfo_ttm is None:
L623                             v = self._first_key(item0,self._FINN_CFO_KEYS)
L624                             if v is not None: cfo_ttm = float(v)
L625                         if capex_ttm is None:
L626                             v = self._first_key(item0,self._FINN_CAPEX_KEYS)
L627                             if v is not None: capex_ttm = float(v)
L628                 except Exception: pass
L629             rows.append({"ticker":sym,"cfo_ttm_fh":np.nan if cfo_ttm is None else cfo_ttm,"capex_ttm_fh":np.nan if capex_ttm is None else capex_ttm})
L630         return pd.DataFrame(rows).set_index("ticker")
L631
L632     def compute_fcf_with_fallback(self, tickers: list[str], finnhub_api_key: str|None=None) -> pd.DataFrame:
L633         yf_df = self.fetch_cfo_capex_ttm_yf(tickers)
L634         T.log("financials (yf) done")
L635         miss_mask = yf_df[["cfo_ttm_yf","capex_ttm_yf","fcf_ttm_yf_direct"]].isna().any(axis=1)
L636         need = yf_df.index[miss_mask].tolist(); print(f"[T] yf financials missing: {len(need)} {need[:10]}{'...' if len(need)>10 else ''}")
L637         if need:
L638             fh_df = self.fetch_cfo_capex_ttm_finnhub(need, api_key=finnhub_api_key)
L639             df = yf_df.join(fh_df, how="left")
L640             for col_yf, col_fh in [("cfo_ttm_yf","cfo_ttm_fh"),("capex_ttm_yf","capex_ttm_fh")]:
L641                 df[col_yf] = df[col_yf].fillna(df[col_fh])
L642             print("[T] financials (finnhub) done (fallback only)")
L643         else:
L644             df = yf_df.assign(cfo_ttm_fh=np.nan, capex_ttm_fh=np.nan)
L645             print("[T] financials (finnhub) skipped (no missing)")
L646         df["cfo_ttm"]  = df["cfo_ttm_yf"].where(df["cfo_ttm_yf"].notna(), df["cfo_ttm_fh"])
L647         df["capex_ttm"] = df["capex_ttm_yf"].where(df["capex_ttm_yf"].notna(), df["capex_ttm_fh"])
L648         cfo, capex = pd.to_numeric(df["cfo_ttm"], errors="coerce"), pd.to_numeric(df["capex_ttm"], errors="coerce").abs()
L649         fcf_calc = cfo - capex
L650         fcf_direct = pd.to_numeric(df.get("fcf_ttm_yf_direct"), errors="coerce")
L651         df["fcf_ttm"] = fcf_calc.where(fcf_calc.notna(), fcf_direct)
L652         df["cfo_source"]  = np.where(df["cfo_ttm_yf"].notna(),"yfinance",np.where(df["cfo_ttm_fh"].notna(),"finnhub",""))
L653         df["capex_source"] = np.where(df["capex_ttm_yf"].notna(),"yfinance",np.where(df["capex_ttm_fh"].notna(),"finnhub",""))
L654         df["fcf_imputed"] = df[["cfo_ttm","capex_ttm"]].isna().any(axis=1) & df["fcf_ttm"].notna()
L655         cols = ["cfo_ttm_yf","capex_ttm_yf","cfo_ttm_fh","capex_ttm_fh","cfo_ttm","capex_ttm","fcf_ttm","fcf_ttm_yf_direct","cfo_source","capex_source","fcf_imputed"]
L656         return df[cols].sort_index()
L657
L658     def _build_eps_df(self, tickers, tickers_bulk, info, sec_map: dict | None = None):
L659         eps_rows=[]
L660         for t in tickers:
L661             info_t = info[t]
L662             sec_t = (sec_map or {}).get(t, {})
L663             eps_ttm = sec_t.get("eps_ttm", info_t.get("trailingEps", np.nan))
L664             eps_q = sec_t.get("eps_q_recent", np.nan)
L665             try:
L666                 qearn, so = tickers_bulk.tickers[t].quarterly_earnings, info_t.get("sharesOutstanding")
L667                 if so and qearn is not None and not qearn.empty and "Earnings" in qearn.columns:
L668                     eps_ttm_q = qearn["Earnings"].head(4).sum()/so
L669                     if pd.notna(eps_ttm_q) and (pd.isna(eps_ttm) or (abs(eps_ttm)>0 and abs(eps_ttm/eps_ttm_q)>3)): eps_ttm = eps_ttm_q
L670                     if pd.isna(eps_q):
L671                         eps_q = qearn["Earnings"].iloc[-1]/so
L672             except Exception: pass
L673             rev_ttm = sec_t.get("rev_ttm", np.nan)
L674             rev_q = sec_t.get("rev_q_recent", np.nan)
L675             if (not sec_t) or pd.isna(rev_ttm):
L676                 try:
L677                     tk = tickers_bulk.tickers[t]
L678                     qfin = getattr(tk, "quarterly_financials", None)
L679                     if qfin is not None and not qfin.empty:
L680                         idx_lower = {str(i).lower(): i for i in qfin.index}
L681                         rev_idx = None
L682                         for name in ("Total Revenue", "TotalRevenue"):
L683                             key = name.lower()
L684                             if key in idx_lower:
L685                                 rev_idx = idx_lower[key]
L686                                 break
L687                         if rev_idx is not None:
L688                             rev_series = pd.to_numeric(qfin.loc[rev_idx], errors="coerce").dropna()
L689                             if not rev_series.empty:
L690                                 rev_ttm_yf = float(rev_series.head(4).sum())
L691                                 if pd.isna(rev_ttm):
L692                                     rev_ttm = rev_ttm_yf
L693                                 if pd.isna(rev_q):
L694                                     rev_q = float(rev_series.iloc[0])
L695                 except Exception:
L696                     pass
L697             eps_rows.append({"ticker":t,"eps_ttm":eps_ttm,"eps_q_recent":eps_q,"rev_ttm":rev_ttm,"rev_q_recent":rev_q})
L698         return self.impute_eps_ttm(pd.DataFrame(eps_rows).set_index("ticker"))
L699
L700     def prepare_data(self):
L701         """Fetch price and fundamental data for all tickers."""
L702         self.sec_dryrun_sample()
L703         cand_info = yf.Tickers(" ".join(self.cand))
L704
L705         def _price(t: str) -> float:
L706             try:
L707                 return cand_info.tickers[t].fast_info.get("lastPrice", np.inf)
L708             except Exception as e:
L709                 print(f"{t}: price fetch failed ({e})")
L710                 return np.inf
L711
L712         cand_prices = {t: _price(t) for t in self.cand}
L713         cand_f = [t for t, p in cand_prices.items() if p <= self.price_max]
L714         T.log("price cap filter done (CAND_PRICE_MAX)")
L715         # 入力ティッカーの重複を除去し、現行→候補の順序を維持
L716         tickers = list(dict.fromkeys(self.exist + cand_f))
L717         T.log(f"universe prepared: unique={len(tickers)} bench={self.bench}")
L718         data = yf.download(tickers + [self.bench], period="600d",
L719                            auto_adjust=True, progress=False, threads=False)
L720         T.log("yf.download done")
L721         px = data["Close"].dropna(how="all", axis=1).ffill(limit=2)
L722         spx = data["Close"][self.bench].reindex(px.index).ffill()
L723         clip_days = int(os.getenv("PRICE_CLIP_DAYS", "0"))   # 0なら無効（既定）
L724         if clip_days > 0:
L725             px, spx = px.tail(clip_days + 1), spx.tail(clip_days + 1)
L726             logger.info("[T] price window clipped by env: %d rows (PRICE_CLIP_DAYS=%d)", len(px), clip_days)
L727         else:
L728             logger.info("[T] price 
```