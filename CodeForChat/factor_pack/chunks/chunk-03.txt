```text
turn dict(cand=cand_f, tickers=tickers, data=data, px=px, spx=spx, tickers_bulk=tickers_bulk, info=info, eps_df=eps_df, fcf_df=fcf_df, returns=returns)
L441
L442 # === Selector：相関低減・選定（スコア＆リターンだけ読む） ===
L443 class Selector:
L444     # ---- DRRS helpers（Selector専用） ----
L445     @staticmethod
L446     def _z_np(X: np.ndarray) -> np.ndarray:
L447         X = np.asarray(X, dtype=np.float32); m = np.nanmean(X, axis=0, keepdims=True); s = np.nanstd(X, axis=0, keepdims=True)+1e-9
L448         return (np.nan_to_num(X)-m)/s
L449
L450     @classmethod
L451     def residual_corr(cls, R: np.ndarray, n_pc: int=3, shrink: float=0.1) -> np.ndarray:
L452         Z = cls._z_np(R); U,S,_ = np.linalg.svd(Z, full_matrices=False); F = U[:,:n_pc]*S[:n_pc]; B = np.linalg.lstsq(F, Z, rcond=None)[0]
L453         E = Z - F@B; C = np.corrcoef(E, rowvar=False)
L454         off = C - np.diag(np.diag(C)); iu = np.triu_indices_from(off,1); avg_abs = np.nanmean(np.abs(off[iu])) if iu[0].size else 0.0
L455         shrink_eff = float(np.clip(shrink + 0.5*avg_abs, 0.1, 0.6)); N = C.shape[0]
L456         return (1.0 - shrink_eff)*C + shrink_eff*np.eye(N, dtype=C.dtype)
L457
L458     @classmethod
L459     def rrqr_like_det(cls, R: np.ndarray, score: np.ndarray, k: int, gamma: float=1.0):
L460         Z, w = cls._z_np(R), (score-score.min())/(np.ptp(score)+1e-12); X = Z*(1.0 + gamma*w)
L461         N, k = X.shape[1], max(0, min(k, X.shape[1]))
L462         if k==0: return []
L463         S, selected, Rres = [], np.zeros(N, dtype=bool), X.copy()
L464         for _ in range(k):
L465             norms = (Rres*Rres).sum(axis=0); cand = np.where(~selected)[0]
L466             j = sorted(cand, key=lambda c:(-norms[c], -w[c], c))[0]
L467             S.append(j); selected[j]=True; u = X[:,j:j+1]; u/=(np.linalg.norm(u)+1e-12); Rres = Rres - u @ (u.T @ Rres)
L468         return sorted(S)
L469
L470     @staticmethod
L471     def _obj(corrM: np.ndarray, score: np.ndarray, idx, lam: float) -> float:
L472         idx = list(idx); P = corrM[np.ix_(idx, idx)]; s = (score-score.mean())/(score.std()+1e-9)
L473         return float(s[idx].sum() - lam*((P.sum()-np.trace(P))/2.0))
L474
L475     @classmethod
L476     def swap_local_det(cls, corrM: np.ndarray, score: np.ndarray, idx, lam: float=0.6, max_pass: int=15):
L477         S, best, improved, passes = sorted(idx), cls._obj(corrM, score, idx, lam), True, 0
L478         while improved and passes<max_pass:
L479             improved, passes = False, passes+1
L480             for i,out in enumerate(list(S)):
L481                 for inn in range(len(score)):
L482                     if inn in S: continue
L483                     cand = sorted(S[:i]+[inn]+S[i+1:]); v = cls._obj(corrM, score, cand, lam)
L484                     if v>best+1e-10: S, best, improved = cand, v, True; break
L485                 if improved: break
L486         return S, best
L487
L488     @staticmethod
L489     def _obj_with_cross(C_within: np.ndarray, C_cross: np.ndarray|None, score: np.ndarray, idx, lam: float, mu: float) -> float:
L490         idx = list(idx); P = C_within[np.ix_(idx, idx)]; s = (score-score.mean())/(score.std()+1e-9)
L491         within = (P.sum()-np.trace(P))/2.0; cross = 0.0
L492         if C_cross is not None and C_cross.size>0: cross = C_cross[idx,:].sum()
L493         return float(s[idx].sum() - lam*within - mu*cross)
L494
L495     @classmethod
L496     def swap_local_det_cross(cls, C_within: np.ndarray, C_cross: np.ndarray|None, score: np.ndarray, idx, lam: float=0.6, mu: float=0.3, max_pass: int=15):
L497         S, best, improved, passes, N = sorted(idx), cls._obj_with_cross(C_within,C_cross,score,idx,lam,mu), True, 0, len(score)
L498         while improved and passes<max_pass:
L499             improved, passes = False, passes+1
L500             for i,out in enumerate(list(S)):
L501                 for inn in range(N):
L502                     if inn in S: continue
L503                     cand = sorted(S[:i]+[inn]+S[i+1:]); v = cls._obj_with_cross(C_within,C_cross,score,cand,lam,mu)
L504                     if v>best+1e-10: S, best, improved = cand, v, True; break
L505                 if improved: break
L506         return S, best
L507
L508     @staticmethod
L509     def avg_corr(C: np.ndarray, idx) -> float:
L510         k = len(idx); P = C[np.ix_(idx, idx)]
L511         return float((P.sum()-np.trace(P))/(k*(k-1)+1e-12))
L512
L513     @classmethod
L514     def select_bucket_drrs(cls, returns_df: pd.DataFrame, score_ser: pd.Series, pool_tickers: list[str], k: int, *, n_pc: int, gamma: float, lam: float, lookback: int, shrink: float=0.10, g_fixed_tickers: list[str]|None=None, mu: float=0.0):
L515         g_fixed = [t for t in (g_fixed_tickers or []) if t in returns_df.columns]
L516         union = [t for t in pool_tickers if t in returns_df.columns]
L517         for t in g_fixed:
L518             if t not in union: union.append(t)
L519         Rdf_all = returns_df[union]; Rdf_all = Rdf_all.iloc[-lookback:] if len(Rdf_all)>=lookback else Rdf_all; Rdf_all = Rdf_all.dropna()
L520         pool_eff, g_eff = [t for t in pool_tickers if t in Rdf_all.columns], [t for t in g_fixed if t in Rdf_all.columns]
L521         if len(pool_eff)==0: return dict(idx=[], tickers=[], avg_res_corr=np.nan, sum_score=0.0, objective=-np.inf)
L522         score = score_ser.reindex(pool_eff).to_numpy(dtype=np.float32)
L523         C_all = cls.residual_corr(Rdf_all.to_numpy(), n_pc=n_pc, shrink=shrink)
L524         col_pos = {c:i for i,c in enumerate(Rdf_all.columns)}; pool_pos = [col_pos[t] for t in pool_eff]
L525         C_within, C_cross = C_all[np.ix_(pool_pos,pool_pos)], None
L526         if len(g_eff)>0 and mu>0.0:
L527             g_pos = [col_pos[t] for t in g_eff]; C_cross = C_all[np.ix_(pool_pos,g_pos)]
L528         R_pool = Rdf_all[pool_eff].to_numpy(); S0 = cls.rrqr_like_det(R_pool, score, k, gamma=gamma)
L529         S, Jn = (cls.swap_local_det_cross(C_within, C_cross, score, S0, lam=lam, mu=mu, max_pass=15) if C_cross is not None else cls.swap_local_det(C_within, score, S0, lam=lam, max_pass=15))
L530         selected_tickers = [pool_eff[i] for i in S]
L531         return dict(idx=S, tickers=selected_tickers, avg_res_corr=cls.avg_corr(C_within,S), sum_score=float(score[S].sum()), objective=float(Jn))
L532
L533     # ---- 選定（スコア Series / returns だけを受ける）----
L534 # === Output：出力整形と送信（表示・Slack） ===
L535 class Output:
L536
L537     def __init__(self, debug=False):
L538         self.debug = debug
L539         self.miss_df = self.g_table = self.d_table = self.io_table = self.df_metrics_fmt = self.debug_table = None
L540         self.g_title = self.d_title = ""
L541         self.g_formatters = self.d_formatters = {}
L542         # 低スコア（GSC+DSC）Top10 表示/送信用
L543         self.low10_table = None
L544
L545     # --- 表示（元 display_results のロジックそのまま） ---
L546     def display_results(self, *, exist, bench, df_z, g_score, d_score_all,
L547                         init_G, init_D, top_G, top_D, **kwargs):
L548         pd.set_option('display.float_format','{:.3f}'.format)
L549         print("📈 ファクター分散最適化の結果")
L550         if self.miss_df is not None and not self.miss_df.empty:
L551             print("Missing Data:")
L552             print(self.miss_df.to_string(index=False))
L553
L554         # ---- 表示用：Changes/Near-Miss のスコア源を“最終集計”に統一するプロキシ ----
L555         try:
L556             sc = getattr(self, "_sc", None)
L557             agg_G = getattr(sc, "_agg_G", None)
L558             agg_D = getattr(sc, "_agg_D", None)
L559         except Exception:
L560             sc = agg_G = agg_D = None
L561         class _SeriesProxy:
L562             __slots__ = ("primary", "fallback")
L563             def __init__(self, primary, fallback): self.primary, self.fallback = primary, fallback
L564             def get(self, key, default=None):
L565                 try:
L566                     v = self.primary.get(key) if hasattr(self.primary, "get") else None
L567                     if v is not None and not (isinstance(v, float) and v != v):
L568                         return v
L569                 except Exception:
L570                     pass
L571                 try:
L572                     return self.fallback.get(key) if hasattr(self.fallback, "get") else default
L573                 except Exception:
L574                     return default
L575         g_score = _SeriesProxy(agg_G, g_score)
L576         d_score_all = _SeriesProxy(agg_D, d_score_all)
L577         near_G = getattr(sc, "_near_G", []) if sc else []
L578         near_D = getattr(sc, "_near_D", []) if sc else []
L579
L580         extra_G = [t for t in init_G if t not in top_G][:5]; G_UNI = top_G + extra_G
L581         gsc_series = pd.Series({t: g_score.get(t) for t in G_UNI}, name='GSC')
L582         self.g_table = pd.concat([df_z.loc[G_UNI,['GROWTH_F','MOM','TRD','VOL']], gsc_series], axis=1)
L583         self.g_table.index = [t + ("⭐️" if t in top_G else "") for t in G_UNI]
L584         self.g_formatters = {col:"{:.2f}".format for col in ['GROWTH_F','MOM','TRD','VOL']}; self.g_formatters['GSC'] = "{:.3f}".format
L585         self.g_title = (f"[G枠 / {N_G} / {_fmt_w(g_weights)} / corrM={corrM} / "
L586                         f"LB={DRRS_G['lookback']} nPC={DRRS_G['n_pc']} γ={DRRS_G['gamma']} λ={DRRS_G['lam']} η={DRRS_G['eta']} shrink={DRRS_SHRINK}]")
L587         if near_G:
L588             add = [t for t in near_G if t not in set(G_UNI)][:10]
L589             if len(add) < 10:
L590                 try:
L591                     aggG = getattr(sc, "_agg_G", pd.Series(dtype=float)).sort_values(ascending=False)
L592                     out_now = sorted(set(exist) - set(top_G + top_D))  # 今回 OUT
L593                     used = set(G_UNI + add)
L594                     def _push(lst):
L595                         nonlocal add, used
L596                         for t in lst:
L597                             if len(add) == 10: break
L598                             if t in aggG.index and t not in used:
L599                                 add.append(t); used.add(t)
L600                     _push(out_now)           # ① 今回 OUT を優先
L601                     _push(list(aggG.index))  # ② まだ足りなければ上位で充填
L602                 except Exception:
L603                     pass
L604             if add:
L605                 near_tbl = pd.concat([df_z.loc[add,['GROWTH_F','MOM','TRD','VOL']], pd.Series({t: g_score.get(t) for t in add}, name='GSC')], axis=1)
L606                 self.g_table = pd.concat([self.g_table, near_tbl], axis=0)
L607         print(self.g_title); print(self.g_table.to_string(formatters=self.g_formatters))
L608
L609         extra_D = [t for t in init_D if t not in top_D][:5]; D_UNI = top_D + extra_D
L610         cols_D = ['QAL','YLD','VOL','TRD']; d_disp = pd.DataFrame(index=D_UNI)
L611         d_disp['QAL'], d_disp['YLD'], d_disp['VOL'], d_disp['TRD'] = df_z.loc[D_UNI,'D_QAL'], df_z.loc[D_UNI,'D_YLD'], df_z.loc[D_UNI,'D_VOL_RAW'], df_z.loc[D_UNI,'D_TRD']
L612         dsc_series = pd.Series({t: d_score_all.get(t) for t in D_UNI}, name='DSC')
L613         self.d_table = pd.concat([d_disp, dsc_series], axis=1); self.d_table.index = [t + ("⭐️" if t in top_D else "") for t in D_UNI]
L614         self.d_formatters = {col:"{:.2f}".format for col in cols_D}; self.d_formatters['DSC']="{:.3f}".format
L615         import scorer
L616         dw_eff = scorer.D_WEIGHTS_EFF
L617         self.d_title = (f"[D枠 / {N_D} / {_fmt_w(dw_eff)} / corrM={corrM} / "
L618                         f"LB={DRRS_D['lookback']} nPC={DRRS_D['n_pc']} γ={DRRS_D['gamma']} λ={DRRS_D['lam']} μ={CROSS_MU_GD} η={DRRS_D['eta']} shrink={DRRS_SHRINK}]")
L619         if near_D:
L620             add = [t for t in near_D if t not in set(D_UNI)][:10]
L621             if add:
L622                 d_disp2 = pd.DataFrame(index=add)
L623                 d_disp2['QAL'], d_disp2['YLD'], d_disp2['VOL'], d_disp2['TRD'] = df_z.loc[add,'D_QAL'], df_z.loc[add,'D_YLD'], df_z.loc[add,'D_VOL_RAW'], df_z.loc[add,'D_TRD
```