```text
       idx += 1
L560                     return float("nan")
L561
L562                 def _quarter_from_annual(vals: list[float]) -> float:
L563                     v = _first_valid(vals)
L564                     return float(v / 4.0) if v == v else float("nan")
L565
L566                 def _quarter_from_annual_prev(vals: list[float]) -> float:
L567                     v = _nth_valid(vals, 1)
L568                     return float(v / 4.0) if v == v else float("nan")
L569
L570                 rev_lastq = _first_valid(rev_q_vals)
L571                 if rev_lastq != rev_lastq:
L572                     rev_lastq = _quarter_from_annual(rev_a_vals)
L573                 eps_lastq = _first_valid(eps_q_vals)
L574                 if eps_lastq != eps_lastq:
L575                     eps_lastq = _quarter_from_annual(eps_a_vals)
L576
L577                 rev_lastq_prev = _nth_valid(rev_q_vals, 4)
L578                 if rev_lastq_prev != rev_lastq_prev:
L579                     rev_lastq_prev = _quarter_from_annual_prev(rev_a_vals)
L580                 eps_lastq_prev = _nth_valid(eps_q_vals, 4)
L581                 if eps_lastq_prev != eps_lastq_prev:
L582                     eps_lastq_prev = _quarter_from_annual_prev(eps_a_vals)
L583
L584                 rev_ttm = self._ttm_from_q_or_a(rev_q_vals, rev_a_vals)
L585                 eps_ttm = self._ttm_from_q_or_a(eps_q_vals, eps_a_vals)
L586                 rev_ttm_prev = self._ttm_from_q_or_a(rev_q_vals[4:], rev_a_vals[1:])
L587                 eps_ttm_prev = self._ttm_from_q_or_a(eps_q_vals[4:], eps_a_vals[1:])
L588
L589                 rev_annual_latest = _first_valid(rev_a_vals)
L590                 rev_annual_prev = _nth_valid(rev_a_vals, 1)
L591                 eps_annual_latest = _first_valid(eps_a_vals)
L592                 eps_annual_prev = _nth_valid(eps_a_vals, 1)
L593
L594                 def _cagr3(vals: list[float]) -> float:
L595                     vals_valid = [v for v in vals if v == v]
L596                     if len(vals_valid) >= 3:
L597                         latest, base = float(vals_valid[0]), float(vals_valid[2])
L598                         if latest > 0 and base > 0:
L599                             try:
L600                                 return float((latest / base) ** (1 / 2) - 1.0)
L601                             except Exception:
L602                                 return float("nan")
L603                     return float("nan")
L604
L605                 rev_cagr3 = _cagr3(rev_a_vals)
L606                 eps_cagr3 = _cagr3(eps_a_vals)
L607
L608                 out[t] = {
L609                     "eps_q_recent": eps_lastq,
L610                     "eps_ttm": eps_ttm,
L611                     "eps_ttm_prev": eps_ttm_prev,
L612                     "eps_lastq_prev": eps_lastq_prev,
L613                     "rev_q_recent": rev_lastq,
L614                     "rev_ttm": rev_ttm,
L615                     "rev_ttm_prev": rev_ttm_prev,
L616                     "rev_lastq_prev": rev_lastq_prev,
L617                     # 後段でDatetimeIndex化できるよう (date,value) を保持。値だけの互換キーも残す。
L618                     # 3年運用に合わせて四半期は直近12本のみ保持（約3年=12Q）
L619                     "eps_q_series_pairs": eps_q_pairs,
L620                     "rev_q_series_pairs": rev_q_pairs,
L621                     "eps_q_series": eps_q_vals,
L622                     "rev_q_series": rev_q_vals,
L623                     "eps_a_series_pairs": eps_a_pairs,
L624                     "rev_a_series_pairs": rev_a_pairs,
L625                     "eps_a_series": eps_a_vals,
L626                     "rev_a_series": rev_a_vals,
L627                     "eps_annual_latest": eps_annual_latest,
L628                     "eps_annual_prev": eps_annual_prev,
L629                     "rev_annual_latest": rev_annual_latest,
L630                     "rev_annual_prev": rev_annual_prev,
L631                     "eps_cagr3": eps_cagr3,
L632                     "rev_cagr3": rev_cagr3,
L633                 }
L634                 n_map += 1
L635                 if any(v == v for v in rev_q_vals) or any(v == v for v in rev_a_vals):
L636                     n_rev += 1
L637                 if any(v == v for v in eps_q_vals) or any(v == v for v in eps_a_vals):
L638                     n_eps += 1
L639             except Exception:
L640                 out[t] = {}
L641                 miss_facts.append(t)
L642             time.sleep(0.30)
L643         # 取得サマリをログ（Actionsで確認しやすいよう print）
L644         try:
L645             total = len(tickers)
L646             print(f"[SEC] map={n_map}/{total}  rev_q_hit={n_rev}  eps_q_hit={n_eps}")
L647             # デバッグ: 取得本数の分布（先頭のみ）
L648             try:
L649                 lens = [len((out.get(t, {}) or {}).get("rev_q_series", [])) for t in tickers]
L650                 print(f"[SEC] rev_q_series length: min={min(lens) if lens else 0} "
L651                       f"p25={np.percentile(lens,25) if lens else 0} median={np.median(lens) if lens else 0} "
L652                       f"p75={np.percentile(lens,75) if lens else 0} max={max(lens) if lens else 0}")
L653             except Exception:
L654                 pass
L655             if miss_map:
L656                 print(f"[SEC] no CIK map: {len(miss_map)} (サンプル例) {miss_map[:20]}")
L657             if miss_facts:
L658                 print(f"[SEC] CIKあり だが対象factなし: {len(miss_facts)} (サンプル例) {miss_facts[:20]}")
L659         except Exception:
L660             pass
L661         return out
L662
L663     def sec_dryrun_sample(self, tickers: list[str] | None = None) -> None:
L664         if not _env_true("SEC_DRYRUN_SAMPLE", False):
L665             return
L666         sample = tickers or ["BRK.B", "BF.B", "GOOGL", "META", "UBER", "PBR.A", "TSM", "NARI", "EVBN", "SWAV"]
L667         print(f"[SEC-DRYRUN] sample tickers: {sample}")
L668         try:
L669             t2cik = self._sec_ticker_map()
L670             hits = 0
L671             for sym in sample:
L672                 candidates: list[str] = []
L673
L674                 def add(key: str) -> None:
L675                     if key and key not in candidates:
L676                         candidates.append(key)
L677
L678                 add((sym or "").upper())
L679                 for alt in self._normalize_ticker(sym):
L680                     add(alt)
L681                 if any(t2cik.get(key) for key in candidates):
L682                     hits += 1
L683             sec_data = self.fetch_eps_rev_from_sec(sample)
L684             rev_hits = sum(1 for v in sec_data.values() if v.get("rev_q_series"))
L685             eps_hits = sum(1 for v in sec_data.values() if v.get("eps_q_series"))
L686             total = len(sample)
L687             print(f"[SEC-DRYRUN] CIK map hit: {hits}/{total}  rev_q_series hits: {rev_hits}  eps_q_series hits: {eps_hits}")
L688         except Exception as e:
L689             print(f"[SEC-DRYRUN] error: {e}")
L690     @staticmethod
L691     def impute_eps_ttm(df: pd.DataFrame, ttm_col: str="eps_ttm", q_col: str="eps_q_recent", out_col: str|None=None) -> pd.DataFrame:
L692         out_col = out_col or ttm_col; df = df.copy(); df["eps_imputed"] = False
L693         cand = df[q_col]*4; ok = df[ttm_col].isna() & cand.replace([np.inf,-np.inf], np.nan).notna()
L694         df.loc[ok, out_col], df.loc[ok,"eps_imputed"] = cand[ok], True; return df
L695
L696     _CF_ALIASES = {"cfo":["Operating Cash Flow","Total Cash From Operating Activities"], "capex":["Capital Expenditure","Capital Expenditures"]}
L697
L698     @staticmethod
L699     def _pick_row(df: pd.DataFrame, names: list[str]) -> pd.Series|None:
L700         if df is None or df.empty: return None
L701         idx_lower={str(i).lower():i for i in df.index}
L702         for n in names:
L703             k=n.lower()
L704             if k in idx_lower: return df.loc[idx_lower[k]]
L705         return None
L706
L707     @staticmethod
L708     def _sum_last_n(s: pd.Series|None, n: int) -> float|None:
L709         if s is None or s.empty: return None
L710         v=s.dropna().astype(float); return None if v.empty else v.iloc[:n].sum()
L711
L712     @staticmethod
L713     def _latest(s: pd.Series|None) -> float|None:
L714         if s is None or s.empty: return None
L715         v=s.dropna().astype(float); return v.iloc[0] if not v.empty else None
L716
L717     def fetch_cfo_capex_ttm_yf(self, tickers: list[str]) -> pd.DataFrame:
L718         from concurrent.futures import ThreadPoolExecutor, as_completed
L719         pick, sumn, latest, aliases = self._pick_row, self._sum_last_n, self._latest, self._CF_ALIASES
L720
L721         def one(t: str):
L722             try:
L723                 tk = yf.Ticker(t)  # ★ セッションは渡さない（YFがcurl_cffiで管理）
L724                 qcf = tk.quarterly_cashflow
L725                 cfo_q, capex_q = pick(qcf, aliases["cfo"]), pick(qcf, aliases["capex"])
L726                 fcf_q = pick(qcf, ["Free Cash Flow","FreeCashFlow","Free cash flow"])
L727                 cfo, capex, fcf = sumn(cfo_q,4), sumn(capex_q,4), sumn(fcf_q,4)
L728                 if any(v is None for v in (cfo, capex, fcf)):
L729                     acf = tk.cashflow
L730                     if cfo   is None: cfo   = latest(pick(acf, aliases["cfo"]))
L731                     if capex is None: capex = latest(pick(acf, aliases["capex"]))
L732                     if fcf   is None: fcf   = latest(pick(acf, ["Free Cash Flow","FreeCashFlow","Free cash flow"]))
L733             except Exception as e:
L734                 print(f"[warn] yf financials error: {t}: {e}"); cfo=capex=fcf=None
L735             n=np.nan
L736             return {"ticker":t,
L737                     "cfo_ttm_yf":   n if cfo   is None else cfo,
L738                     "capex_ttm_yf": n if capex is None else capex,
L739                     "fcf_ttm_yf_direct": n if fcf is None else fcf}
L740
L741         rows, mw = [], int(os.getenv("FIN_THREADS","8"))
L742         with ThreadPoolExecutor(max_workers=mw) as ex:
L743             rows=[f.result() for f in as_completed(ex.submit(one,t) for t in tickers)]
L744         return pd.DataFrame(rows).set_index("ticker")
L745
L746     _FINN_CFO_KEYS = ["netCashProvidedByOperatingActivities","netCashFromOperatingActivities","cashFlowFromOperatingActivities","operatingCashFlow"]
L747     _FINN_CAPEX_KEYS = ["capitalExpenditure","capitalExpenditures","purchaseOfPPE","investmentsInPropertyPlantAndEquipment"]
L748
L749     @staticmethod
L750     def _first_key(d: dict, keys: list[str]):
L751         for k in keys:
L752             if k in d and d[k] is not None: return d[k]
L753         return None
L754
L755     @staticmethod
L756     def _finn_get(session: requests.Session, url: str, params: dict, retries: int=3, sleep_s: float=0.5):
L757         for i in range(retries):
L758             r = session.get(url, params=params, timeout=15)
L759             if r.status_code==429: time.sleep(min(2**i*sleep_s,4.0)); continue
L760             r.raise_for_status(); return r.json()
L761         r.raise_for_status()
L762
L763     def fetch_cfo_capex_ttm_finnhub(self, tickers: list[str], api_key: str|None=None) -> pd.DataFrame:
L764         api_key = api_key or os.getenv("FINNHUB_API_KEY")
L765         if not api_key: raise ValueError("Finnhub API key not provided. Set FINNHUB_API_KEY or pass api_key=")
L766         base, s, rows = "https://finnhub.io/api/v1", requests.Session(), []
L767         for sym in tickers:
L768             cfo_ttm = capex_ttm = None
L769             try:
L770                 j = self._finn_get(s, f"{base}/stock/cash-flow", {"symbol":sym,"frequency":"quarterly","limit":8,"token":api_key})
L771                 arr = j.get("cashFlow") or []; cfo_vals, capex_vals = [], []
L772                 for item in arr[:4]:
L773                     cfo_vals.append(self._first_key(item,self._FINN_CFO_KEYS)); capex_vals.append(self._first_key(item,self._FINN_CAPEX_KEYS))
L774                 if any(v is not None for v in cfo_vals): cfo_ttm = float(np.nansum([np.nan if v is None else float(v) for
```