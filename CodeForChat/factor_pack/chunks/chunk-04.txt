```text
(add) == 10: break
L646                             if t in aggG.index and t not in used:
L647                                 add.append(t); used.add(t)
L648                     _push(out_now)           # ① 今回 OUT を優先
L649                     _push(list(aggG.index))  # ② まだ足りなければ上位で充填
L650                 except Exception:
L651                     pass
L652             if add:
L653                 near_tbl = pd.concat([df_z.loc[add,['GROWTH_F','MOM','TRD','VOL']], pd.Series({t: g_score.get(t) for t in add}, name='GSC')], axis=1)
L654                 self.g_table = pd.concat([self.g_table, near_tbl], axis=0)
L655         print(self.g_title); print(self.g_table.to_string(formatters=self.g_formatters))
L656
L657         extra_D = [t for t in init_D if t not in top_D][:5]; D_UNI = top_D + extra_D
L658         cols_D = ['QAL','YLD','VOL','TRD']; d_disp = pd.DataFrame(index=D_UNI)
L659         d_disp['QAL'], d_disp['YLD'], d_disp['VOL'], d_disp['TRD'] = df_z.loc[D_UNI,'D_QAL'], df_z.loc[D_UNI,'D_YLD'], df_z.loc[D_UNI,'D_VOL_RAW'], df_z.loc[D_UNI,'D_TRD']
L660         dsc_series = pd.Series({t: d_score_all.get(t) for t in D_UNI}, name='DSC')
L661         self.d_table = pd.concat([d_disp, dsc_series], axis=1); self.d_table.index = [t + ("⭐️" if t in top_D else "") for t in D_UNI]
L662         self.d_formatters = {col:"{:.2f}".format for col in cols_D}; self.d_formatters['DSC']="{:.3f}".format
L663         import scorer
L664         dw_eff = scorer.D_WEIGHTS_EFF
L665         self.d_title = (f"[D枠 / {N_D} / {_fmt_w(dw_eff)} / corrM={corrM} / "
L666                         f"LB={DRRS_D['lookback']} nPC={DRRS_D['n_pc']} γ={DRRS_D['gamma']} λ={DRRS_D['lam']} μ={CROSS_MU_GD} η={DRRS_D['eta']} shrink={DRRS_SHRINK}]")
L667         if near_D:
L668             add = [t for t in near_D if t not in set(D_UNI)][:10]
L669             if add:
L670                 d_disp2 = pd.DataFrame(index=add)
L671                 d_disp2['QAL'], d_disp2['YLD'], d_disp2['VOL'], d_disp2['TRD'] = df_z.loc[add,'D_QAL'], df_z.loc[add,'D_YLD'], df_z.loc[add,'D_VOL_RAW'], df_z.loc[add,'D_TRD']
L672                 near_tbl = pd.concat([d_disp2, pd.Series({t: d_score_all.get(t) for t in add}, name='DSC')], axis=1)
L673                 self.d_table = pd.concat([self.d_table, near_tbl], axis=0)
L674         print(self.d_title); print(self.d_table.to_string(formatters=self.d_formatters))
L675
L676         # === Changes（IN の GSC/DSC を表示。OUT は銘柄名のみ） ===
L677         in_list = sorted(set(list(top_G)+list(top_D)) - set(exist))
L678         out_list = sorted(set(exist) - set(list(top_G)+list(top_D)))
L679
L680         self.io_table = pd.DataFrame({
L681             'IN': pd.Series(in_list),
L682             '/ OUT': pd.Series(out_list)
L683         })
L684         g_list = [f"{g_score.get(t):.3f}" if pd.notna(g_score.get(t)) else '—' for t in out_list]
L685         d_list = [f"{d_score_all.get(t):.3f}" if pd.notna(d_score_all.get(t)) else '—' for t in out_list]
L686         self.io_table['GSC'] = pd.Series(g_list)
L687         self.io_table['DSC'] = pd.Series(d_list)
L688
L689         print("Changes:")
L690         print(self.io_table.to_string(index=False))
L691
L692         all_tickers = list(set(exist + list(top_G) + list(top_D) + [bench])); prices = yf.download(all_tickers, period='1y', auto_adjust=True, progress=False, threads=False)['Close'].ffill(limit=2)
L693         ret = prices.pct_change(); portfolios = {'CUR':exist,'NEW':list(top_G)+list(top_D)}; metrics={}
L694         for name,ticks in portfolios.items():
L695             pr = ret[ticks].mean(axis=1, skipna=True).dropna(); cum = (1+pr).cumprod()-1; n = len(pr)
L696             if n>=252: ann_ret, ann_vol = (1+cum.iloc[-1])**(252/n)-1, pr.std()*np.sqrt(252)
L697             else: ann_ret, ann_vol = cum.iloc[-1], pr.std()*np.sqrt(n)
L698             sharpe, drawdown = ann_ret/ann_vol, (cum - cum.cummax()).min()
L699             if len(ticks)>=2:
L700                 C_raw = ret[ticks].corr(); RAW_rho = C_raw.mask(np.eye(len(ticks), dtype=bool)).stack().mean()
L701                 R = ret[ticks].dropna().to_numpy(); C_resid = Selector.residual_corr(R, n_pc=3, shrink=DRRS_SHRINK)
L702                 RESID_rho = float((C_resid.sum()-np.trace(C_resid))/(C_resid.shape[0]*(C_resid.shape[0]-1)))
L703             else: RAW_rho = RESID_rho = np.nan
L704             divy = ttm_div_yield_portfolio(ticks); metrics[name] = {'RET':ann_ret,'VOL':ann_vol,'SHP':sharpe,'MDD':drawdown,'RAWρ':RAW_rho,'RESIDρ':RESID_rho,'DIVY':divy}
L705         df_metrics = pd.DataFrame(metrics).T; df_metrics_pct = df_metrics.copy(); self.df_metrics = df_metrics
L706         for col in ['RET','VOL','MDD','DIVY']: df_metrics_pct[col] = df_metrics_pct[col]*100
L707         cols_order = ['RET','VOL','SHP','MDD','RAWρ','RESIDρ','DIVY']; df_metrics_pct = df_metrics_pct.reindex(columns=cols_order)
L708         def _fmt_row(s):
L709             return pd.Series({'RET':f"{s['RET']:.1f}%",'VOL':f"{s['VOL']:.1f}%",'SHP':f"{s['SHP']:.1f}",'MDD':f"{s['MDD']:.1f}%",'RAWρ':(f"{s['RAWρ']:.2f}" if pd.notna(s['RAWρ']) else "NaN"),'RESIDρ':(f"{s['RESIDρ']:.2f}" if pd.notna(s['RESIDρ']) else "NaN"),'DIVY':f"{s['DIVY']:.1f}%"})
L710         self.df_metrics_fmt = df_metrics_pct.apply(_fmt_row, axis=1); print("Performance Comparison:"); print(self.df_metrics_fmt.to_string())
L711         # === 追加: GSC+DSC が低い順 TOP10 ===
L712         try:
L713             all_scores = pd.DataFrame({'GSC': df_z['GSC'], 'DSC': df_z['DSC']}).copy()
L714             all_scores['G_plus_D'] = all_scores['GSC'] + all_scores['DSC']
L715             all_scores = all_scores.dropna(subset=['G_plus_D'])
L716             self.low10_table = all_scores.sort_values('G_plus_D', ascending=True).head(10).round(3)
L717             print("Low Score Candidates (GSC+DSC bottom 10):")
L718             print(self.low10_table.to_string())
L719         except Exception as e:
L720             print(f"[warn] low-score ranking failed: {e}")
L721             self.low10_table = None
L722         # --- ここから: デバッグ出力は _compact_debug で一本化（表示経路もSlack経路もこれだけ）---
L723         if debug_mode:
L724             from types import SimpleNamespace
L725
L726             df_full_src    = getattr(getattr(self, "_sc", None), "_feat", None)
L727             df_full        = getattr(df_full_src, "df_full", None) or kwargs.get("df_full")
L728             df_full_z_pass = getattr(df_full_src, "df_full_z", None) or kwargs.get("df_full_z")
L729
L730             fb_like = SimpleNamespace(
L731                 df_full=df_full,
L732                 df_z=df_z,
L733                 df_full_z=df_full_z_pass,
L734                 g_score=g_score,
L735                 d_score_all=d_score_all,
L736                 missing_logs=self.miss_df,
L737             )
L738             sb_like = SimpleNamespace(top_G=top_G, top_D=top_D)
L739
L740             self.debug_text = _compact_debug(
L741                 fb_like,
L742                 sb_like,
L743                 prevG=kwargs.get("prev_G", exist),
L744                 prevD=kwargs.get("prev_D", exist),
L745                 max_rows=int(os.getenv("DEBUG_MAX_ROWS", "120")),
L746             )
L747
L748             if not (self.debug_text or "").strip():
L749                 src_df = df_full or df_full_z_pass or df_z
L750                 if src_df is not None and getattr(src_df, "empty", False) is False:
L751                     core = [c for c in [
L752                         "GRW","TR_EPS","TR_REV","EPS_Q_YOY","REV_Q_YOY","REV_YOY_ACC",
L753                         "RULE40","FCF_MGN","GSC","DSC"
L754                     ] if c in src_df.columns]
L755                     if core:
L756                         pick: list[str] = []
L757                         for t in (self.g_table, self.d_table, self.low10_table):
L758                             if t is not None and getattr(t, "empty", False) is False:
L759                                 pick += [i for i in list(t.index) if i in src_df.index]
L760                         view = (src_df.loc[pick, core] if pick else src_df[core]).head(int(os.getenv("DEBUG_MAX_ROWS", "80")))
L761                         self.debug_text = "DEBUG fallback\n" + view.to_string()
L762                     else:
L763                         self.debug_text = "(no debug columns)"
L764                 else:
L765                     self.debug_text = "(no dataframe)"
L766         else:
L767             self.debug_text = ""
L768         if debug_mode and (self.debug_text or "").strip():
L769             print("```DEBUG (selected + current + near-miss)```")
L770             print(self.debug_text)
L771             _write_debug_log(self.debug_text, "debug_scores.txt")
L772
L773     def notify_slack(self):
L774         """[log] 互換名：本文を標準出力へ"""
L775
L776         def _filter_suffix_from(spec: dict, group: str) -> str:
L777             g = spec.get(group, {})
L778             parts = [str(m) for m in g.get("pre_mask", [])]
L779             for k, v in (g.get("pre_filter", {}) or {}).items():
L780                 base, op = (k[:-4], "<") if k.endswith("_max") else ((k[:-4], ">") if k.endswith("_min") else (k, "="))
L781                 name = {"beta": "β"}.get(base, base)
L782                 try:
L783                     val = f"{float(v):g}"
L784                 except Exception:
L785                     val = str(v)
L786                 parts.append(f"{name}{op}{val}")
L787             return "" if not parts else " / filter:" + " & ".join(parts)
L788
L789         def _inject_filter_suffix(title: str, group: str) -> str:
L790             suf = _filter_suffix_from(FILTER_SPEC, group)
L791             return f"{title[:-1]}{suf}]" if suf and title.endswith("]") else (title + suf)
L792
L793         def _blk(title, tbl, fmt=None, drop=()):
L794             if tbl is None or getattr(tbl, 'empty', False):
L795                 return f"{title}\n(選定なし)\n"
L796             if drop and hasattr(tbl, 'columns'):
L797                 keep = [c for c in tbl.columns if c not in drop]
L798                 tbl, fmt = tbl[keep], {k: v for k, v in (fmt or {}).items() if k in keep}
L799             return f"{title}\n```{tbl.to_string(formatters=fmt)}```\n"
L800
L801         message = "📈 ファクター分散最適化の結果\n"
L802         if self.miss_df is not None and not self.miss_df.empty:
L803             message += "Missing Data\n```" + self.miss_df.to_string(index=False) + "```\n"
L804         message += _blk(_inject_filter_suffix(self.g_title, "G"), self.g_table, self.g_formatters, drop=("TRD",))
L805         message += _blk(_inject_filter_suffix(self.d_title, "D"), self.d_table, self.d_formatters)
L806         message += "Changes\n" + ("(変更なし)\n" if self.io_table is None or getattr(self.io_table, 'empty', False) else f"```{self.io_table.to_string(index=False)}```\n")
L807         message += "Performance Comparison:\n```" + self.df_metrics_fmt.to_string() + "```"
L808
L809         print(message)
L810
L811         if (self.debug_text or "").strip():
L812             print("```DEBUG (after Low Score)```")
L813             print(self.debug_text)
L814
L815 def _infer_g_universe(feature_df, selected12=None, near5=None):
L816     try:
L817         out = feature_df.index[feature_df['group'].astype(str).str.upper().eq('G')].tolist()
L818         if out: return out
L819     except Exception:
L820         pass
L821     base = set()
L822     for lst in (selected12 or []), (near5 or []):
L823         for x in (lst or []): base.add(x)
L824     return list(base) if base else list(feature_df.index)
L825
L826 def _fmt_with_fire_mark(tickers, feature_df):
L827     out = []
L828     for t in tickers or []:
L829         try:
L830             br = bool(feature_df.at[t, "G_BREAKOUT_recent_5d"])
L831             pb = bool(feature_df.at[t, "G_PULLBACK_recent_5d"])
L832             out.append(f"{t}{' 🔥' if (br or pb) else ''}")
L833         except Exception:
L834             out.append(t)
L835     return out
L836
L837 def _label_recent_event(t, feature_df):
L838     try:
L839         br = bool
```