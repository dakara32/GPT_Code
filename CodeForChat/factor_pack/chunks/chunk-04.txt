```text
port ThreadPoolExecutor, as_completed
L827         pick, sumn, latest, aliases = self._pick_row, self._sum_last_n, self._latest, self._CF_ALIASES
L828
L829         def one(t: str):
L830             try:
L831                 tk = yf.Ticker(t)  # ★ セッションは渡さない（YFがcurl_cffiで管理）
L832                 qcf = tk.quarterly_cashflow
L833                 cfo_q, capex_q = pick(qcf, aliases["cfo"]), pick(qcf, aliases["capex"])
L834                 fcf_q = pick(qcf, ["Free Cash Flow","FreeCashFlow","Free cash flow"])
L835                 cfo, capex, fcf = sumn(cfo_q,4), sumn(capex_q,4), sumn(fcf_q,4)
L836                 if any(v is None for v in (cfo, capex, fcf)):
L837                     acf = tk.cashflow
L838                     if cfo   is None: cfo   = latest(pick(acf, aliases["cfo"]))
L839                     if capex is None: capex = latest(pick(acf, aliases["capex"]))
L840                     if fcf   is None: fcf   = latest(pick(acf, ["Free Cash Flow","FreeCashFlow","Free cash flow"]))
L841             except Exception as e:
L842                 print(f"[warn] yf financials error: {t}: {e}"); cfo=capex=fcf=None
L843             n=np.nan
L844             return {"ticker":t,
L845                     "cfo_ttm_yf":   n if cfo   is None else cfo,
L846                     "capex_ttm_yf": n if capex is None else capex,
L847                     "fcf_ttm_yf_direct": n if fcf is None else fcf}
L848
L849         rows, mw = [], int(os.getenv("FIN_THREADS","8"))
L850         with ThreadPoolExecutor(max_workers=mw) as ex:
L851             rows=[f.result() for f in as_completed(ex.submit(one,t) for t in tickers)]
L852         return pd.DataFrame(rows).set_index("ticker")
L853
L854     _FINN_CFO_KEYS = ["netCashProvidedByOperatingActivities","netCashFromOperatingActivities","cashFlowFromOperatingActivities","operatingCashFlow"]
L855     _FINN_CAPEX_KEYS = ["capitalExpenditure","capitalExpenditures","purchaseOfPPE","investmentsInPropertyPlantAndEquipment"]
L856
L857     @staticmethod
L858     def _first_key(d: dict, keys: list[str]):
L859         for k in keys:
L860             if k in d and d[k] is not None: return d[k]
L861         return None
L862
L863     @staticmethod
L864     def _finn_get(session: requests.Session, url: str, params: dict, retries: int=3, sleep_s: float=0.5):
L865         for i in range(retries):
L866             r = session.get(url, params=params, timeout=15)
L867             if r.status_code==429: time.sleep(min(2**i*sleep_s,4.0)); continue
L868             r.raise_for_status(); return r.json()
L869         r.raise_for_status()
L870
L871     def fetch_cfo_capex_ttm_finnhub(self, tickers: list[str], api_key: str|None=None) -> pd.DataFrame:
L872         api_key = api_key or os.getenv("FINNHUB_API_KEY")
L873         if not api_key: raise ValueError("Finnhub API key not provided. Set FINNHUB_API_KEY or pass api_key=")
L874         base, s, rows = "https://finnhub.io/api/v1", requests.Session(), []
L875         for sym in tickers:
L876             cfo_ttm = capex_ttm = None
L877             try:
L878                 j = self._finn_get(s, f"{base}/stock/cash-flow", {"symbol":sym,"frequency":"quarterly","limit":8,"token":api_key})
L879                 arr = j.get("cashFlow") or []; cfo_vals, capex_vals = [], []
L880                 for item in arr[:4]:
L881                     cfo_vals.append(self._first_key(item,self._FINN_CFO_KEYS)); capex_vals.append(self._first_key(item,self._FINN_CAPEX_KEYS))
L882                 if any(v is not None for v in cfo_vals): cfo_ttm = float(np.nansum([np.nan if v is None else float(v) for v in cfo_vals]))
L883                 if any(v is not None for v in capex_vals): capex_ttm = float(np.nansum([np.nan if v is None else float(v) for v in capex_vals]))
L884             except Exception: pass
L885             if cfo_ttm is None or capex_ttm is None:
L886                 try:
L887                     j = self._finn_get(s, f"{base}/stock/cash-flow", {"symbol":sym,"frequency":"annual","limit":1,"token":api_key})
L888                     arr = j.get("cashFlow") or []
L889                     if arr:
L890                         item0 = arr[0]
L891                         if cfo_ttm is None:
L892                             v = self._first_key(item0,self._FINN_CFO_KEYS)
L893                             if v is not None: cfo_ttm = float(v)
L894                         if capex_ttm is None:
L895                             v = self._first_key(item0,self._FINN_CAPEX_KEYS)
L896                             if v is not None: capex_ttm = float(v)
L897                 except Exception: pass
L898             rows.append({"ticker":sym,"cfo_ttm_fh":np.nan if cfo_ttm is None else cfo_ttm,"capex_ttm_fh":np.nan if capex_ttm is None else capex_ttm})
L899         return pd.DataFrame(rows).set_index("ticker")
L900
L901     def compute_fcf_with_fallback(self, tickers: list[str], finnhub_api_key: str|None=None) -> pd.DataFrame:
L902         yf_df = self.fetch_cfo_capex_ttm_yf(tickers)
L903         T.log("financials (yf) done")
L904         miss_mask = yf_df[["cfo_ttm_yf","capex_ttm_yf","fcf_ttm_yf_direct"]].isna().any(axis=1)
L905         need = yf_df.index[miss_mask].tolist(); print(f"[T] yf financials missing: {len(need)} {need[:10]}{'...' if len(need)>10 else ''}")
L906         if need:
L907             fh_df = self.fetch_cfo_capex_ttm_finnhub(need, api_key=finnhub_api_key)
L908             df = yf_df.join(fh_df, how="left")
L909             for col_yf, col_fh in [("cfo_ttm_yf","cfo_ttm_fh"),("capex_ttm_yf","capex_ttm_fh")]:
L910                 df[col_yf] = df[col_yf].fillna(df[col_fh])
L911             print("[T] financials (finnhub) done (fallback only)")
L912         else:
L913             df = yf_df.assign(cfo_ttm_fh=np.nan, capex_ttm_fh=np.nan)
L914             print("[T] financials (finnhub) skipped (no missing)")
L915         df["cfo_ttm"]  = df["cfo_ttm_yf"].where(df["cfo_ttm_yf"].notna(), df["cfo_ttm_fh"])
L916         df["capex_ttm"] = df["capex_ttm_yf"].where(df["capex_ttm_yf"].notna(), df["capex_ttm_fh"])
L917         cfo, capex = pd.to_numeric(df["cfo_ttm"], errors="coerce"), pd.to_numeric(df["capex_ttm"], errors="coerce").abs()
L918         fcf_calc = cfo - capex
L919         fcf_direct = pd.to_numeric(df.get("fcf_ttm_yf_direct"), errors="coerce")
L920         df["fcf_ttm"] = fcf_calc.where(fcf_calc.notna(), fcf_direct)
L921         df["cfo_source"]  = np.where(df["cfo_ttm_yf"].notna(),"yfinance",np.where(df["cfo_ttm_fh"].notna(),"finnhub",""))
L922         df["capex_source"] = np.where(df["capex_ttm_yf"].notna(),"yfinance",np.where(df["capex_ttm_fh"].notna(),"finnhub",""))
L923         df["fcf_imputed"] = df[["cfo_ttm","capex_ttm"]].isna().any(axis=1) & df["fcf_ttm"].notna()
L924         cols = ["cfo_ttm_yf","capex_ttm_yf","cfo_ttm_fh","capex_ttm_fh","cfo_ttm","capex_ttm","fcf_ttm","fcf_ttm_yf_direct","cfo_source","capex_source","fcf_imputed"]
L925         return df[cols].sort_index()
L926
L927     def _build_eps_df(self, tickers, tickers_bulk, info, sec_map: dict | None = None):
L928         eps_rows=[]
L929         for t in tickers:
L930             info_t = info[t]
L931             sec_t = (sec_map or {}).get(t, {})
L932             eps_ttm = sec_t.get("eps_ttm", info_t.get("trailingEps", np.nan))
L933             eps_q = sec_t.get("eps_q_recent", np.nan)
L934             try:
L935                 tk = tickers_bulk.tickers.get(t)
L936                 if tk is None:
L937                     sym = info_t.get("_yf_symbol") if isinstance(info_t, dict) else None
L938                     if sym:
L939                         tk = tickers_bulk.tickers.get(sym)
L940                 qearn = tk.quarterly_earnings if tk is not None else None
L941                 so = info_t.get("sharesOutstanding")
L942                 if so and qearn is not None and not qearn.empty and "Earnings" in qearn.columns:
L943                     eps_ttm_q = qearn["Earnings"].head(4).sum()/so
L944                     if pd.notna(eps_ttm_q) and (pd.isna(eps_ttm) or (abs(eps_ttm)>0 and abs(eps_ttm/eps_ttm_q)>3)): eps_ttm = eps_ttm_q
L945                     if pd.isna(eps_q):
L946                         eps_q = qearn["Earnings"].iloc[-1]/so
L947             except Exception: pass
L948             rev_ttm = sec_t.get("rev_ttm", np.nan)
L949             rev_q = sec_t.get("rev_q_recent", np.nan)
L950             if (not sec_t) or pd.isna(rev_ttm):
L951                 try:
L952                     tk = tickers_bulk.tickers.get(t)
L953                     if tk is None and isinstance(info_t, dict):
L954                         sym = info_t.get("_yf_symbol")
L955                         if sym:
L956                             tk = tickers_bulk.tickers.get(sym)
L957                     qfin = getattr(tk, "quarterly_financials", None)
L958                     if qfin is not None and not qfin.empty:
L959                         idx_lower = {str(i).lower(): i for i in qfin.index}
L960                         rev_idx = None
L961                         for name in ("Total Revenue", "TotalRevenue"):
L962                             key = name.lower()
L963                             if key in idx_lower:
L964                                 rev_idx = idx_lower[key]
L965                                 break
L966                         if rev_idx is not None:
L967                             rev_series = pd.to_numeric(qfin.loc[rev_idx], errors="coerce").dropna()
L968                             if not rev_series.empty:
L969                                 rev_ttm_yf = float(rev_series.head(4).sum())
L970                                 if pd.isna(rev_ttm):
L971                                     rev_ttm = rev_ttm_yf
L972                                 if pd.isna(rev_q):
L973                                     rev_q = float(rev_series.iloc[0])
L974                 except Exception:
L975                     pass
L976             eps_rows.append({
L977                 "ticker": t,
L978                 "eps_ttm": eps_ttm,
L979                 "eps_ttm_prev": sec_t.get("eps_ttm_prev", np.nan),
L980                 "eps_q_recent": eps_q,
L981                 "eps_q_prev": sec_t.get("eps_lastq_prev", np.nan),
L982                 "rev_ttm": rev_ttm,
L983                 "rev_ttm_prev": sec_t.get("rev_ttm_prev", np.nan),
L984                 "rev_q_recent": rev_q,
L985                 "rev_q_prev": sec_t.get("rev_lastq_prev", np.nan),
L986                 "eps_annual_latest": sec_t.get("eps_annual_latest", np.nan),
L987                 "eps_annual_prev": sec_t.get("eps_annual_prev", np.nan),
L988                 "rev_annual_latest": sec_t.get("rev_annual_latest", np.nan),
L989                 "rev_annual_prev": sec_t.get("rev_annual_prev", np.nan),
L990                 "eps_cagr3": sec_t.get("eps_cagr3", np.nan),
L991                 "rev_cagr3": sec_t.get("rev_cagr3", np.nan),
L992             })
L993         return self.impute_eps_ttm(pd.DataFrame(eps_rows).set_index("ticker"))
L994
L995     def prepare_data(self):
L996         """Fetch price and fundamental data for all tickers."""
L997         self.sec_dryrun_sample()
L998         # --- yfinance 用にティッカーを正規化（"$"剥がし、"."→"-"） ---
L999         def _to_yf(sym: str) -> str:
L1000             s = (sym or "").strip().lstrip("$").replace("＄", "")
L1001             # BRK.B / PBR.A などは Yahoo では '-' を使用
L1002             yf_sym = s.replace("．", ".").replace(".", "-")
L1003             return yf_sym or (sym or "")
L1004
L1005         cand_y = [_to_yf(t) for t in self.cand]
L1006         cand_info = yf.Tickers(" ".join(cand_y))
L1007
L1008         def _price(orig: str, ysym: str) -> float:
L1009             try:
L1010                 return cand_info.tickers[ysym].fast_info.get("lastPrice", np.inf)
L1011             except Exception as e:
L1012                 print(f"{orig}: price fetch failed ({e})")
L1013                 return np.inf
L1014
L1015         cand_prices = {orig: _price(orig, ysym) for orig, ysym in zip(self.cand, cand_y)}
L1016         can
```