```text
tna(g_score.get(t)) else '—' for t in out_list]
L621         d_list = [f"{d_score_all.get(t):.3f}" if pd.notna(d_score_all.get(t)) else '—' for t in out_list]
L622         self.io_table['GSC'] = pd.Series(g_list)
L623         self.io_table['DSC'] = pd.Series(d_list)
L624
L625         print("Changes:")
L626         print(self.io_table.to_string(index=False))
L627
L628         all_tickers = list(set(exist + list(top_G) + list(top_D) + [bench])); prices = yf.download(all_tickers, period='1y', auto_adjust=True, progress=False)['Close']
L629         ret = prices.pct_change(); portfolios = {'CUR':exist,'NEW':list(top_G)+list(top_D)}; metrics={}
L630         for name,ticks in portfolios.items():
L631             pr = ret[ticks].mean(axis=1, skipna=True).dropna(); cum = (1+pr).cumprod()-1; n = len(pr)
L632             if n>=252: ann_ret, ann_vol = (1+cum.iloc[-1])**(252/n)-1, pr.std()*np.sqrt(252)
L633             else: ann_ret, ann_vol = cum.iloc[-1], pr.std()*np.sqrt(n)
L634             sharpe, drawdown = ann_ret/ann_vol, (cum - cum.cummax()).min()
L635             if len(ticks)>=2:
L636                 C_raw = ret[ticks].corr(); RAW_rho = C_raw.mask(np.eye(len(ticks), dtype=bool)).stack().mean()
L637                 R = ret[ticks].dropna().to_numpy(); C_resid = Selector.residual_corr(R, n_pc=3, shrink=DRRS_SHRINK)
L638                 RESID_rho = float((C_resid.sum()-np.trace(C_resid))/(C_resid.shape[0]*(C_resid.shape[0]-1)))
L639             else: RAW_rho = RESID_rho = np.nan
L640             divy = ttm_div_yield_portfolio(ticks); metrics[name] = {'RET':ann_ret,'VOL':ann_vol,'SHP':sharpe,'MDD':drawdown,'RAWρ':RAW_rho,'RESIDρ':RESID_rho,'DIVY':divy}
L641         df_metrics = pd.DataFrame(metrics).T; df_metrics_pct = df_metrics.copy(); self.df_metrics = df_metrics
L642         for col in ['RET','VOL','MDD','DIVY']: df_metrics_pct[col] = df_metrics_pct[col]*100
L643         cols_order = ['RET','VOL','SHP','MDD','RAWρ','RESIDρ','DIVY']; df_metrics_pct = df_metrics_pct.reindex(columns=cols_order)
L644         def _fmt_row(s):
L645             return pd.Series({'RET':f"{s['RET']:.1f}%",'VOL':f"{s['VOL']:.1f}%",'SHP':f"{s['SHP']:.1f}",'MDD':f"{s['MDD']:.1f}%",'RAWρ':(f"{s['RAWρ']:.2f}" if pd.notna(s['RAWρ']) else "NaN"),'RESIDρ':(f"{s['RESIDρ']:.2f}" if pd.notna(s['RESIDρ']) else "NaN"),'DIVY':f"{s['DIVY']:.1f}%"})
L646         self.df_metrics_fmt = df_metrics_pct.apply(_fmt_row, axis=1); print("Performance Comparison:"); print(self.df_metrics_fmt.to_string())
L647         if self.debug:
L648             self.debug_table = pd.concat([df_z[['TR','EPS','REV','ROE','BETA','DIV','FCF','RS','TR_str','DIV_STREAK']], g_score.rename('GSC'), d_score_all.rename('DSC')], axis=1).round(3)
L649             print("Debug Data:"); print(self.debug_table.to_string())
L650
L651         # === 追加: GSC+DSC が低い順 TOP10 ===
L652         try:
L653             all_scores = pd.DataFrame({'GSC': df_z['GSC'], 'DSC': df_z['DSC']}).copy()
L654             all_scores['G_plus_D'] = all_scores['GSC'] + all_scores['DSC']
L655             all_scores = all_scores.dropna(subset=['G_plus_D'])
L656             self.low10_table = all_scores.sort_values('G_plus_D', ascending=True).head(10).round(3)
L657             print("Low Score Candidates (GSC+DSC bottom 10):")
L658             print(self.low10_table.to_string())
L659         except Exception as e:
L660             print(f"[warn] low-score ranking failed: {e}")
L661             self.low10_table = None
L662
L663     # --- Slack送信（元 notify_slack のロジックそのまま） ---
L664     def notify_slack(self):
L665         SLACK_WEBHOOK_URL = os.environ.get("SLACK_WEBHOOK_URL")
L666         if not SLACK_WEBHOOK_URL: raise ValueError("SLACK_WEBHOOK_URL not set (環境変数が未設定です)")
L667         def _filter_suffix_from(spec: dict, group: str) -> str:
L668             g = spec.get(group, {})
L669             parts = [str(m) for m in g.get("pre_mask", [])]
L670             for k, v in (g.get("pre_filter", {}) or {}).items():
L671                 base, op = (k[:-4], "<") if k.endswith("_max") else ((k[:-4], ">") if k.endswith("_min") else (k, "="))
L672                 name = {"beta": "β"}.get(base, base)
L673                 try: val = f"{float(v):g}"
L674                 except: val = str(v)
L675                 parts.append(f"{name}{op}{val}")
L676             return "" if not parts else " / filter:" + " & ".join(parts)
L677         def _inject_filter_suffix(title: str, group: str) -> str:
L678             suf = _filter_suffix_from(FILTER_SPEC, group)
L679             return f"{title[:-1]}{suf}]" if suf and title.endswith("]") else (title + suf)
L680         def _blk(title, tbl, fmt=None, drop=()):
L681             if tbl is None or getattr(tbl,'empty',False): return f"{title}\n(選定なし)\n"
L682             if drop and hasattr(tbl,'columns'):
L683                 keep = [c for c in tbl.columns if c not in drop]
L684                 tbl, fmt = tbl[keep], {k:v for k,v in (fmt or {}).items() if k in keep}
L685             return f"{title}\n```{tbl.to_string(formatters=fmt)}```\n"
L686
L687         g_title = _inject_filter_suffix(self.g_title, "G")
L688         d_title = _inject_filter_suffix(self.d_title, "D")
L689         message  = "📈 ファクター分散最適化の結果\n"
L690         if self.miss_df is not None and not self.miss_df.empty:
L691             message += "Missing Data\n```" + self.miss_df.to_string(index=False) + "```\n"
L692         message += _blk(g_title, self.g_table, self.g_formatters, drop=("TRD",))
L693         message += _blk(d_title, self.d_table, self.d_formatters)
L694         message += "Changes\n" + ("(変更なし)\n" if self.io_table is None or getattr(self.io_table,'empty',False) else f"```{self.io_table.to_string(index=False)}```\n")
L695         message += "Performance Comparison:\n```" + self.df_metrics_fmt.to_string() + "```"
L696         if self.debug and self.debug_table is not None:
L697             message += "\nDebug Data\n```" + self.debug_table.to_string() + "```"
L698         payload = {"text": message}
L699         try:
L700             resp = requests.post(SLACK_WEBHOOK_URL, json=payload); resp.raise_for_status(); print("✅ Slack（Webhook）へ送信しました")
L701         except Exception as e: print(f"⚠️ Slack通知エラー: {e}")
L702
L703 def _infer_g_universe(feature_df, selected12=None, near5=None):
L704     try:
L705         out = feature_df.index[feature_df['group'].astype(str).str.upper().eq('G')].tolist()
L706         if out: return out
L707     except Exception:
L708         pass
L709     base = set()
L710     for lst in (selected12 or []), (near5 or []):
L711         for x in (lst or []): base.add(x)
L712     return list(base) if base else list(feature_df.index)
L713
L714 def _fmt_with_fire_mark(tickers, feature_df):
L715     out = []
L716     for t in tickers or []:
L717         try:
L718             br = bool(feature_df.at[t, "G_BREAKOUT_recent_5d"])
L719             pb = bool(feature_df.at[t, "G_PULLBACK_recent_5d"])
L720             out.append(f"{t}{' 🔥' if (br or pb) else ''}")
L721         except Exception:
L722             out.append(t)
L723     return out
L724
L725 def _label_recent_event(t, feature_df):
L726     try:
L727         br = bool(feature_df.at[t, "G_BREAKOUT_recent_5d"]); dbr = str(feature_df.at[t, "G_BREAKOUT_last_date"]) if br else ""
L728         pb = bool(feature_df.at[t, "G_PULLBACK_recent_5d"]); dpb = str(feature_df.at[t, "G_PULLBACK_last_date"]) if pb else ""
L729         if   br and not pb: return f"{t}（ブレイクアウト確定 {dbr}）"
L730         elif pb and not br: return f"{t}（押し目反発 {dpb}）"
L731         elif br and pb:     return f"{t}（ブレイクアウト確定 {dbr}／押し目反発 {dpb}）"
L732     except Exception:
L733         pass
L734     return t
L735
L736 # === パイプライン可視化：G/D共通フロー（出力は不変） ===
L737
L738 def io_build_input_bundle() -> InputBundle:
L739     """
L740     既存の『データ取得→前処理』を実行し、InputBundle を返す。
L741     処理内容・列名・丸め・例外・ログ文言は現行どおり（変更禁止）。
L742     """
L743     state = Input(cand=cand, exist=exist, bench=bench, price_max=CAND_PRICE_MAX, finnhub_api_key=FINNHUB_API_KEY).prepare_data()
L744     return InputBundle(cand=state["cand"], tickers=state["tickers"], bench=bench, data=state["data"], px=state["px"], spx=state["spx"], tickers_bulk=state["tickers_bulk"], info=state["info"], eps_df=state["eps_df"], fcf_df=state["fcf_df"], returns=state["returns"])
L745
L746 def run_group(sc: Scorer, group: str, inb: InputBundle, cfg: PipelineConfig,
L747               n_target: int) -> tuple[list, float, float, float]:
L748     """
L749     G/Dを同一手順で処理：採点→フィルター→選定（相関低減込み）。
L750     戻り値：(pick, avg_res_corr, sum_score, objective)
L751     JSON保存は既存フォーマット（キー名・丸め桁・順序）を踏襲。
L752     """
L753     sc.cfg = cfg
L754
L755     if hasattr(sc, "score_build_features"):
L756         feat = sc.score_build_features(inb)
L757         if not hasattr(sc, "_feat_logged"):
L758             T.log("features built (scorer)")
L759             sc._feat_logged = True
L760         agg = sc.score_aggregate(feat, group, cfg) if hasattr(sc, "score_aggregate") else feat
L761     else:
L762         fb = sc.aggregate_scores(inb, cfg)
L763         if not hasattr(sc, "_feat_logged"):
L764             T.log("features built (scorer)")
L765             sc._feat_logged = True
L766         sc._feat = fb
L767         agg = fb.g_score if group == "G" else fb.d_score_all
L768         if group == "D" and hasattr(fb, "df"):
L769             agg = agg[fb.df['BETA'] < D_BETA_MAX]
L770
L771     if hasattr(sc, "filter_candidates"):
L772         agg = agg[sc.filter_candidates(inb, agg, group, cfg)]
L773
L774     selector = Selector()
L775     if hasattr(sc, "select_diversified"):
L776         pick, avg_r, sum_sc, obj = sc.select_diversified(agg, group, cfg, n_target,
L777             selector=selector, prev_tickers=None,
L778             corrM=cfg.drrs.corrM, shrink=cfg.drrs.shrink,
L779             cross_mu=cfg.drrs.cross_mu_gd)
L780     else:
L781         if group == "G":
L782             init = agg.nlargest(min(cfg.drrs.corrM, len(agg))).index.tolist()
L783             res = selector.select_bucket_drrs(returns_df=inb.returns, score_ser=agg, pool_tickers=init, k=n_target,
L784                 n_pc=cfg.drrs.G.get("n_pc", 3), gamma=cfg.drrs.G.get("gamma", 1.2),
L785                 lam=cfg.drrs.G.get("lam", 0.68),
L786                 lookback=cfg.drrs.G.get("lookback", 252),
L787                 shrink=cfg.drrs.shrink, g_fixed_tickers=None, mu=0.0)
L788         else:
L789             init = agg.nlargest(min(cfg.drrs.corrM, len(agg))).index.tolist()
L790             g_fixed = getattr(sc, "_top_G", None)
L791             res = selector.select_bucket_drrs(returns_df=inb.returns, score_ser=agg, pool_tickers=init, k=n_target,
L792                 n_pc=cfg.drrs.D.get("n_pc", 4), gamma=cfg.drrs.D.get("gamma", 0.8),
L793                 lam=cfg.drrs.D.get("lam", 0.85),
L794                 lookback=cfg.drrs.D.get("lookback", 504),
L795                 shrink=cfg.drrs.shrink, g_fixed_tickers=g_fixed,
L796                 mu=cfg.drrs.cross_mu_gd)
L797         pick = res["tickers"]; avg_r = res["avg_res_corr"]
L798         sum_sc = res["sum_score"]; obj = res["objective"]
L799         if group == "D":
L800             _, pick = _disjoint_keepG(getattr(sc, "_top_G", []), pick, init)
L801             T.log("selection finalized (G/D)")
L802     try:
L803         inc = [t for t in exist if t in agg.index]
L804         pick = _sticky_keep_current(
L805             agg=agg, pick=pick, incumbents=inc, n_target=n_target,
L806             delta_z=SWAP_DELTA_Z, keep_buffer=SWAP_KEEP_BUFFER
L807         )
L808     except Exception as _e:
L809         print(f"[warn] sticky_keep_current skipped: {str(_e)}")
L810     # --- Near-Miss: 惜しくも選ばれなかった上位10を保持（Slack表示用） ---
L811     # 5) Near-Miss と最終集計Seriesを保持（表示専用。計算へ影響なし）
L812     try:
L813         pool = agg.drop(index=[t for t in pick if t in agg.index], errors="ignore")
L814         near10 = list(pool.sort_values(ascending=False).head(10).index)
L815         setattr(sc, f"_near_{group}", near10)
L816         setattr(sc, f"_agg_{group}", ag
```