```text
lf.g_title = (f"[G枠 / {N_G} / {_fmt_w(g_weights)} / corrM={corrM} / "
L657                         f"LB={DRRS_G['lookback']} nPC={DRRS_G['n_pc']} γ={DRRS_G['gamma']} λ={DRRS_G['lam']} η={DRRS_G['eta']} shrink={DRRS_SHRINK}]")
L658         if near_G:
L659             add = [t for t in near_G if t not in set(G_UNI)][:10]
L660             if len(add) < 10:
L661                 try:
L662                     aggG = getattr(sc, "_agg_G", pd.Series(dtype=float)).sort_values(ascending=False)
L663                     out_now = sorted(set(exist) - set(top_G + top_D))  # 今回 OUT
L664                     used = set(G_UNI + add)
L665                     def _push(lst):
L666                         nonlocal add, used
L667                         for t in lst:
L668                             if len(add) == 10: break
L669                             if t in aggG.index and t not in used:
L670                                 add.append(t); used.add(t)
L671                     _push(out_now)           # ① 今回 OUT を優先
L672                     _push(list(aggG.index))  # ② まだ足りなければ上位で充填
L673                 except Exception:
L674                     pass
L675             if add:
L676                 near_tbl = pd.concat([df_z.loc[add,['GROWTH_F','MOM','TRD','VOL']], pd.Series({t: g_score.get(t) for t in add}, name='GSC')], axis=1)
L677                 self.g_table = pd.concat([self.g_table, near_tbl], axis=0)
L678         print(self.g_title); print(self.g_table.to_string(formatters=self.g_formatters))
L679
L680         extra_D = [t for t in init_D if t not in top_D][:5]; D_UNI = top_D + extra_D
L681         cols_D = ['QAL','YLD','VOL','TRD']; d_disp = pd.DataFrame(index=D_UNI)
L682         d_disp['QAL'], d_disp['YLD'], d_disp['VOL'], d_disp['TRD'] = df_z.loc[D_UNI,'D_QAL'], df_z.loc[D_UNI,'D_YLD'], df_z.loc[D_UNI,'D_VOL_RAW'], df_z.loc[D_UNI,'D_TRD']
L683         dsc_series = pd.Series({t: d_score_all.get(t) for t in D_UNI}, name='DSC')
L684         self.d_table = pd.concat([d_disp, dsc_series], axis=1); self.d_table.index = [t + ("⭐️" if t in top_D else "") for t in D_UNI]
L685         self.d_formatters = {col:"{:.2f}".format for col in cols_D}; self.d_formatters['DSC']="{:.3f}".format
L686         import scorer
L687         dw_eff = scorer.D_WEIGHTS_EFF
L688         self.d_title = (f"[D枠 / {N_D} / {_fmt_w(dw_eff)} / corrM={corrM} / "
L689                         f"LB={DRRS_D['lookback']} nPC={DRRS_D['n_pc']} γ={DRRS_D['gamma']} λ={DRRS_D['lam']} μ={CROSS_MU_GD} η={DRRS_D['eta']} shrink={DRRS_SHRINK}]")
L690         if near_D:
L691             add = [t for t in near_D if t not in set(D_UNI)][:10]
L692             if add:
L693                 d_disp2 = pd.DataFrame(index=add)
L694                 d_disp2['QAL'], d_disp2['YLD'], d_disp2['VOL'], d_disp2['TRD'] = df_z.loc[add,'D_QAL'], df_z.loc[add,'D_YLD'], df_z.loc[add,'D_VOL_RAW'], df_z.loc[add,'D_TRD']
L695                 near_tbl = pd.concat([d_disp2, pd.Series({t: d_score_all.get(t) for t in add}, name='DSC')], axis=1)
L696                 self.d_table = pd.concat([self.d_table, near_tbl], axis=0)
L697         print(self.d_title); print(self.d_table.to_string(formatters=self.d_formatters))
L698
L699         # === Changes（IN の GSC/DSC を表示。OUT は銘柄名のみ） ===
L700         in_list = sorted(set(list(top_G)+list(top_D)) - set(exist))
L701         out_list = sorted(set(exist) - set(list(top_G)+list(top_D)))
L702
L703         self.io_table = pd.DataFrame({
L704             'IN': pd.Series(in_list),
L705             '/ OUT': pd.Series(out_list)
L706         })
L707         g_list = [f"{g_score.get(t):.3f}" if pd.notna(g_score.get(t)) else '—' for t in out_list]
L708         d_list = [f"{d_score_all.get(t):.3f}" if pd.notna(d_score_all.get(t)) else '—' for t in out_list]
L709         self.io_table['GSC'] = pd.Series(g_list)
L710         self.io_table['DSC'] = pd.Series(d_list)
L711
L712         print("Changes:")
L713         print(self.io_table.to_string(index=False))
L714
L715         all_tickers = list(set(exist + list(top_G) + list(top_D) + [bench])); prices = yf.download(all_tickers, period='1y', auto_adjust=True, progress=False, threads=False)['Close'].ffill(limit=2)
L716         ret = prices.pct_change(); portfolios = {'CUR':exist,'NEW':list(top_G)+list(top_D)}; metrics={}
L717         for name,ticks in portfolios.items():
L718             pr = ret[ticks].mean(axis=1, skipna=True).dropna(); cum = (1+pr).cumprod()-1; n = len(pr)
L719             if n>=252: ann_ret, ann_vol = (1+cum.iloc[-1])**(252/n)-1, pr.std()*np.sqrt(252)
L720             else: ann_ret, ann_vol = cum.iloc[-1], pr.std()*np.sqrt(n)
L721             sharpe, drawdown = ann_ret/ann_vol, (cum - cum.cummax()).min()
L722             if len(ticks)>=2:
L723                 C_raw = ret[ticks].corr(); RAW_rho = C_raw.mask(np.eye(len(ticks), dtype=bool)).stack().mean()
L724                 R = ret[ticks].dropna().to_numpy(); C_resid = Selector.residual_corr(R, n_pc=3, shrink=DRRS_SHRINK)
L725                 RESID_rho = float((C_resid.sum()-np.trace(C_resid))/(C_resid.shape[0]*(C_resid.shape[0]-1)))
L726             else: RAW_rho = RESID_rho = np.nan
L727             divy = ttm_div_yield_portfolio(ticks); metrics[name] = {'RET':ann_ret,'VOL':ann_vol,'SHP':sharpe,'MDD':drawdown,'RAWρ':RAW_rho,'RESIDρ':RESID_rho,'DIVY':divy}
L728         df_metrics = pd.DataFrame(metrics).T; df_metrics_pct = df_metrics.copy(); self.df_metrics = df_metrics
L729         for col in ['RET','VOL','MDD','DIVY']: df_metrics_pct[col] = df_metrics_pct[col]*100
L730         cols_order = ['RET','VOL','SHP','MDD','RAWρ','RESIDρ','DIVY']; df_metrics_pct = df_metrics_pct.reindex(columns=cols_order)
L731         def _fmt_row(s):
L732             return pd.Series({'RET':f"{s['RET']:.1f}%",'VOL':f"{s['VOL']:.1f}%",'SHP':f"{s['SHP']:.1f}",'MDD':f"{s['MDD']:.1f}%",'RAWρ':(f"{s['RAWρ']:.2f}" if pd.notna(s['RAWρ']) else "NaN"),'RESIDρ':(f"{s['RESIDρ']:.2f}" if pd.notna(s['RESIDρ']) else "NaN"),'DIVY':f"{s['DIVY']:.1f}%"})
L733         self.df_metrics_fmt = df_metrics_pct.apply(_fmt_row, axis=1); print("Performance Comparison:"); print(self.df_metrics_fmt.to_string())
L734         # --- ここから: デバッグ出力は _compact_debug で一本化（表示経路もSlack経路もこれだけ）---
L735         if self.debug:
L736             from types import SimpleNamespace
L737             fb_like = SimpleNamespace(
L738                 df_z=df_z,
L739                 df_full_z=kwargs.get("df_full_z"),
L740                 g_score=g_score,
L741                 d_score_all=d_score_all,
L742                 missing_logs=self.miss_df,
L743             )
L744             sb_like = SimpleNamespace(top_G=top_G, top_D=top_D)
L745             self.debug_text = _compact_debug(
L746                 fb_like,
L747                 sb_like,
L748                 prevG=kwargs.get("prev_G", exist),
L749                 prevD=kwargs.get("prev_D", exist),
L750                 max_rows=int(os.getenv("DEBUG_MAX_ROWS", "140")),
L751             )
L752             print(self.debug_text)
L753         else:
L754             self.debug_text = ""
L755         # === 追加: GSC+DSC が低い順 TOP10 ===
L756         try:
L757             all_scores = pd.DataFrame({'GSC': df_z['GSC'], 'DSC': df_z['DSC']}).copy()
L758             all_scores['G_plus_D'] = all_scores['GSC'] + all_scores['DSC']
L759             all_scores = all_scores.dropna(subset=['G_plus_D'])
L760             self.low10_table = all_scores.sort_values('G_plus_D', ascending=True).head(10).round(3)
L761             print("Low Score Candidates (GSC+DSC bottom 10):")
L762             print(self.low10_table.to_string())
L763         except Exception as e:
L764             print(f"[warn] low-score ranking failed: {e}")
L765             self.low10_table = None
L766
L767     # --- Slack送信（元 notify_slack のロジックそのまま） ---
L768     def notify_slack(self):
L769         SLACK_WEBHOOK_URL = os.environ.get("SLACK_WEBHOOK_URL")
L770         if not SLACK_WEBHOOK_URL: raise ValueError("SLACK_WEBHOOK_URL not set (環境変数が未設定です)")
L771         def _filter_suffix_from(spec: dict, group: str) -> str:
L772             g = spec.get(group, {})
L773             parts = [str(m) for m in g.get("pre_mask", [])]
L774             for k, v in (g.get("pre_filter", {}) or {}).items():
L775                 base, op = (k[:-4], "<") if k.endswith("_max") else ((k[:-4], ">") if k.endswith("_min") else (k, "="))
L776                 name = {"beta": "β"}.get(base, base)
L777                 try: val = f"{float(v):g}"
L778                 except: val = str(v)
L779                 parts.append(f"{name}{op}{val}")
L780             return "" if not parts else " / filter:" + " & ".join(parts)
L781         def _inject_filter_suffix(title: str, group: str) -> str:
L782             suf = _filter_suffix_from(FILTER_SPEC, group)
L783             return f"{title[:-1]}{suf}]" if suf and title.endswith("]") else (title + suf)
L784         def _blk(title, tbl, fmt=None, drop=()):
L785             if tbl is None or getattr(tbl,'empty',False): return f"{title}\n(選定なし)\n"
L786             if drop and hasattr(tbl,'columns'):
L787                 keep = [c for c in tbl.columns if c not in drop]
L788                 tbl, fmt = tbl[keep], {k:v for k,v in (fmt or {}).items() if k in keep}
L789             return f"{title}\n```{tbl.to_string(formatters=fmt)}```\n"
L790
L791         g_title = _inject_filter_suffix(self.g_title, "G")
L792         d_title = _inject_filter_suffix(self.d_title, "D")
L793         message  = "📈 ファクター分散最適化の結果\n"
L794         if self.miss_df is not None and not self.miss_df.empty:
L795             message += "Missing Data\n```" + self.miss_df.to_string(index=False) + "```\n"
L796         message += _blk(g_title, self.g_table, self.g_formatters, drop=("TRD",))
L797         message += _blk(d_title, self.d_table, self.d_formatters)
L798         message += "Changes\n" + ("(変更なし)\n" if self.io_table is None or getattr(self.io_table,'empty',False) else f"```{self.io_table.to_string(index=False)}```\n")
L799         message += "Performance Comparison:\n```" + self.df_metrics_fmt.to_string() + "```"
L800         if self.debug and getattr(self, "debug_text", ""):
L801             message += "\n```" + self.debug_text + "```"
L802         payload = {"text": message}
L803         try:
L804             resp = requests.post(SLACK_WEBHOOK_URL, json=payload); resp.raise_for_status(); print("✅ Slack（Webhook）へ送信しました")
L805         except Exception as e: print(f"⚠️ Slack通知エラー: {e}")
L806
L807 def _infer_g_universe(feature_df, selected12=None, near5=None):
L808     try:
L809         out = feature_df.index[feature_df['group'].astype(str).str.upper().eq('G')].tolist()
L810         if out: return out
L811     except Exception:
L812         pass
L813     base = set()
L814     for lst in (selected12 or []), (near5 or []):
L815         for x in (lst or []): base.add(x)
L816     return list(base) if base else list(feature_df.index)
L817
L818 def _fmt_with_fire_mark(tickers, feature_df):
L819     out = []
L820     for t in tickers or []:
L821         try:
L822             br = bool(feature_df.at[t, "G_BREAKOUT_recent_5d"])
L823             pb = bool(feature_df.at[t, "G_PULLBACK_recent_5d"])
L824             out.append(f"{t}{' 🔥' if (br or pb) else ''}")
L825         except Exception:
L826             out.append(t)
L827     return out
L828
L829 def _label_recent_event(t, feature_df):
L830     try:
L831         br = bool(feature_df.at[t, "G_BREAKOUT_recent_5d"]); dbr = str(feature_df.at[t, "G_BREAKOUT_last_date"]) if br else ""
L832         pb = bool(feature_df.at[t, "G_PULLBACK_recent_5d"]); dpb = str(feature_df.at[t, "G_PULLBACK_last_date"]) if pb else ""
L833         if   br and not pb: return f"{t}（ブレイクアウト確定 {dbr}）"
L834         elif pb and not br: return f"{t}（押し目反発 {dpb}）"
L835         elif br and pb:     return f"{t}（ブレイクアウト確定 {dbr}／押し目反発 {dpb}）"
L836     except Exception:
L837         pass
L838     return t
L839
L840 # === パイプライン可視化：G/D共通フロー（出力は不変） ===
L841
L842 def io_build_input_bundle() -> InputBundle:
L843     """
L84
```