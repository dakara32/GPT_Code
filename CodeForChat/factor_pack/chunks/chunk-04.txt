```text
e_all)
L668         near_G = getattr(sc, "_near_G", []) if sc else []
L669         near_D = getattr(sc, "_near_D", []) if sc else []
L670
L671         extra_G = [t for t in init_G if t not in top_G][:5]; G_UNI = top_G + extra_G
L672         gsc_series = pd.Series({t: g_score.get(t) for t in G_UNI}, name='GSC')
L673         self.g_table = pd.concat([df_z.loc[G_UNI,['GROWTH_F','MOM','TRD','VOL']], gsc_series], axis=1)
L674         self.g_table.index = [t + ("⭐️" if t in top_G else "") for t in G_UNI]
L675         self.g_formatters = {col:"{:.2f}".format for col in ['GROWTH_F','MOM','TRD','VOL']}; self.g_formatters['GSC'] = "{:.3f}".format
L676         self.g_title = (f"[G枠 / {N_G} / {_fmt_w(g_weights)} / corrM={corrM} / "
L677                         f"LB={DRRS_G['lookback']} nPC={DRRS_G['n_pc']} γ={DRRS_G['gamma']} λ={DRRS_G['lam']} η={DRRS_G['eta']} shrink={DRRS_SHRINK}]")
L678         if near_G:
L679             add = [t for t in near_G if t not in set(G_UNI)][:10]
L680             if len(add) < 10:
L681                 try:
L682                     aggG = getattr(sc, "_agg_G", pd.Series(dtype=float)).sort_values(ascending=False)
L683                     out_now = sorted(set(exist) - set(top_G + top_D))  # 今回 OUT
L684                     used = set(G_UNI + add)
L685                     def _push(lst):
L686                         nonlocal add, used
L687                         for t in lst:
L688                             if len(add) == 10: break
L689                             if t in aggG.index and t not in used:
L690                                 add.append(t); used.add(t)
L691                     _push(out_now)           # ① 今回 OUT を優先
L692                     _push(list(aggG.index))  # ② まだ足りなければ上位で充填
L693                 except Exception:
L694                     pass
L695             if add:
L696                 near_tbl = pd.concat([df_z.loc[add,['GROWTH_F','MOM','TRD','VOL']], pd.Series({t: g_score.get(t) for t in add}, name='GSC')], axis=1)
L697                 self.g_table = pd.concat([self.g_table, near_tbl], axis=0)
L698         print(self.g_title); print(self.g_table.to_string(formatters=self.g_formatters))
L699
L700         extra_D = [t for t in init_D if t not in top_D][:5]; D_UNI = top_D + extra_D
L701         cols_D = ['QAL','YLD','VOL','TRD']; d_disp = pd.DataFrame(index=D_UNI)
L702         d_disp['QAL'], d_disp['YLD'], d_disp['VOL'], d_disp['TRD'] = df_z.loc[D_UNI,'D_QAL'], df_z.loc[D_UNI,'D_YLD'], df_z.loc[D_UNI,'D_VOL_RAW'], df_z.loc[D_UNI,'D_TRD']
L703         dsc_series = pd.Series({t: d_score_all.get(t) for t in D_UNI}, name='DSC')
L704         self.d_table = pd.concat([d_disp, dsc_series], axis=1); self.d_table.index = [t + ("⭐️" if t in top_D else "") for t in D_UNI]
L705         self.d_formatters = {col:"{:.2f}".format for col in cols_D}; self.d_formatters['DSC']="{:.3f}".format
L706         import scorer
L707         dw_eff = scorer.D_WEIGHTS_EFF
L708         self.d_title = (f"[D枠 / {N_D} / {_fmt_w(dw_eff)} / corrM={corrM} / "
L709                         f"LB={DRRS_D['lookback']} nPC={DRRS_D['n_pc']} γ={DRRS_D['gamma']} λ={DRRS_D['lam']} μ={CROSS_MU_GD} η={DRRS_D['eta']} shrink={DRRS_SHRINK}]")
L710         if near_D:
L711             add = [t for t in near_D if t not in set(D_UNI)][:10]
L712             if add:
L713                 d_disp2 = pd.DataFrame(index=add)
L714                 d_disp2['QAL'], d_disp2['YLD'], d_disp2['VOL'], d_disp2['TRD'] = df_z.loc[add,'D_QAL'], df_z.loc[add,'D_YLD'], df_z.loc[add,'D_VOL_RAW'], df_z.loc[add,'D_TRD']
L715                 near_tbl = pd.concat([d_disp2, pd.Series({t: d_score_all.get(t) for t in add}, name='DSC')], axis=1)
L716                 self.d_table = pd.concat([self.d_table, near_tbl], axis=0)
L717         print(self.d_title); print(self.d_table.to_string(formatters=self.d_formatters))
L718
L719         # === Changes（IN の GSC/DSC を表示。OUT は銘柄名のみ） ===
L720         in_list = sorted(set(list(top_G)+list(top_D)) - set(exist))
L721         out_list = sorted(set(exist) - set(list(top_G)+list(top_D)))
L722
L723         self.io_table = pd.DataFrame({
L724             'IN': pd.Series(in_list),
L725             '/ OUT': pd.Series(out_list)
L726         })
L727         g_list = [f"{g_score.get(t):.3f}" if pd.notna(g_score.get(t)) else '—' for t in out_list]
L728         d_list = [f"{d_score_all.get(t):.3f}" if pd.notna(d_score_all.get(t)) else '—' for t in out_list]
L729         self.io_table['GSC'] = pd.Series(g_list)
L730         self.io_table['DSC'] = pd.Series(d_list)
L731
L732         print("Changes:")
L733         print(self.io_table.to_string(index=False))
L734
L735         all_tickers = list(set(exist + list(top_G) + list(top_D) + [bench])); prices = yf.download(all_tickers, period='1y', auto_adjust=True, progress=False, threads=False)['Close'].ffill(limit=2)
L736         ret = prices.pct_change(); portfolios = {'CUR':exist,'NEW':list(top_G)+list(top_D)}; metrics={}
L737         for name,ticks in portfolios.items():
L738             pr = ret[ticks].mean(axis=1, skipna=True).dropna(); cum = (1+pr).cumprod()-1; n = len(pr)
L739             if n>=252: ann_ret, ann_vol = (1+cum.iloc[-1])**(252/n)-1, pr.std()*np.sqrt(252)
L740             else: ann_ret, ann_vol = cum.iloc[-1], pr.std()*np.sqrt(n)
L741             sharpe, drawdown = ann_ret/ann_vol, (cum - cum.cummax()).min()
L742             if len(ticks)>=2:
L743                 C_raw = ret[ticks].corr(); RAW_rho = C_raw.mask(np.eye(len(ticks), dtype=bool)).stack().mean()
L744                 R = ret[ticks].dropna().to_numpy(); C_resid = Selector.residual_corr(R, n_pc=3, shrink=DRRS_SHRINK)
L745                 RESID_rho = float((C_resid.sum()-np.trace(C_resid))/(C_resid.shape[0]*(C_resid.shape[0]-1)))
L746             else: RAW_rho = RESID_rho = np.nan
L747             divy = ttm_div_yield_portfolio(ticks); metrics[name] = {'RET':ann_ret,'VOL':ann_vol,'SHP':sharpe,'MDD':drawdown,'RAWρ':RAW_rho,'RESIDρ':RESID_rho,'DIVY':divy}
L748         df_metrics = pd.DataFrame(metrics).T; df_metrics_pct = df_metrics.copy(); self.df_metrics = df_metrics
L749         for col in ['RET','VOL','MDD','DIVY']: df_metrics_pct[col] = df_metrics_pct[col]*100
L750         cols_order = ['RET','VOL','SHP','MDD','RAWρ','RESIDρ','DIVY']; df_metrics_pct = df_metrics_pct.reindex(columns=cols_order)
L751         def _fmt_row(s):
L752             return pd.Series({'RET':f"{s['RET']:.1f}%",'VOL':f"{s['VOL']:.1f}%",'SHP':f"{s['SHP']:.1f}",'MDD':f"{s['MDD']:.1f}%",'RAWρ':(f"{s['RAWρ']:.2f}" if pd.notna(s['RAWρ']) else "NaN"),'RESIDρ':(f"{s['RESIDρ']:.2f}" if pd.notna(s['RESIDρ']) else "NaN"),'DIVY':f"{s['DIVY']:.1f}%"})
L753         self.df_metrics_fmt = df_metrics_pct.apply(_fmt_row, axis=1); print("Performance Comparison:"); print(self.df_metrics_fmt.to_string())
L754         # === 追加: GSC+DSC が低い順 TOP10 ===
L755         try:
L756             all_scores = pd.DataFrame({'GSC': df_z['GSC'], 'DSC': df_z['DSC']}).copy()
L757             all_scores['G_plus_D'] = all_scores['GSC'] + all_scores['DSC']
L758             all_scores = all_scores.dropna(subset=['G_plus_D'])
L759             self.low10_table = all_scores.sort_values('G_plus_D', ascending=True).head(10).round(3)
L760             print("Low Score Candidates (GSC+DSC bottom 10):")
L761             print(self.low10_table.to_string())
L762         except Exception as e:
L763             print(f"[warn] low-score ranking failed: {e}")
L764             self.low10_table = None
L765         # --- ここから: デバッグ出力は _compact_debug で一本化（表示経路もSlack経路もこれだけ）---
L766         if debug_mode:
L767             from types import SimpleNamespace
L768
L769             df_full_src    = getattr(getattr(self, "_sc", None), "_feat", None)
L770             df_full        = getattr(df_full_src, "df_full", None) or kwargs.get("df_full")
L771             df_full_z_pass = getattr(df_full_src, "df_full_z", None) or kwargs.get("df_full_z")
L772
L773             fb_like = SimpleNamespace(
L774                 df_full=df_full,
L775                 df_z=df_z,
L776                 df_full_z=df_full_z_pass,
L777                 g_score=g_score,
L778                 d_score_all=d_score_all,
L779                 missing_logs=self.miss_df,
L780             )
L781             sb_like = SimpleNamespace(top_G=top_G, top_D=top_D)
L782
L783             self.debug_text = _compact_debug(
L784                 fb_like,
L785                 sb_like,
L786                 prevG=kwargs.get("prev_G", exist),
L787                 prevD=kwargs.get("prev_D", exist),
L788                 max_rows=int(os.getenv("DEBUG_MAX_ROWS", "120")),
L789             )
L790
L791             if not (self.debug_text or "").strip():
L792                 src_df = df_full or df_full_z_pass or df_z
L793                 if src_df is not None and getattr(src_df, "empty", False) is False:
L794                     core = [c for c in [
L795                         "GRW","TR_EPS","TR_REV","EPS_Q_YOY","REV_Q_YOY","REV_YOY_ACC",
L796                         "RULE40","FCF_MGN","GSC","DSC"
L797                     ] if c in src_df.columns]
L798                     if core:
L799                         pick: list[str] = []
L800                         for t in (self.g_table, self.d_table, self.low10_table):
L801                             if t is not None and getattr(t, "empty", False) is False:
L802                                 pick += [i for i in list(t.index) if i in src_df.index]
L803                         view = (src_df.loc[pick, core] if pick else src_df[core]).head(int(os.getenv("DEBUG_MAX_ROWS", "80")))
L804                         self.debug_text = "DEBUG fallback\n" + view.to_string()
L805                     else:
L806                         self.debug_text = "(no debug columns)"
L807                 else:
L808                     self.debug_text = "(no dataframe)"
L809         else:
L810             self.debug_text = ""
L811         # Slack側で分割送信するためコンソールには出力しない
L812         # if debug_mode and self.debug_text:
L813         #     print(self.debug_text)
L814
L815     # --- Slack送信（元 notify_slack のロジックそのまま） ---
L816     def notify_slack(self):
L817         SLACK_WEBHOOK_URL = os.environ.get("SLACK_WEBHOOK_URL")
L818         if not SLACK_WEBHOOK_URL:
L819             raise ValueError("SLACK_WEBHOOK_URL not set")
L820
L821         def _filter_suffix_from(spec: dict, group: str) -> str:
L822             g = spec.get(group, {})
L823             parts = [str(m) for m in g.get("pre_mask", [])]
L824             for k, v in (g.get("pre_filter", {}) or {}).items():
L825                 base, op = (k[:-4], "<") if k.endswith("_max") else ((k[:-4], ">") if k.endswith("_min") else (k, "="))
L826                 name = {"beta": "β"}.get(base, base)
L827                 try:
L828                     val = f"{float(v):g}"
L829                 except Exception:
L830                     val = str(v)
L831                 parts.append(f"{name}{op}{val}")
L832             return "" if not parts else " / filter:" + " & ".join(parts)
L833
L834         def _inject_filter_suffix(title: str, group: str) -> str:
L835             suf = _filter_suffix_from(FILTER_SPEC, group)
L836             return f"{title[:-1]}{suf}]" if suf and title.endswith("]") else (title + suf)
L837
L838         def _blk(title, tbl, fmt=None, drop=()):
L839             if tbl is None or getattr(tbl, 'empty', False):
L840                 return f"{title}\n(選定なし)\n"
L841             if drop and hasattr(tbl, 'columns'):
L842                 keep = [c for c in tbl.columns if c not in drop]
L843                 tbl, fmt = tbl[keep], {k: v for k, v in (fmt or {}).items() if k in keep}
L844             return f"{title}\n```{tbl.to_string(formatters=fmt)}```\n"
L845
L846         message = "📈 ファクター分散最適化の結果\n"
L847         if self.miss_df is not None and not self.miss_df.empty:
L848             message += "Missing Data\n```" + self.miss_df.to_string(index=False) + "```\n"
L849         message += _blk(_inject_filter_suffix(self.g_title, "G"), self.g_table, self.g_
```