```text
, g_score)
L668         d_score_all = _SeriesProxy(agg_D, d_score_all)
L669         near_G = getattr(sc, "_near_G", []) if sc else []
L670         near_D = getattr(sc, "_near_D", []) if sc else []
L671
L672         extra_G = [t for t in init_G if t not in top_G][:5]; G_UNI = top_G + extra_G
L673         gsc_series = pd.Series({t: g_score.get(t) for t in G_UNI}, name='GSC')
L674         self.g_table = pd.concat([df_z.loc[G_UNI,['GROWTH_F','MOM','TRD','VOL']], gsc_series], axis=1)
L675         self.g_table.index = [t + ("⭐️" if t in top_G else "") for t in G_UNI]
L676         self.g_formatters = {col:"{:.2f}".format for col in ['GROWTH_F','MOM','TRD','VOL']}; self.g_formatters['GSC'] = "{:.3f}".format
L677         self.g_title = (f"[G枠 / {N_G} / {_fmt_w(g_weights)} / corrM={corrM} / "
L678                         f"LB={DRRS_G['lookback']} nPC={DRRS_G['n_pc']} γ={DRRS_G['gamma']} λ={DRRS_G['lam']} η={DRRS_G['eta']} shrink={DRRS_SHRINK}]")
L679         if near_G:
L680             add = [t for t in near_G if t not in set(G_UNI)][:10]
L681             if len(add) < 10:
L682                 try:
L683                     aggG = getattr(sc, "_agg_G", pd.Series(dtype=float)).sort_values(ascending=False)
L684                     out_now = sorted(set(exist) - set(top_G + top_D))  # 今回 OUT
L685                     used = set(G_UNI + add)
L686                     def _push(lst):
L687                         nonlocal add, used
L688                         for t in lst:
L689                             if len(add) == 10: break
L690                             if t in aggG.index and t not in used:
L691                                 add.append(t); used.add(t)
L692                     _push(out_now)           # ① 今回 OUT を優先
L693                     _push(list(aggG.index))  # ② まだ足りなければ上位で充填
L694                 except Exception:
L695                     pass
L696             if add:
L697                 near_tbl = pd.concat([df_z.loc[add,['GROWTH_F','MOM','TRD','VOL']], pd.Series({t: g_score.get(t) for t in add}, name='GSC')], axis=1)
L698                 self.g_table = pd.concat([self.g_table, near_tbl], axis=0)
L699         print(self.g_title); print(self.g_table.to_string(formatters=self.g_formatters))
L700
L701         extra_D = [t for t in init_D if t not in top_D][:5]; D_UNI = top_D + extra_D
L702         cols_D = ['QAL','YLD','VOL','TRD']; d_disp = pd.DataFrame(index=D_UNI)
L703         d_disp['QAL'], d_disp['YLD'], d_disp['VOL'], d_disp['TRD'] = df_z.loc[D_UNI,'D_QAL'], df_z.loc[D_UNI,'D_YLD'], df_z.loc[D_UNI,'D_VOL_RAW'], df_z.loc[D_UNI,'D_TRD']
L704         dsc_series = pd.Series({t: d_score_all.get(t) for t in D_UNI}, name='DSC')
L705         self.d_table = pd.concat([d_disp, dsc_series], axis=1); self.d_table.index = [t + ("⭐️" if t in top_D else "") for t in D_UNI]
L706         self.d_formatters = {col:"{:.2f}".format for col in cols_D}; self.d_formatters['DSC']="{:.3f}".format
L707         import scorer
L708         dw_eff = scorer.D_WEIGHTS_EFF
L709         self.d_title = (f"[D枠 / {N_D} / {_fmt_w(dw_eff)} / corrM={corrM} / "
L710                         f"LB={DRRS_D['lookback']} nPC={DRRS_D['n_pc']} γ={DRRS_D['gamma']} λ={DRRS_D['lam']} μ={CROSS_MU_GD} η={DRRS_D['eta']} shrink={DRRS_SHRINK}]")
L711         if near_D:
L712             add = [t for t in near_D if t not in set(D_UNI)][:10]
L713             if add:
L714                 d_disp2 = pd.DataFrame(index=add)
L715                 d_disp2['QAL'], d_disp2['YLD'], d_disp2['VOL'], d_disp2['TRD'] = df_z.loc[add,'D_QAL'], df_z.loc[add,'D_YLD'], df_z.loc[add,'D_VOL_RAW'], df_z.loc[add,'D_TRD']
L716                 near_tbl = pd.concat([d_disp2, pd.Series({t: d_score_all.get(t) for t in add}, name='DSC')], axis=1)
L717                 self.d_table = pd.concat([self.d_table, near_tbl], axis=0)
L718         print(self.d_title); print(self.d_table.to_string(formatters=self.d_formatters))
L719
L720         # === Changes（IN の GSC/DSC を表示。OUT は銘柄名のみ） ===
L721         in_list = sorted(set(list(top_G)+list(top_D)) - set(exist))
L722         out_list = sorted(set(exist) - set(list(top_G)+list(top_D)))
L723
L724         self.io_table = pd.DataFrame({
L725             'IN': pd.Series(in_list),
L726             '/ OUT': pd.Series(out_list)
L727         })
L728         g_list = [f"{g_score.get(t):.3f}" if pd.notna(g_score.get(t)) else '—' for t in out_list]
L729         d_list = [f"{d_score_all.get(t):.3f}" if pd.notna(d_score_all.get(t)) else '—' for t in out_list]
L730         self.io_table['GSC'] = pd.Series(g_list)
L731         self.io_table['DSC'] = pd.Series(d_list)
L732
L733         print("Changes:")
L734         print(self.io_table.to_string(index=False))
L735
L736         all_tickers = list(set(exist + list(top_G) + list(top_D) + [bench])); prices = yf.download(all_tickers, period='1y', auto_adjust=True, progress=False, threads=False)['Close'].ffill(limit=2)
L737         ret = prices.pct_change(); portfolios = {'CUR':exist,'NEW':list(top_G)+list(top_D)}; metrics={}
L738         for name,ticks in portfolios.items():
L739             pr = ret[ticks].mean(axis=1, skipna=True).dropna(); cum = (1+pr).cumprod()-1; n = len(pr)
L740             if n>=252: ann_ret, ann_vol = (1+cum.iloc[-1])**(252/n)-1, pr.std()*np.sqrt(252)
L741             else: ann_ret, ann_vol = cum.iloc[-1], pr.std()*np.sqrt(n)
L742             sharpe, drawdown = ann_ret/ann_vol, (cum - cum.cummax()).min()
L743             if len(ticks)>=2:
L744                 C_raw = ret[ticks].corr(); RAW_rho = C_raw.mask(np.eye(len(ticks), dtype=bool)).stack().mean()
L745                 R = ret[ticks].dropna().to_numpy(); C_resid = Selector.residual_corr(R, n_pc=3, shrink=DRRS_SHRINK)
L746                 RESID_rho = float((C_resid.sum()-np.trace(C_resid))/(C_resid.shape[0]*(C_resid.shape[0]-1)))
L747             else: RAW_rho = RESID_rho = np.nan
L748             divy = ttm_div_yield_portfolio(ticks); metrics[name] = {'RET':ann_ret,'VOL':ann_vol,'SHP':sharpe,'MDD':drawdown,'RAWρ':RAW_rho,'RESIDρ':RESID_rho,'DIVY':divy}
L749         df_metrics = pd.DataFrame(metrics).T; df_metrics_pct = df_metrics.copy(); self.df_metrics = df_metrics
L750         for col in ['RET','VOL','MDD','DIVY']: df_metrics_pct[col] = df_metrics_pct[col]*100
L751         cols_order = ['RET','VOL','SHP','MDD','RAWρ','RESIDρ','DIVY']; df_metrics_pct = df_metrics_pct.reindex(columns=cols_order)
L752         def _fmt_row(s):
L753             return pd.Series({'RET':f"{s['RET']:.1f}%",'VOL':f"{s['VOL']:.1f}%",'SHP':f"{s['SHP']:.1f}",'MDD':f"{s['MDD']:.1f}%",'RAWρ':(f"{s['RAWρ']:.2f}" if pd.notna(s['RAWρ']) else "NaN"),'RESIDρ':(f"{s['RESIDρ']:.2f}" if pd.notna(s['RESIDρ']) else "NaN"),'DIVY':f"{s['DIVY']:.1f}%"})
L754         self.df_metrics_fmt = df_metrics_pct.apply(_fmt_row, axis=1); print("Performance Comparison:"); print(self.df_metrics_fmt.to_string())
L755         # === 追加: GSC+DSC が低い順 TOP10 ===
L756         try:
L757             all_scores = pd.DataFrame({'GSC': df_z['GSC'], 'DSC': df_z['DSC']}).copy()
L758             all_scores['G_plus_D'] = all_scores['GSC'] + all_scores['DSC']
L759             all_scores = all_scores.dropna(subset=['G_plus_D'])
L760             self.low10_table = all_scores.sort_values('G_plus_D', ascending=True).head(10).round(3)
L761             print("Low Score Candidates (GSC+DSC bottom 10):")
L762             print(self.low10_table.to_string())
L763         except Exception as e:
L764             print(f"[warn] low-score ranking failed: {e}")
L765             self.low10_table = None
L766         # --- ここから: デバッグ出力は _compact_debug で一本化（表示経路もSlack経路もこれだけ）---
L767         if debug_mode:
L768             from types import SimpleNamespace
L769
L770             df_full_src    = getattr(getattr(self, "_sc", None), "_feat", None)
L771             df_full        = getattr(df_full_src, "df_full", None) or kwargs.get("df_full")
L772             df_full_z_pass = getattr(df_full_src, "df_full_z", None) or kwargs.get("df_full_z")
L773
L774             fb_like = SimpleNamespace(
L775                 df_full=df_full,
L776                 df_z=df_z,
L777                 df_full_z=df_full_z_pass,
L778                 g_score=g_score,
L779                 d_score_all=d_score_all,
L780                 missing_logs=self.miss_df,
L781             )
L782             sb_like = SimpleNamespace(top_G=top_G, top_D=top_D)
L783
L784             self.debug_text = _compact_debug(
L785                 fb_like,
L786                 sb_like,
L787                 prevG=kwargs.get("prev_G", exist),
L788                 prevD=kwargs.get("prev_D", exist),
L789                 max_rows=int(os.getenv("DEBUG_MAX_ROWS", "120")),
L790             )
L791
L792             if debug_mode:
L793                 dt = (self.debug_text or "").strip()
L794                 logger.info("===== DEBUG (after Low Score) START =====")
L795                 if dt:
L796                     logger.info("\n%s", dt)
L797                 else:
L798                     logger.info("<empty debug_text>")
L799                 logger.info("===== DEBUG (after Low Score) END =====")
L800
L801             if not (self.debug_text or "").strip():
L802                 src_df = df_full or df_full_z_pass or df_z
L803                 if src_df is not None and getattr(src_df, "empty", False) is False:
L804                     core = [c for c in [
L805                         "GRW","TR_EPS","TR_REV","EPS_Q_YOY","REV_Q_YOY","REV_YOY_ACC",
L806                         "RULE40","FCF_MGN","GSC","DSC"
L807                     ] if c in src_df.columns]
L808                     if core:
L809                         pick: list[str] = []
L810                         for t in (self.g_table, self.d_table, self.low10_table):
L811                             if t is not None and getattr(t, "empty", False) is False:
L812                                 pick += [i for i in list(t.index) if i in src_df.index]
L813                         view = (src_df.loc[pick, core] if pick else src_df[core]).head(int(os.getenv("DEBUG_MAX_ROWS", "80")))
L814                         self.debug_text = "DEBUG fallback\n" + view.to_string()
L815                     else:
L816                         self.debug_text = "(no debug columns)"
L817                 else:
L818                     self.debug_text = "(no dataframe)"
L819         else:
L820             self.debug_text = ""
L821         if debug_mode:
L822             self._debug_logged = True
L823         else:
L824             logger.debug(
L825                 "skip debug log: debug_mode=%s debug_text_empty=%s",
L826                 debug_mode, not bool((self.debug_text or '').strip())
L827             )
L828             self._debug_logged = True
L829
L830     # --- Slack送信（元 notify_slack のロジックそのまま） ---
L831     def notify_slack(self):
L832         SLACK_WEBHOOK_URL = os.environ.get("SLACK_WEBHOOK_URL")
L833
L834         if not SLACK_WEBHOOK_URL:
L835             print("⚠️ SLACK_WEBHOOK_URL not set (main report skipped)")
L836             return
L837
L838         def _filter_suffix_from(spec: dict, group: str) -> str:
L839             g = spec.get(group, {})
L840             parts = [str(m) for m in g.get("pre_mask", [])]
L841             for k, v in (g.get("pre_filter", {}) or {}).items():
L842                 base, op = (k[:-4], "<") if k.endswith("_max") else ((k[:-4], ">") if k.endswith("_min") else (k, "="))
L843                 name = {"beta": "β"}.get(base, base)
L844                 try:
L845                     val = f"{float(v):g}"
L846                 except Exception:
L847                     val = str(v)
L848                 parts.append(f"{name}{op}{val}")
L849             return "" if not parts else " / filter:" + " & ".join(parts)
L850
L851         def _inject_filter_suffix(title: str, group: str) -> str:
L852             suf = _filter_suffix_from(FILTER_SPEC, group)
L853             return f"{title[:-1]}{suf}]" if suf and title.endswith("]") else (title + suf)
L854
L855         def _blk(title, tbl, fmt=None, drop=())
```