```text
m, max_pass=15))
L939         selected_tickers = [pool_eff[i] for i in S]
L940         return dict(idx=S, tickers=selected_tickers, avg_res_corr=cls.avg_corr(C_within,S), sum_score=float(score[S].sum()), objective=float(Jn))
L941
L942     # ---- 選定（スコア Series / returns だけを受ける）----
L943 # === Output：出力整形と送信（表示・Slack） ===
L944 class Output:
L945
L946     def __init__(self, debug=None):
L947         # self.debug は使わない（互換のため引数は受けるが無視）
L948         self.miss_df = self.g_table = self.d_table = self.io_table = self.df_metrics_fmt = self.debug_table = None
L949         self.g_title = self.d_title = ""
L950         self.g_formatters = self.d_formatters = {}
L951         # 低スコア（GSC+DSC）Top10 表示/送信用
L952         self.low10_table = None
L953         self.debug_text = ""   # デバッグ用本文はここに一本化
L954         self._debug_logged = False
L955
L956     # --- 表示（元 display_results のロジックそのまま） ---
L957     def display_results(self, *, exist, bench, df_z, g_score, d_score_all,
L958                         init_G, init_D, top_G, top_D, **kwargs):
L959         logger.info("📌 reached display_results")
L960         pd.set_option('display.float_format','{:.3f}'.format)
L961         print("📈 ファクター分散最適化の結果")
L962         if self.miss_df is not None and not self.miss_df.empty:
L963             print("Missing Data:")
L964             print(self.miss_df.to_string(index=False))
L965
L966         # ---- 表示用：Changes/Near-Miss のスコア源を“最終集計”に統一するプロキシ ----
L967         try:
L968             sc = getattr(self, "_sc", None)
L969             agg_G = getattr(sc, "_agg_G", None)
L970             agg_D = getattr(sc, "_agg_D", None)
L971         except Exception:
L972             sc = agg_G = agg_D = None
L973         class _SeriesProxy:
L974             __slots__ = ("primary", "fallback")
L975             def __init__(self, primary, fallback): self.primary, self.fallback = primary, fallback
L976             def get(self, key, default=None):
L977                 try:
L978                     v = self.primary.get(key) if hasattr(self.primary, "get") else None
L979                 except Exception:
L980                     v = None
L981                 if v is not None and not (isinstance(v, float) and v != v):
L982                     return v
L983                 try:
L984                     return self.fallback.get(key) if hasattr(self.fallback, "get") else default
L985                 except Exception:
L986                     return default
L987         g_score = _SeriesProxy(agg_G, g_score)
L988         d_score_all = _SeriesProxy(agg_D, d_score_all)
L989         near_G = getattr(sc, "_near_G", []) if sc else []
L990         near_D = getattr(sc, "_near_D", []) if sc else []
L991
L992         extra_G = [t for t in init_G if t not in top_G][:5]; G_UNI = top_G + extra_G
L993         gsc_series = pd.Series({t: g_score.get(t) for t in G_UNI}, name='GSC')
L994         self.g_table = pd.concat([df_z.loc[G_UNI,['GROWTH_F','MOM','TRD','VOL']], gsc_series], axis=1)
L995         self.g_table.index = [t + ("⭐️" if t in top_G else "") for t in G_UNI]
L996         self.g_formatters = {col:"{:.2f}".format for col in ['GROWTH_F','MOM','TRD','VOL']}; self.g_formatters['GSC'] = "{:.3f}".format
L997         self.g_title = (f"[G枠 / {N_G} / {_fmt_w(g_weights)} / corrM={corrM} / "
L998                         f"LB={DRRS_G['lookback']} nPC={DRRS_G['n_pc']} γ={DRRS_G['gamma']} λ={DRRS_G['lam']} η={DRRS_G['eta']} shrink={DRRS_SHRINK}]")
L999         if near_G:
L1000             add = [t for t in near_G if t not in set(G_UNI)][:10]
L1001             if len(add) < 10:
L1002                 try:
L1003                     aggG = getattr(sc, "_agg_G", pd.Series(dtype=float)).sort_values(ascending=False)
L1004                     out_now = sorted(set(exist) - set(top_G + top_D))  # 今回 OUT
L1005                     used = set(G_UNI + add)
L1006                     def _push(lst):
L1007                         nonlocal add, used
L1008                         for t in lst:
L1009                             if len(add) == 10: break
L1010                             if t in aggG.index and t not in used:
L1011                                 add.append(t); used.add(t)
L1012                     _push(out_now)           # ① 今回 OUT を優先
L1013                     _push(list(aggG.index))  # ② まだ足りなければ上位で充填
L1014                 except Exception:
L1015                     pass
L1016             if add:
L1017                 near_tbl = pd.concat([df_z.loc[add,['GROWTH_F','MOM','TRD','VOL']], pd.Series({t: g_score.get(t) for t in add}, name='GSC')], axis=1)
L1018                 self.g_table = pd.concat([self.g_table, near_tbl], axis=0)
L1019         print(self.g_title); print(self.g_table.to_string(formatters=self.g_formatters))
L1020
L1021         extra_D = [t for t in init_D if t not in top_D][:5]; D_UNI = top_D + extra_D
L1022         cols_D = ['QAL','YLD','VOL','TRD']; d_disp = pd.DataFrame(index=D_UNI)
L1023         d_disp['QAL'], d_disp['YLD'], d_disp['VOL'], d_disp['TRD'] = df_z.loc[D_UNI,'D_QAL'], df_z.loc[D_UNI,'D_YLD'], df_z.loc[D_UNI,'D_VOL_RAW'], df_z.loc[D_UNI,'D_TRD']
L1024         dsc_series = pd.Series({t: d_score_all.get(t) for t in D_UNI}, name='DSC')
L1025         self.d_table = pd.concat([d_disp, dsc_series], axis=1); self.d_table.index = [t + ("⭐️" if t in top_D else "") for t in D_UNI]
L1026         self.d_formatters = {col:"{:.2f}".format for col in cols_D}; self.d_formatters['DSC']="{:.3f}".format
L1027         import scorer
L1028         dw_eff = scorer.D_WEIGHTS_EFF
L1029         self.d_title = (f"[D枠 / {N_D} / {_fmt_w(dw_eff)} / corrM={corrM} / "
L1030                         f"LB={DRRS_D['lookback']} nPC={DRRS_D['n_pc']} γ={DRRS_D['gamma']} λ={DRRS_D['lam']} μ={CROSS_MU_GD} η={DRRS_D['eta']} shrink={DRRS_SHRINK}]")
L1031         if near_D:
L1032             add = [t for t in near_D if t not in set(D_UNI)][:10]
L1033             if add:
L1034                 d_disp2 = pd.DataFrame(index=add)
L1035                 d_disp2['QAL'], d_disp2['YLD'], d_disp2['VOL'], d_disp2['TRD'] = df_z.loc[add,'D_QAL'], df_z.loc[add,'D_YLD'], df_z.loc[add,'D_VOL_RAW'], df_z.loc[add,'D_TRD']
L1036                 near_tbl = pd.concat([d_disp2, pd.Series({t: d_score_all.get(t) for t in add}, name='DSC')], axis=1)
L1037                 self.d_table = pd.concat([self.d_table, near_tbl], axis=0)
L1038         print(self.d_title); print(self.d_table.to_string(formatters=self.d_formatters))
L1039
L1040         # === Changes（IN の GSC/DSC を表示。OUT は銘柄名のみ） ===
L1041         in_list = sorted(set(list(top_G)+list(top_D)) - set(exist))
L1042         out_list = sorted(set(exist) - set(list(top_G)+list(top_D)))
L1043
L1044         self.io_table = pd.DataFrame({
L1045             'IN': pd.Series(in_list),
L1046             '/ OUT': pd.Series(out_list)
L1047         })
L1048         g_list = [f"{g_score.get(t):.3f}" if pd.notna(g_score.get(t)) else '—' for t in out_list]
L1049         d_list = [f"{d_score_all.get(t):.3f}" if pd.notna(d_score_all.get(t)) else '—' for t in out_list]
L1050         self.io_table['GSC'] = pd.Series(g_list)
L1051         self.io_table['DSC'] = pd.Series(d_list)
L1052
L1053         print("Changes:")
L1054         print(self.io_table.to_string(index=False))
L1055
L1056         all_tickers = list(set(exist + list(top_G) + list(top_D) + [bench])); prices = yf.download(all_tickers, period='1y', auto_adjust=True, progress=False, threads=False)['Close'].ffill(limit=2)
L1057         ret = prices.pct_change(); portfolios = {'CUR':exist,'NEW':list(top_G)+list(top_D)}; metrics={}
L1058         for name,ticks in portfolios.items():
L1059             pr = ret[ticks].mean(axis=1, skipna=True).dropna(); cum = (1+pr).cumprod()-1; n = len(pr)
L1060             if n>=252: ann_ret, ann_vol = (1+cum.iloc[-1])**(252/n)-1, pr.std()*np.sqrt(252)
L1061             else: ann_ret, ann_vol = cum.iloc[-1], pr.std()*np.sqrt(n)
L1062             sharpe, drawdown = ann_ret/ann_vol, (cum - cum.cummax()).min()
L1063             if len(ticks)>=2:
L1064                 C_raw = ret[ticks].corr(); RAW_rho = C_raw.mask(np.eye(len(ticks), dtype=bool)).stack().mean()
L1065                 R = ret[ticks].dropna().to_numpy(); C_resid = Selector.residual_corr(R, n_pc=3, shrink=DRRS_SHRINK)
L1066                 RESID_rho = float((C_resid.sum()-np.trace(C_resid))/(C_resid.shape[0]*(C_resid.shape[0]-1)))
L1067             else: RAW_rho = RESID_rho = np.nan
L1068             divy = ttm_div_yield_portfolio(ticks); metrics[name] = {'RET':ann_ret,'VOL':ann_vol,'SHP':sharpe,'MDD':drawdown,'RAWρ':RAW_rho,'RESIDρ':RESID_rho,'DIVY':divy}
L1069         df_metrics = pd.DataFrame(metrics).T; df_metrics_pct = df_metrics.copy(); self.df_metrics = df_metrics
L1070         for col in ['RET','VOL','MDD','DIVY']: df_metrics_pct[col] = df_metrics_pct[col]*100
L1071         cols_order = ['RET','VOL','SHP','MDD','RAWρ','RESIDρ','DIVY']; df_metrics_pct = df_metrics_pct.reindex(columns=cols_order)
L1072         def _fmt_row(s):
L1073             return pd.Series({'RET':f"{s['RET']:.1f}%",'VOL':f"{s['VOL']:.1f}%",'SHP':f"{s['SHP']:.1f}",'MDD':f"{s['MDD']:.1f}%",'RAWρ':(f"{s['RAWρ']:.2f}" if pd.notna(s['RAWρ']) else "NaN"),'RESIDρ':(f"{s['RESIDρ']:.2f}" if pd.notna(s['RESIDρ']) else "NaN"),'DIVY':f"{s['DIVY']:.1f}%"})
L1074         self.df_metrics_fmt = df_metrics_pct.apply(_fmt_row, axis=1); print("Performance Comparison:"); print(self.df_metrics_fmt.to_string())
L1075         # === 追加: GSC+DSC が低い順 TOP10 ===
L1076         try:
L1077             all_scores = pd.DataFrame({'GSC': df_z['GSC'], 'DSC': df_z['DSC']}).copy()
L1078             all_scores['G_plus_D'] = all_scores['GSC'] + all_scores['DSC']
L1079             all_scores = all_scores.dropna(subset=['G_plus_D'])
L1080             self.low10_table = all_scores.sort_values('G_plus_D', ascending=True).head(10).round(3)
L1081             print("Low Score Candidates (GSC+DSC bottom 10):")
L1082             print(self.low10_table.to_string())
L1083         except Exception as e:
L1084             print(f"[warn] low-score ranking failed: {e}")
L1085             self.low10_table = None
L1086         self.debug_text = ""
L1087         if debug_mode:
L1088             logger.info("debug_mode=True: df_z dump handled in scorer; skipping factor-side debug output")
L1089         else:
L1090             logger.debug(
L1091                 "skip debug log: debug_mode=%s debug_text_empty=%s",
L1092                 debug_mode, True
L1093             )
L1094         self._debug_logged = True
L1095
L1096     # --- Slack送信（元 notify_slack のロジックそのまま） ---
L1097     def notify_slack(self):
L1098         SLACK_WEBHOOK_URL = os.environ.get("SLACK_WEBHOOK_URL")
L1099
L1100         if not SLACK_WEBHOOK_URL:
L1101             print("⚠️ SLACK_WEBHOOK_URL not set (main report skipped)")
L1102             return
L1103
L1104         def _filter_suffix_from(spec: dict, group: str) -> str:
L1105             g = spec.get(group, {})
L1106             parts = [str(m) for m in g.get("pre_mask", [])]
L1107             for k, v in (g.get("pre_filter", {}) or {}).items():
L1108                 base, op = (k[:-4], "<") if k.endswith("_max") else ((k[:-4], ">") if k.endswith("_min") else (k, "="))
L1109                 name = {"beta": "β"}.get(base, base)
L1110                 try:
L1111                     val = f"{float(v):g}"
L1112                 except Exception:
L1113                     val = str(v)
L1114                 parts.append(f"{name}{op}{val}")
L1115             return "" if not parts else " / filter:" + " & ".join(parts)
L1116
L1117         def _inject_filter_suffix(title: str, group: str) -> str:
L1118             suf = _filter_suffix_from(FILTER_SPEC, group)
L1119             return f"{title[:-1]}{suf}]" if suf and title.endswith("]") else (title + suf)
L1120
L1121         def _blk(title, tbl, fmt=None, drop=()):
L1122             if tbl is None or getattr(tbl, 'empty', False):
L1123                 return f"{title}\n(選定なし)\n"
L1124             if drop and hasattr(tbl, 'columns'):
L
```