```text
st in (selected12 or []), (near5 or []):
L841         for x in (lst or []): base.add(x)
L842     return list(base) if base else list(feature_df.index)
L843
L844 def _fmt_with_fire_mark(tickers, feature_df):
L845     out = []
L846     for t in tickers or []:
L847         try:
L848             br = bool(feature_df.at[t, "G_BREAKOUT_recent_5d"])
L849             pb = bool(feature_df.at[t, "G_PULLBACK_recent_5d"])
L850             out.append(f"{t}{' 🔥' if (br or pb) else ''}")
L851         except Exception:
L852             out.append(t)
L853     return out
L854
L855 def _label_recent_event(t, feature_df):
L856     try:
L857         br = bool(feature_df.at[t, "G_BREAKOUT_recent_5d"]); dbr = str(feature_df.at[t, "G_BREAKOUT_last_date"]) if br else ""
L858         pb = bool(feature_df.at[t, "G_PULLBACK_recent_5d"]); dpb = str(feature_df.at[t, "G_PULLBACK_last_date"]) if pb else ""
L859         if   br and not pb: return f"{t}（ブレイクアウト確定 {dbr}）"
L860         elif pb and not br: return f"{t}（押し目反発 {dpb}）"
L861         elif br and pb:     return f"{t}（ブレイクアウト確定 {dbr}／押し目反発 {dpb}）"
L862     except Exception:
L863         pass
L864     return t
L865
L866 # === パイプライン可視化：G/D共通フロー（出力は不変） ===
L867
L868 def io_build_input_bundle() -> InputBundle:
L869     """
L870     既存の『データ取得→前処理』を実行し、InputBundle を返す。
L871     処理内容・列名・丸め・例外・ログ文言は現行どおり（変更禁止）。
L872     """
L873     state = Input(cand=cand, exist=exist, bench=bench, price_max=CAND_PRICE_MAX, finnhub_api_key=FINNHUB_API_KEY).prepare_data()
L874     return InputBundle(cand=state["cand"], tickers=state["tickers"], bench=bench, data=state["data"], px=state["px"], spx=state["spx"], tickers_bulk=state["tickers_bulk"], info=state["info"], eps_df=state["eps_df"], fcf_df=state["fcf_df"], returns=state["returns"])
L875
L876 def run_group(sc: Scorer, group: str, inb: InputBundle, cfg: PipelineConfig,
L877               n_target: int) -> tuple[list, float, float, float]:
L878     """
L879     G/Dを同一手順で処理：採点→フィルター→選定（相関低減込み）。
L880     戻り値：(pick, avg_res_corr, sum_score, objective)
L881     JSON保存は既存フォーマット（キー名・丸め桁・順序）を踏襲。
L882     """
L883     sc.cfg = cfg
L884
L885     if hasattr(sc, "score_build_features"):
L886         feat = sc.score_build_features(inb)
L887         if not hasattr(sc, "_feat_logged"):
L888             T.log("features built (scorer)")
L889             sc._feat_logged = True
L890         agg = sc.score_aggregate(feat, group, cfg) if hasattr(sc, "score_aggregate") else feat
L891     else:
L892         fb = sc.aggregate_scores(inb, cfg)
L893         if not hasattr(sc, "_feat_logged"):
L894             T.log("features built (scorer)")
L895             sc._feat_logged = True
L896         sc._feat = fb
L897         agg = fb.g_score if group == "G" else fb.d_score_all
L898         if group == "D" and hasattr(fb, "df"):
L899             agg = agg[fb.df['BETA'] < D_BETA_MAX]
L900
L901     if hasattr(sc, "filter_candidates"):
L902         agg = agg[sc.filter_candidates(inb, agg, group, cfg)]
L903
L904     selector = Selector()
L905     if hasattr(sc, "select_diversified"):
L906         pick, avg_r, sum_sc, obj = sc.select_diversified(agg, group, cfg, n_target,
L907             selector=selector, prev_tickers=None,
L908             corrM=cfg.drrs.corrM, shrink=cfg.drrs.shrink,
L909             cross_mu=cfg.drrs.cross_mu_gd)
L910     else:
L911         if group == "G":
L912             init = agg.nlargest(min(cfg.drrs.corrM, len(agg))).index.tolist()
L913             res = selector.select_bucket_drrs(returns_df=inb.returns, score_ser=agg, pool_tickers=init, k=n_target,
L914                 n_pc=cfg.drrs.G.get("n_pc", 3), gamma=cfg.drrs.G.get("gamma", 1.2),
L915                 lam=cfg.drrs.G.get("lam", 0.68),
L916                 lookback=cfg.drrs.G.get("lookback", 252),
L917                 shrink=cfg.drrs.shrink, g_fixed_tickers=None, mu=0.0)
L918         else:
L919             init = agg.nlargest(min(cfg.drrs.corrM, len(agg))).index.tolist()
L920             g_fixed = getattr(sc, "_top_G", None)
L921             res = selector.select_bucket_drrs(returns_df=inb.returns, score_ser=agg, pool_tickers=init, k=n_target,
L922                 n_pc=cfg.drrs.D.get("n_pc", 4), gamma=cfg.drrs.D.get("gamma", 0.8),
L923                 lam=cfg.drrs.D.get("lam", 0.85),
L924                 lookback=cfg.drrs.D.get("lookback", 504),
L925                 shrink=cfg.drrs.shrink, g_fixed_tickers=g_fixed,
L926                 mu=cfg.drrs.cross_mu_gd)
L927         pick = res["tickers"]; avg_r = res["avg_res_corr"]
L928         sum_sc = res["sum_score"]; obj = res["objective"]
L929         if group == "D":
L930             _, pick = _disjoint_keepG(getattr(sc, "_top_G", []), pick, init)
L931             T.log("selection finalized (G/D)")
L932     try:
L933         inc = [t for t in exist if t in agg.index]
L934         pick = _sticky_keep_current(
L935             agg=agg, pick=pick, incumbents=inc, n_target=n_target,
L936             delta_z=SWAP_DELTA_Z, keep_buffer=SWAP_KEEP_BUFFER
L937         )
L938     except Exception as _e:
L939         print(f"[warn] sticky_keep_current skipped: {str(_e)}")
L940     # --- Near-Miss: 惜しくも選ばれなかった上位10を保持（Slack表示用） ---
L941     # 5) Near-Miss と最終集計Seriesを保持（表示専用。計算へ影響なし）
L942     try:
L943         pool = agg.drop(index=[t for t in pick if t in agg.index], errors="ignore")
L944         near10 = list(pool.sort_values(ascending=False).head(10).index)
L945         setattr(sc, f"_near_{group}", near10)
L946         setattr(sc, f"_agg_{group}", agg)
L947     except Exception:
L948         pass
L949
L950     if group == "D":
L951         T.log("save done")
L952     if group == "G":
L953         sc._top_G = pick
L954     return pick, avg_r, sum_sc, obj
L955
L956 def run_pipeline() -> SelectionBundle:
L957     """
L958     G/D共通フローの入口。I/Oはここだけで実施し、計算はScorerに委譲。
L959     Slack文言・丸め・順序は既存の Output を用いて変更しない。
L960     """
L961     inb = io_build_input_bundle()
L962     cfg = PipelineConfig(weights=WeightsConfig(g=g_weights, d=D_weights),
L963         drrs=DRRSParams(corrM=corrM, shrink=DRRS_SHRINK,
L964                          G=DRRS_G, D=DRRS_D, cross_mu_gd=CROSS_MU_GD),
L965         price_max=CAND_PRICE_MAX)
L966     sc = Scorer()
L967     top_G, avgG, sumG, objG = run_group(sc, "G", inb, cfg, N_G)
L968     poolG = list(getattr(sc, "_agg_G", pd.Series(dtype=float)).sort_values(ascending=False).index)
L969     alpha = Scorer.spx_to_alpha(inb.spx)
L970     sectors = {t:(inb.info.get(t,{}).get("sector") or "U") for t in poolG}; scores = {t:Scorer.g_score.get(t,0.0) for t in poolG}
L971     top_G = Scorer.pick_top_softcap(scores, sectors, N=N_G, cap=2, alpha=alpha, hard=5)
L972     sc._top_G = top_G
L973     try:
L974         aggG = getattr(sc, "_agg_G", pd.Series(dtype=float)).sort_values(ascending=False)
L975         sc._near_G = [t for t in aggG.index if t not in set(top_G)][:10]
L976     except Exception:
L977         pass
L978     base = sum(Scorer.g_score.get(t,0.0) for t in poolG[:N_G])
L979     effs = sum(Scorer.g_score.get(t,0.0) for t in top_G)
L980     print(f"[soft_cap2] score_cost={(base-effs)/max(1e-9,abs(base)):.2%}, alpha={alpha:.3f}")
L981     top_D, avgD, sumD, objD = run_group(sc, "D", inb, cfg, N_D)
L982     fb = getattr(sc, "_feat", None)
L983     near_G = getattr(sc, "_near_G", [])
L984     selected12 = list(top_G)
L985     df = fb.df if fb is not None else pd.DataFrame()
L986     guni = _infer_g_universe(df, selected12, near_G)
L987     try:
L988         fire_recent = [t for t in guni
L989                        if (str(df.at[t, "G_BREAKOUT_recent_5d"]) == "True") or
L990                           (str(df.at[t, "G_PULLBACK_recent_5d"]) == "True")]
L991     except Exception: fire_recent = []
L992
L993     lines = [
L994         "【G枠レポート｜週次モニタ（直近5営業日）】",
L995         "【凡例】🔥=直近5営業日内に「ブレイクアウト確定」または「押し目反発」を検知",
L996         f"選定{N_G}: {', '.join(_fmt_with_fire_mark(selected12, df))}" if selected12 else f"選定{N_G}: なし",
L997         f"次点10: {', '.join(_fmt_with_fire_mark(near_G, df))}" if near_G else "次点10: なし",]
L998
L999     if fire_recent:
L1000         fire_list = ", ".join([_label_recent_event(t, df) for t in fire_recent])
L1001         lines.append(f"過去5営業日の検知: {fire_list}")
L1002     else:
L1003         lines.append("過去5営業日の検知: なし")
L1004
L1005     try:
L1006         webhook = os.environ.get("SLACK_WEBHOOK_URL", "")
L1007         if webhook:
L1008             requests.post(webhook, json={"text": "\n".join([s for s in lines if s != ""])}, timeout=10)
L1009     except Exception:
L1010         pass
L1011
L1012     out = Output()
L1013     # 表示側から選定時の集計へアクセスできるように保持（表示専用・副作用なし）
L1014     try: out._sc = sc
L1015     except Exception: pass
L1016     if hasattr(sc, "_feat"):
L1017         try:
L1018             fb = sc._feat
L1019             out.miss_df = fb.missing_logs
L1020             out.display_results(
L1021                 exist=exist,
L1022                 bench=bench,
L1023                 df_z=fb.df_z,
L1024                 g_score=fb.g_score,
L1025                 d_score_all=fb.d_score_all,
L1026                 init_G=top_G,
L1027                 init_D=top_D,
L1028                 top_G=top_G,
L1029                 top_D=top_D,
L1030                 df_full_z=getattr(fb, "df_full_z", None),
L1031                 prev_G=getattr(sc, "_prev_G", exist),
L1032                 prev_D=getattr(sc, "_prev_D", exist),
L1033             )
L1034         except Exception:
L1035             pass
L1036     out.notify_slack()
L1037     sb = SelectionBundle(resG={"tickers": top_G, "avg_res_corr": avgG,
L1038               "sum_score": sumG, "objective": objG},
L1039         resD={"tickers": top_D, "avg_res_corr": avgD,
L1040               "sum_score": sumD, "objective": objD},
L1041         top_G=top_G, top_D=top_D, init_G=top_G, init_D=top_D)
L1042
L1043     # --- Low Score Candidates (GSC+DSC bottom 10) : send before debug dump ---
L1044     try:
L1045         _low_df = (pd.DataFrame({"GSC": fb.g_score, "DSC": fb.d_score_all})
L1046               .assign(G_plus_D=lambda x: x["GSC"] + x["DSC"])
L1047               .sort_values("G_plus_D")
L1048               .head(10)
L1049               .round(3))
L1050         low_msg = "Low Score Candidates (GSC+DSC bottom 10)\n" + _low_df.to_string(index=True, index_names=False)
L1051         _post_slack({"text": f"```{low_msg}```"})
L1052     except Exception as _e:
L1053         _post_slack({"text": f"```Low Score Candidates: 作成失敗: {_e}```"})
L1054
L1055     return sb
L1056
L1057 if __name__ == "__main__":
L1058     run_pipeline()
```

## <scorer.py>
```text
L1 # scorer.py
L2 # kawatest
L3 # =============================================================================
L4 # Scorer: ファクター/指標の生成と合成スコア算出を担う純粋層
L5 #
L6 # 【このファイルだけ読めば分かるポイント】
L7 # - 入力(InputBundle)は「価格/出来高/ベンチ/基本情報/EPS/FCF/リターン」を含むDTO
L8 # - 出力(FeatureBundle)は「raw特徴量 df」「標準化 df_z」「G/D スコア」「欠損ログ」
L9 # - 重み等のコンフィグ(PipelineConfig)は factor から渡す（cfg 必須）
L10 # - 旧カラム名は Scorer 内で自動リネームして受け入れ（後方互換）
L11 #   例) eps_ttm -> EPS_TTM, eps_q_recent -> EPS_Q_LastQ, fcf_ttm -> FCF_TTM
L12 #
L13 # 【I/O契約（Scorerが参照するInputBundleフィールド）】
L14 #   - cand: List[str]    … 候補銘柄（単体実行では未使用）
L15 #   - tickers: List[str] … 対象銘柄リスト
L16 #   - bench: str         … ベンチマークティッカー（例 '^GSPC'）
L17 #   - data: pd.DataFrame … yfinance download結果 ('Close','Volume' 等の階層列)
L18 #   - px: pd.DataFrame   … data['Close'] 相当（終値）
L19 #   - spx: pd.Series     … ベンチマークの終値
L20 #   - tickers_bulk: object         … yfinance.Tickers
L21 #   - info: Dict[str, dict]        … yfinance info per ticker
L22 #   - eps_df: pd.DataFrame         … 必須列: EPS_TTM, EPS_Q_LastQ（旧名も可）
L23 #   - fcf_df: pd.DataFrame         … 必須列: FCF_TTM（旧名も可）
L24 #   - returns: pd.DataFrame        … px[tickers].pct_change() 相当
L25 #
L26 # ※入出力の形式・例外文言は既存実装を変えません（安全な短縮のみ）
L27 # =============================================================================
L28
L29 import os, sys, warnings
L30 import requests
L31 import numpy as np
L32 import pandas as pd
L33 import yfinance as yf
L34 from typing import Any, TYPE_
```