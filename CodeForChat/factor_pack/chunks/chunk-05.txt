```text
                   nonlocal add, used
L909                         for t in lst:
L910                             if len(add) == 10: break
L911                             if t in aggG.index and t not in used:
L912                                 add.append(t); used.add(t)
L913                     _push(out_now)           # ① 今回 OUT を優先
L914                     _push(list(aggG.index))  # ② まだ足りなければ上位で充填
L915                 except Exception:
L916                     pass
L917             if add:
L918                 near_tbl = pd.concat([df_z.loc[add,['GROWTH_F','MOM','TRD','VOL']], pd.Series({t: g_score.get(t) for t in add}, name='GSC')], axis=1)
L919                 self.g_table = pd.concat([self.g_table, near_tbl], axis=0)
L920         print(self.g_title); print(self.g_table.to_string(formatters=self.g_formatters))
L921
L922         extra_D = [t for t in init_D if t not in top_D][:5]; D_UNI = top_D + extra_D
L923         cols_D = ['QAL','YLD','VOL','TRD']; d_disp = pd.DataFrame(index=D_UNI)
L924         d_disp['QAL'], d_disp['YLD'], d_disp['VOL'], d_disp['TRD'] = df_z.loc[D_UNI,'D_QAL'], df_z.loc[D_UNI,'D_YLD'], df_z.loc[D_UNI,'D_VOL_RAW'], df_z.loc[D_UNI,'D_TRD']
L925         dsc_series = pd.Series({t: d_score_all.get(t) for t in D_UNI}, name='DSC')
L926         self.d_table = pd.concat([d_disp, dsc_series], axis=1); self.d_table.index = [t + ("⭐️" if t in top_D else "") for t in D_UNI]
L927         self.d_formatters = {col:"{:.2f}".format for col in cols_D}; self.d_formatters['DSC']="{:.3f}".format
L928         import scorer
L929         dw_eff = scorer.D_WEIGHTS_EFF
L930         self.d_title = (f"[D枠 / {N_D} / {_fmt_w(dw_eff)} / corrM={corrM} / "
L931                         f"LB={DRRS_D['lookback']} nPC={DRRS_D['n_pc']} γ={DRRS_D['gamma']} λ={DRRS_D['lam']} μ={CROSS_MU_GD} η={DRRS_D['eta']} shrink={DRRS_SHRINK}]")
L932         if near_D:
L933             add = [t for t in near_D if t not in set(D_UNI)][:10]
L934             if add:
L935                 d_disp2 = pd.DataFrame(index=add)
L936                 d_disp2['QAL'], d_disp2['YLD'], d_disp2['VOL'], d_disp2['TRD'] = df_z.loc[add,'D_QAL'], df_z.loc[add,'D_YLD'], df_z.loc[add,'D_VOL_RAW'], df_z.loc[add,'D_TRD']
L937                 near_tbl = pd.concat([d_disp2, pd.Series({t: d_score_all.get(t) for t in add}, name='DSC')], axis=1)
L938                 self.d_table = pd.concat([self.d_table, near_tbl], axis=0)
L939         print(self.d_title); print(self.d_table.to_string(formatters=self.d_formatters))
L940
L941         # === Changes（IN の GSC/DSC を表示。OUT は銘柄名のみ） ===
L942         in_list = sorted(set(list(top_G)+list(top_D)) - set(exist))
L943         out_list = sorted(set(exist) - set(list(top_G)+list(top_D)))
L944
L945         self.io_table = pd.DataFrame({
L946             'IN': pd.Series(in_list),
L947             '/ OUT': pd.Series(out_list)
L948         })
L949         g_list = [f"{g_score.get(t):.3f}" if pd.notna(g_score.get(t)) else '—' for t in out_list]
L950         d_list = [f"{d_score_all.get(t):.3f}" if pd.notna(d_score_all.get(t)) else '—' for t in out_list]
L951         self.io_table['GSC'] = pd.Series(g_list)
L952         self.io_table['DSC'] = pd.Series(d_list)
L953
L954         print("Changes:")
L955         print(self.io_table.to_string(index=False))
L956
L957         all_tickers = list(set(exist + list(top_G) + list(top_D) + [bench])); prices = yf.download(all_tickers, period='1y', auto_adjust=True, progress=False, threads=False)['Close'].ffill(limit=2)
L958         ret = prices.pct_change(); portfolios = {'CUR':exist,'NEW':list(top_G)+list(top_D)}; metrics={}
L959         for name,ticks in portfolios.items():
L960             pr = ret[ticks].mean(axis=1, skipna=True).dropna(); cum = (1+pr).cumprod()-1; n = len(pr)
L961             if n>=252: ann_ret, ann_vol = (1+cum.iloc[-1])**(252/n)-1, pr.std()*np.sqrt(252)
L962             else: ann_ret, ann_vol = cum.iloc[-1], pr.std()*np.sqrt(n)
L963             sharpe, drawdown = ann_ret/ann_vol, (cum - cum.cummax()).min()
L964             if len(ticks)>=2:
L965                 C_raw = ret[ticks].corr(); RAW_rho = C_raw.mask(np.eye(len(ticks), dtype=bool)).stack().mean()
L966                 R = ret[ticks].dropna().to_numpy(); C_resid = Selector.residual_corr(R, n_pc=3, shrink=DRRS_SHRINK)
L967                 RESID_rho = float((C_resid.sum()-np.trace(C_resid))/(C_resid.shape[0]*(C_resid.shape[0]-1)))
L968             else: RAW_rho = RESID_rho = np.nan
L969             divy = ttm_div_yield_portfolio(ticks); metrics[name] = {'RET':ann_ret,'VOL':ann_vol,'SHP':sharpe,'MDD':drawdown,'RAWρ':RAW_rho,'RESIDρ':RESID_rho,'DIVY':divy}
L970         df_metrics = pd.DataFrame(metrics).T; df_metrics_pct = df_metrics.copy(); self.df_metrics = df_metrics
L971         for col in ['RET','VOL','MDD','DIVY']: df_metrics_pct[col] = df_metrics_pct[col]*100
L972         cols_order = ['RET','VOL','SHP','MDD','RAWρ','RESIDρ','DIVY']; df_metrics_pct = df_metrics_pct.reindex(columns=cols_order)
L973         def _fmt_row(s):
L974             return pd.Series({'RET':f"{s['RET']:.1f}%",'VOL':f"{s['VOL']:.1f}%",'SHP':f"{s['SHP']:.1f}",'MDD':f"{s['MDD']:.1f}%",'RAWρ':(f"{s['RAWρ']:.2f}" if pd.notna(s['RAWρ']) else "NaN"),'RESIDρ':(f"{s['RESIDρ']:.2f}" if pd.notna(s['RESIDρ']) else "NaN"),'DIVY':f"{s['DIVY']:.1f}%"})
L975         self.df_metrics_fmt = df_metrics_pct.apply(_fmt_row, axis=1); print("Performance Comparison:"); print(self.df_metrics_fmt.to_string())
L976         # === 追加: GSC+DSC が低い順 TOP10 ===
L977         try:
L978             all_scores = pd.DataFrame({'GSC': df_z['GSC'], 'DSC': df_z['DSC']}).copy()
L979             all_scores['G_plus_D'] = all_scores['GSC'] + all_scores['DSC']
L980             all_scores = all_scores.dropna(subset=['G_plus_D'])
L981             self.low10_table = all_scores.sort_values('G_plus_D', ascending=True).head(10).round(3)
L982             print("Low Score Candidates (GSC+DSC bottom 10):")
L983             print(self.low10_table.to_string())
L984         except Exception as e:
L985             print(f"[warn] low-score ranking failed: {e}")
L986             self.low10_table = None
L987         self.debug_text = ""
L988         if debug_mode:
L989             logger.info("debug_mode=True: df_z dump handled in scorer; skipping factor-side debug output")
L990         else:
L991             logger.debug(
L992                 "skip debug log: debug_mode=%s debug_text_empty=%s",
L993                 debug_mode, True
L994             )
L995         self._debug_logged = True
L996
L997     # --- Slack送信（元 notify_slack のロジックそのまま） ---
L998     def notify_slack(self):
L999         SLACK_WEBHOOK_URL = os.environ.get("SLACK_WEBHOOK_URL")
L1000
L1001         if not SLACK_WEBHOOK_URL:
L1002             print("⚠️ SLACK_WEBHOOK_URL not set (main report skipped)")
L1003             return
L1004
L1005         def _filter_suffix_from(spec: dict, group: str) -> str:
L1006             g = spec.get(group, {})
L1007             parts = [str(m) for m in g.get("pre_mask", [])]
L1008             for k, v in (g.get("pre_filter", {}) or {}).items():
L1009                 base, op = (k[:-4], "<") if k.endswith("_max") else ((k[:-4], ">") if k.endswith("_min") else (k, "="))
L1010                 name = {"beta": "β"}.get(base, base)
L1011                 try:
L1012                     val = f"{float(v):g}"
L1013                 except Exception:
L1014                     val = str(v)
L1015                 parts.append(f"{name}{op}{val}")
L1016             return "" if not parts else " / filter:" + " & ".join(parts)
L1017
L1018         def _inject_filter_suffix(title: str, group: str) -> str:
L1019             suf = _filter_suffix_from(FILTER_SPEC, group)
L1020             return f"{title[:-1]}{suf}]" if suf and title.endswith("]") else (title + suf)
L1021
L1022         def _blk(title, tbl, fmt=None, drop=()):
L1023             if tbl is None or getattr(tbl, 'empty', False):
L1024                 return f"{title}\n(選定なし)\n"
L1025             if drop and hasattr(tbl, 'columns'):
L1026                 keep = [c for c in tbl.columns if c not in drop]
L1027                 tbl, fmt = tbl[keep], {k: v for k, v in (fmt or {}).items() if k in keep}
L1028             return f"{title}\n```{tbl.to_string(formatters=fmt)}```\n"
L1029
L1030         message = "📈 ファクター分散最適化の結果\n"
L1031         if self.miss_df is not None and not self.miss_df.empty:
L1032             message += "Missing Data\n```" + self.miss_df.to_string(index=False) + "```\n"
L1033         message += _blk(_inject_filter_suffix(self.g_title, "G"), self.g_table, self.g_formatters, drop=("TRD",))
L1034         message += _blk(_inject_filter_suffix(self.d_title, "D"), self.d_table, self.d_formatters)
L1035         message += "Changes\n" + ("(変更なし)\n" if self.io_table is None or getattr(self.io_table, 'empty', False) else f"```{self.io_table.to_string(index=False)}```\n")
L1036         message += "Performance Comparison:\n```" + self.df_metrics_fmt.to_string() + "```"
L1037
L1038         try:
L1039             r = requests.post(SLACK_WEBHOOK_URL, json={"text": message})
L1040             print(f"[DBG] main_post status={getattr(r, 'status_code', None)} size={len(message)}")
L1041             if r is not None:
L1042                 r.raise_for_status()
L1043         except Exception as e:
L1044             print(f"[ERR] main_post_failed: {e}")
L1045
L1046 def _infer_g_universe(feature_df, selected12=None, near5=None):
L1047     try:
L1048         out = feature_df.index[feature_df['group'].astype(str).str.upper().eq('G')].tolist()
L1049         if out: return out
L1050     except Exception:
L1051         pass
L1052     base = set()
L1053     for lst in (selected12 or []), (near5 or []):
L1054         for x in (lst or []): base.add(x)
L1055     return list(base) if base else list(feature_df.index)
L1056
L1057 def _fmt_with_fire_mark(tickers, feature_df):
L1058     out = []
L1059     for t in tickers or []:
L1060         try:
L1061             br = bool(feature_df.at[t, "G_BREAKOUT_recent_5d"])
L1062             pb = bool(feature_df.at[t, "G_PULLBACK_recent_5d"])
L1063             out.append(f"{t}{' 🔥' if (br or pb) else ''}")
L1064         except Exception:
L1065             out.append(t)
L1066     return out
L1067
L1068 def _label_recent_event(t, feature_df):
L1069     try:
L1070         br = bool(feature_df.at[t, "G_BREAKOUT_recent_5d"]); dbr = str(feature_df.at[t, "G_BREAKOUT_last_date"]) if br else ""
L1071         pb = bool(feature_df.at[t, "G_PULLBACK_recent_5d"]); dpb = str(feature_df.at[t, "G_PULLBACK_last_date"]) if pb else ""
L1072         if   br and not pb: return f"{t}（ブレイクアウト確定 {dbr}）"
L1073         elif pb and not br: return f"{t}（押し目反発 {dpb}）"
L1074         elif br and pb:     return f"{t}（ブレイクアウト確定 {dbr}／押し目反発 {dpb}）"
L1075     except Exception:
L1076         pass
L1077     return t
L1078
L1079 # === パイプライン可視化：G/D共通フロー（出力は不変） ===
L1080
L1081 def io_build_input_bundle() -> InputBundle:
L1082     """
L1083     既存の『データ取得→前処理』を実行し、InputBundle を返す。
L1084     処理内容・列名・丸め・例外・ログ文言は現行どおり（変更禁止）。
L1085     """
L1086     state = Input(cand=cand, exist=exist, bench=bench, price_max=CAND_PRICE_MAX, finnhub_api_key=FINNHUB_API_KEY).prepare_data()
L1087     return InputBundle(cand=state["cand"], tickers=state["tickers"], bench=bench, data=state["data"], px=state["px"], spx=state["spx"], tickers_bulk=state["tickers_bulk"], info=state["info"], eps_df=state["eps_df"], fcf_df=state["fcf_df"], returns=state["returns"])
L1088
L1089 def run_group(sc: Scorer, group: str, inb: InputBundle, cfg: PipelineConfig,
L1090               n_target: int) -> tuple[list, float, float, float]:
L1091     """
L1092     G/Dを同一手順で処理：採点→フィルター→選定（相関低減込み）。
L1093     戻り値：(pick, avg_res_corr, sum_score, objective)
L1094     JSON保存は既存フォーマット（キー名・丸め桁・順序）を踏襲。
L1095     """
L1096     sc.cfg = cfg
L1097
L1098     if hasattr(sc, "score_build_features"):
L1099         feat = sc.score_build_features(inb)
L1100         if not hasattr(sc, "_feat_logged"):
L1101            
```